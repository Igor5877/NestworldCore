--- a/net/minecraft/server/level/ServerEntity.java
+++ b/net/minecraft/server/level/ServerEntity.java
@@ -92,10 +_,9 @@
       if ($$11 instanceof ItemFrame itemframe) {
          if (this.f_8521_ % 10 == 0) {
             ItemStack itemstack = itemframe.m_31822_();
-            if (itemstack.m_41720_() instanceof MapItem) {
-               Integer integer = MapItem.m_151131_(itemstack);
-               MapItemSavedData mapitemsaveddata = MapItem.m_151128_(integer, this.f_8509_);
-               if (mapitemsaveddata != null) {
+         Integer integer = MapItem.m_151131_(itemstack);
+         MapItemSavedData mapitemsaveddata = MapItem.m_42853_(itemstack, this.f_8509_);
+         if (mapitemsaveddata != null) {
                   for(ServerPlayer serverplayer : this.f_8509_.m_6907_()) {
                      mapitemsaveddata.m_77918_(serverplayer, itemstack);
                      Packet<?> packet = mapitemsaveddata.m_164796_(integer, serverplayer);
@@ -104,11 +_,12 @@
                      }
                   }
                }
-            }
 
             this.m_8543_();
          }
       }
+
+
 
       if (this.f_8521_ % this.f_8511_ == 0 || this.f_8510_.f_19812_ || this.f_8510_.m_20088_().m_135352_()) {
          if (this.f_8510_.m_20159_()) {
@@ -215,13 +_,18 @@
    public void m_8534_(ServerPlayer p_8535_) {
       this.f_8510_.m_6452_(p_8535_);
       p_8535_.f_8906_.m_9829_(new ClientboundRemoveEntitiesPacket(this.f_8510_.m_19879_()));
+      net.minecraftforge.event.ForgeEventFactory.onStopEntityTracking(this.f_8510_, p_8535_);
    }
 
    public void m_8541_(ServerPlayer p_8542_) {
       List<Packet<ClientGamePacketListener>> list = new ArrayList<>();
       this.m_289200_(p_8542_, list::add);
+      // NestWorld: 2026-08-29 AddPairing Burst Attribution ТЗ -- measurement only, no
+      // batching/async change. See JoinBurstMetrics's javadoc.
+      net.nestworld.region.JoinBurstMetrics.recordPairingDetailed(this.f_8510_, p_8542_, list);
       p_8542_.f_8906_.m_9829_(new ClientboundBundlePacket(list));
       this.f_8510_.m_6457_(p_8542_);
+      net.minecraftforge.event.ForgeEventFactory.onStartEntityTracking(this.f_8510_, p_8542_);
    }
 
    public void m_289200_(ServerPlayer p_289562_, Consumer<Packet<ClientGamePacketListener>> p_289563_) {
@@ -232,8 +_,20 @@
       Packet<ClientGamePacketListener> packet = this.f_8510_.m_5654_();
       this.f_8519_ = Mth.m_14143_(this.f_8510_.m_6080_() * 256.0F / 360.0F);
       p_289563_.accept(packet);
-      if (this.f_263120_ != null) {
-         p_289563_.accept(new ClientboundSetEntityDataPacket(this.f_8510_.m_19879_(), this.f_263120_));
+      // NestWorld: read the entity data FRESH at pairing instead of the cached
+      // trackedDataValues. The cache is captured at ServerEntity construction (main thread)
+      // and only ever refreshed inside sendChanges(). On the region-threaded core an item's
+      // DATA_ITEM is set on a region thread, and a still, not-yet-watched ItemEntity sitting
+      // in a chunk that the main thread reads as not "entity-ticking" never runs
+      // sendChanges() (see ChunkMap tick gate), so the cache keeps its empty construction-time
+      // snapshot forever. A player that later starts tracking the item then receives empty
+      // metadata and the dropped item renders invisible — its visibility depends entirely on
+      // the one-shot DATA_ITEM, unlike mobs/TNT which render from the spawn packet. Reading
+      // getNonDefaultValues() here guarantees every new viewer gets the entity's current
+      // data. On vanilla this equals the cache (single-threaded), so no behavioural change.
+      List<SynchedEntityData.DataValue<?>> nestworldPairingData = this.f_8510_.m_20088_().m_252804_();
+      if (nestworldPairingData != null) {
+         p_289563_.accept(new ClientboundSetEntityDataPacket(this.f_8510_.m_19879_(), nestworldPairingData));
       }
 
       boolean flag = this.f_8512_;
