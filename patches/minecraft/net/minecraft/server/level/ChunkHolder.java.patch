--- a/net/minecraft/server/level/ChunkHolder.java
+++ b/net/minecraft/server/level/ChunkHolder.java
@@ -55,14 +_,21 @@
    private int f_140007_;
    private int f_140008_;
    final ChunkPos f_140009_;
-   private boolean f_140010_;
+   private volatile boolean f_140010_;
    private final ShortSet[] f_140011_;
+   // NestWorld: region threads record block changes (setBlock during a region tick,
+   // incl. cross-region explosions) into changedBlocksPerSection while the main thread
+   // snapshots+iterates it in broadcastChanges -> the single-thread ShortOpenHashSet
+   // corrupts (NPE in SetIterator). This lock makes blockChanged's check-create-add
+   // atomic with broadcastChanges' snapshot-and-clear; packet building stays off-lock.
+   private final java.util.concurrent.locks.ReentrantLock nestworldBlockChangeLock = new java.util.concurrent.locks.ReentrantLock();
    private final BitSet f_140012_ = new BitSet();
    private final BitSet f_140013_ = new BitSet();
    private final LevelLightEngine f_140014_;
    private final ChunkHolder.LevelChangeListener f_140015_;
    private final ChunkHolder.PlayerProvider f_140016_;
    private boolean f_140017_;
+   LevelChunk currentlyLoading; // Forge: Used to bypass future chain when loading chunks.
    private CompletableFuture<Void> f_142981_ = CompletableFuture.completedFuture((Void)null);
 
    public ChunkHolder(ChunkPos p_142986_, int p_142987_, LevelHeightAccessor p_142988_, LevelLightEngine p_142989_, ChunkHolder.LevelChangeListener p_142990_, ChunkHolder.PlayerProvider p_142991_) {
@@ -150,12 +_,17 @@
       LevelChunk levelchunk = this.m_140085_();
       if (levelchunk != null) {
          int i = this.f_142983_.m_151564_(p_140057_.m_123342_());
-         if (this.f_140011_[i] == null) {
-            this.f_140010_ = true;
-            this.f_140011_[i] = new ShortOpenHashSet();
-         }
+         this.nestworldBlockChangeLock.lock();
+         try {
+            if (this.f_140011_[i] == null) {
+               this.f_140010_ = true;
+               this.f_140011_[i] = new ShortOpenHashSet();
+            }
 
-         this.f_140011_[i].add(SectionPos.m_123218_(p_140057_));
+            this.f_140011_[i].add(SectionPos.m_123218_(p_140057_));
+         } finally {
+            this.nestworldBlockChangeLock.unlock();
+         }
       }
    }
 
@@ -200,10 +_,19 @@
          if (this.f_140010_) {
             List<ServerPlayer> list1 = this.f_140016_.m_183262_(this.f_140009_, false);
 
-            for(int j = 0; j < this.f_140011_.length; ++j) {
-               ShortSet shortset = this.f_140011_[j];
+            ShortSet[] nestworldSets;
+            this.nestworldBlockChangeLock.lock();
+            try {
+               nestworldSets = this.f_140011_.clone();
+               java.util.Arrays.fill(this.f_140011_, (ShortSet)null);
+               this.f_140010_ = false;
+            } finally {
+               this.nestworldBlockChangeLock.unlock();
+            }
+
+            for(int j = 0; j < nestworldSets.length; ++j) {
+               ShortSet shortset = nestworldSets[j];
                if (shortset != null) {
-                  this.f_140011_[j] = null;
                   if (!list1.isEmpty()) {
                      int i = this.f_142983_.m_151568_(j);
                      SectionPos sectionpos = SectionPos.m_123196_(p_140055_.m_7697_(), i);
@@ -223,8 +_,6 @@
                   }
                }
             }
-
-            this.f_140010_ = false;
          }
       }
    }
@@ -269,10 +_,34 @@
       }
 
       if (ChunkLevel.m_287158_(this.f_140007_).m_62427_(p_140050_)) {
-         CompletableFuture<Either<ChunkAccess, ChunkHolder.ChunkLoadingFailure>> completablefuture1 = p_140051_.m_140292_(this, p_140050_);
-         this.m_143017_(completablefuture1, "schedule " + p_140050_);
-         this.f_140001_.set(i, completablefuture1);
-         return completablefuture1;
+         // NestWorld: gate brand-new top-level scheduling admissions (see
+         // NestworldTuning.CHUNK_GEN_ADMIT_BUDGET). No-op when disabled (default), when
+         // called mid-cascade, or when player-driven — nestworldTryScheduleNow bypasses
+         // the check entirely in those cases. Bounded by a per-window admit RATE and,
+         // independently, an optional standing in-flight concurrency cap (NestworldTuning.
+         // CHUNK_GEN_ADMIT_INFLIGHT_CAP, default unlimited/off) for operators who need
+         // backlog across successive bursts bounded harder than the rate alone provides.
+         final ChunkStatus nestworldStatus = p_140050_;
+         final ChunkMap nestworldMap = p_140051_;
+         CompletableFuture<Either<ChunkAccess, ChunkHolder.ChunkLoadingFailure>> nestworldNow =
+               nestworldMap.nestworldTryScheduleNow(this, () -> nestworldMap.m_140292_(this, nestworldStatus));
+         if (nestworldNow != null) {
+            this.m_143017_(nestworldNow, "schedule " + nestworldStatus);
+            this.f_140001_.set(i, nestworldNow);
+            return nestworldNow;
+         }
+         CompletableFuture<Either<ChunkAccess, ChunkHolder.ChunkLoadingFailure>> nestworldPending = new CompletableFuture<>();
+         this.f_140001_.set(i, nestworldPending);
+         nestworldMap.nestworldDeferAdmission(() -> {
+            CompletableFuture<Either<ChunkAccess, ChunkHolder.ChunkLoadingFailure>> nestworldReal = nestworldMap.m_140292_(this, nestworldStatus);
+            this.m_143017_(nestworldReal, "schedule(deferred) " + nestworldStatus);
+            nestworldReal.whenComplete((nestworldResult, nestworldErr) -> {
+               nestworldMap.nestworldOnResolved(); // NestWorld: release the in-flight slot
+               if (nestworldErr != null) nestworldPending.completeExceptionally(nestworldErr);
+               else nestworldPending.complete(nestworldResult);
+            });
+         });
+         return nestworldPending;
       } else {
          return completablefuture == null ? f_139996_ : completablefuture;
       }
