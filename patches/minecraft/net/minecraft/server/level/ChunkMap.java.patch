--- a/net/minecraft/server/level/ChunkMap.java
+++ b/net/minecraft/server/level/ChunkMap.java
@@ -130,6 +_,10 @@
    private boolean f_140140_;
    private final ChunkTaskPriorityQueueSorter f_140141_;
    private final ProcessorHandle<ChunkTaskPriorityQueueSorter.Message<Runnable>> f_140142_;
+   // NestWorld: parallel worldgen-mailbox shards (see NestworldTuning.WORLDGEN_SHARDS).
+   // worldgenMailbox (above) is always shard 0 and stays the field used by anything that
+   // doesn't need to know sharding exists; nestworldWorldgenShards[0] == worldgenMailbox.
+   private final ProcessorHandle<ChunkTaskPriorityQueueSorter.Message<Runnable>>[] nestworldWorldgenShards;
    private final ProcessorHandle<ChunkTaskPriorityQueueSorter.Message<Runnable>> f_140143_;
    private final ChunkProgressListener f_140144_;
    private final ChunkStatusUpdateListener f_143031_;
@@ -143,6 +_,345 @@
    private final Long2LongMap f_202981_ = new Long2LongOpenHashMap();
    private final Queue<Runnable> f_140125_ = Queues.newConcurrentLinkedQueue();
    int f_140126_;
+   // NestWorld (E1): reused scratch for the Phase-1 parallel detection scan in tick() — see there.
+   // Only touched on the main thread within tick(); workers write disjoint slots claimed via the
+   // atomic cursors.
+   private ChunkMap.TrackedEntity[] nestworldBcArr = new ChunkMap.TrackedEntity[0];
+   private ChunkMap.TrackedEntity[] nestworldChangedArr = new ChunkMap.TrackedEntity[0];
+   private final java.util.concurrent.atomic.AtomicInteger nestworldBcCursor = new java.util.concurrent.atomic.AtomicInteger();
+   private final java.util.concurrent.atomic.AtomicInteger nestworldChangedCursor = new java.util.concurrent.atomic.AtomicInteger();
+
+   // NestWorld: caps how many BRAND-NEW (holder,status) chunk-scheduling units are
+   // admitted into generation per ~tick-length WALL-CLOCK window, for bulk (non-player)
+   // top-level requests. See NestworldTuning.CHUNK_GEN_ADMIT_BUDGET for the rationale.
+   //
+   // CRITICAL DESIGN NOTE: the budget window is WALL-CLOCK, not "once per server tick".
+   // Deferred admissions are drained from ServerChunkCache's OWN pollTask() (both the
+   // outer wrapper and the MainThreadExecutor inner override) — that's the one point
+   // reached by EVERY synchronous wait in the game: MinecraftServer's startup/tick
+   // waitUntilNextTick loop, AND mainThreadProcessor's managedBlock loop for a
+   // synchronous getChunk(load=true) — plus once/tick from ChunkMap#tick as a
+   // steady-state fallback for non-blocking callers. A tick()-only drain is UNSOUND:
+   // the very call that gets deferred can be the one a synchronous caller is
+   // managedBlock-waiting on, and managedBlock blocks the current tick from ever
+   // finishing — so a drain that only runs at the top of the NEXT tick never runs at
+   // all (confirmed by testing: hung forever, 0% CPU, even a fresh server's normal
+   // spawn-chunk prep, which never calls ChunkMap#tick at all). Draining from pollTask()
+   // sidesteps this: it makes progress on every poll regardless of which wait loop or
+   // tick boundary called it. All access is main-thread-only (see nestworldTryScheduleNow).
+   private long nestworldAdmitWindowStartNanos = 0L;
+   private int nestworldAdmitWindowRemaining = Integer.MAX_VALUE;
+   private static final long NESTWORLD_ADMIT_WINDOW_NANOS = 50_000_000L; // ~1 tick at 20 TPS
+   private final ThreadLocal<Boolean> nestworldInCascade = ThreadLocal.withInitial(() -> Boolean.FALSE);
+   private final java.util.ArrayDeque<Runnable> nestworldDeferredAdmissions = new java.util.ArrayDeque<>();
+   // NestWorld: a per-window admit RATE alone isn't enough to bound backlog across MANY
+   // separate bursts (each passes its own rate check independently) — this tracks how
+   // many bulk (holder,status) units are currently admitted but not yet resolved (FULL
+   // or failed). 2026-08-07: this used to be capped against CHUNK_GEN_ADMIT_BUDGET
+   // itself (the SAME small number as the rate), which is what made ordinary forceload
+   // bursts serialize into many sequential rounds and take 20s+ for what should be
+   // sub-second work (see NestworldTuning.CHUNK_GEN_ADMIT_BUDGET's javadoc for the
+   // measured before/after). Now checked against the independently-configured
+   // NestworldTuning.CHUNK_GEN_ADMIT_INFLIGHT_CAP instead (0 = no standing cap, only
+   // the rate paces admission) — see that constant's javadoc for the reasoning.
+   private int nestworldInFlight = 0;
+
+   private void nestworldRefreshAdmitWindow() {
+      long nestworldNow = System.nanoTime();
+      if (nestworldNow - this.nestworldAdmitWindowStartNanos >= NESTWORLD_ADMIT_WINDOW_NANOS) {
+         this.nestworldAdmitWindowStartNanos = nestworldNow;
+         this.nestworldAdmitWindowRemaining = net.nestworld.region.NestworldTuning.CHUNK_GEN_ADMIT_BUDGET <= 0
+               ? Integer.MAX_VALUE : net.nestworld.region.NestworldTuning.CHUNK_GEN_ADMIT_BUDGET;
+      }
+   }
+
+   /**
+    * NestWorld: run {@code p_nw_real} (the actual schedule() call) right now and
+    * return its future, UNLESS bulk (holder,status) admission capacity is exhausted —
+    * in which case this does nothing and returns null (caller must defer). Player-
+    * driven tickets, cascade-internal (dependency/neighbour) calls, and the cap being
+    * disabled all bypass the check entirely (no bookkeeping needed — nothing to leak).
+    * A genuine capacity-gated admission atomically consumes rate + in-flight budget and
+    * releases the in-flight slot when the resulting future resolves — admit and release
+    * live in the same method so they can never drift out of sync.
+    */
+   @Nullable
+   CompletableFuture<Either<ChunkAccess, ChunkHolder.ChunkLoadingFailure>> nestworldTryScheduleNow(
+         ChunkHolder p_nw_holder, java.util.function.Supplier<CompletableFuture<Either<ChunkAccess, ChunkHolder.ChunkLoadingFailure>>> p_nw_real) {
+      if (net.nestworld.region.NestworldTuning.CHUNK_GEN_ADMIT_BUDGET <= 0
+            || Boolean.TRUE.equals(this.nestworldInCascade.get())
+            || this.f_140145_.nestworldHasPlayerTicket(p_nw_holder.m_140092_().m_45588_())) {
+         return p_nw_real.get();
+      }
+      if (!this.nestworldHasAdmitCapacity()) return null;
+      this.nestworldOnAdmitted();
+      // NestWorld: p_nw_real.get() can throw SYNCHRONOUSLY — it runs arbitrary vanilla/mod
+      // (holder,status) scheduling code, not just return an already-built future. If it
+      // throws before returning anything, there is no future to attach whenComplete to,
+      // so nestworldOnResolved() would never run and this in-flight slot would leak
+      // forever (CONFIRMED as a real bug class: reproduced an analogous leak in
+      // NestworldGenPool.gate() — see its comment — on a real 188-mod pack; a single
+      // mod's synchronous throw during a gated generate() call froze that pool permanently
+      // since nothing ever released the slot). Must release + fail our own returned future
+      // here too, not just on the async path.
+      CompletableFuture<Either<ChunkAccess, ChunkHolder.ChunkLoadingFailure>> nestworldF;
+      try {
+         nestworldF = p_nw_real.get();
+      } catch (Throwable nestworldT) {
+         this.nestworldOnResolved();
+         CompletableFuture<Either<ChunkAccess, ChunkHolder.ChunkLoadingFailure>> nestworldFailed = new CompletableFuture<>();
+         nestworldFailed.completeExceptionally(nestworldT);
+         return nestworldFailed;
+      }
+      nestworldF.whenComplete((nestworldR, nestworldE) -> this.nestworldOnResolved());
+      return nestworldF;
+   }
+
+   /** NestWorld: true if both the per-window rate AND the standing in-flight
+    *  concurrency cap (independently configured — see NestworldTuning.
+    *  CHUNK_GEN_ADMIT_INFLIGHT_CAP) have room for one more bulk admission right now. */
+   private boolean nestworldHasAdmitCapacity() {
+      this.nestworldRefreshAdmitWindow();
+      if (this.nestworldAdmitWindowRemaining <= 0) return false;
+      int nestworldInflightCap = net.nestworld.region.NestworldTuning.CHUNK_GEN_ADMIT_INFLIGHT_CAP;
+      return nestworldInflightCap <= 0 || this.nestworldInFlight < nestworldInflightCap;
+   }
+
+   /** NestWorld: record one bulk admission — consumes rate-window budget and occupies
+    *  one in-flight slot until the caller reports completion via nestworldOnResolved. */
+   private void nestworldOnAdmitted() {
+      this.nestworldAdmitWindowRemaining--;
+      this.nestworldInFlight++;
+   }
+
+   /** NestWorld: release the in-flight slot once an admitted unit's real future
+    *  resolves (success or failure) — called via whenComplete on that future. */
+   void nestworldOnResolved() {
+      this.nestworldInFlight--;
+   }
+
+   /** NestWorld: queue a gated (holder,status) scheduling unit to retry once budget frees up. */
+   void nestworldDeferAdmission(Runnable p_nw_retry) {
+      this.nestworldDeferredAdmissions.add(p_nw_retry);
+   }
+
+   /**
+    * NestWorld: drain deferred (holder,status) scheduling units under both the
+    * wall-clock admit window AND the in-flight concurrency cap. Safe (and required) to
+    * call repeatedly from within an active managedBlock wait — see the field-group
+    * comment above for why a tick-only drain cannot work. Also called once per tick
+    * from ChunkMap#tick as the steady-state driver for deferrals whose original caller
+    * wasn't itself blocking (e.g. an off-main-thread getChunkFuture request).
+    */
+   void nestworldDrainDeferredAdmissions() {
+      if (this.nestworldDeferredAdmissions.isEmpty()) return;
+      while (this.nestworldHasAdmitCapacity() && !this.nestworldDeferredAdmissions.isEmpty()) {
+         this.nestworldOnAdmitted();
+         this.nestworldDeferredAdmissions.poll().run();
+      }
+   }
+
+   // NestWorld: bounds how many chunk-status GENERATION calls (the actual CPU-bound
+   // NOISE/Aquifer/feature-placement work) run concurrently across the worker pool. See
+   // NestworldTuning.CHUNK_GEN_MAX_CONCURRENT.
+   //
+   // THREAD SAFETY: unlike every other nestworld* field in this class (main-thread-
+   // only), this is touched from WORKER threads too — the thenComposeAsync callback
+   // that calls generate() runs on worldgenMailbox, not main — hence the synchronized
+   // blocks guarding both fields below.
+   //
+   // ONLY the statuses in NESTWORLD_GATEABLE_STATUSES are ever gated — this was NOT
+   // the original design and its narrowing is safety-critical, not a tuning choice.
+   // Verified by reading every ChunkStatus's generationTask body: NOISE/SURFACE/
+   // CARVERS/FEATURES (and every status except FULL) never invoke the passed
+   // protoChunkToFullChunk-style callback — they're pure leaf computations over the
+   // already-fetched neighbour list (fetched via getChunkRangeFuture, itself already
+   // cascade-exempt — see that method's comment), so a queued task here holds no
+   // partial state another queued task could be waiting on. FULL's generationTask is
+   // literally `callback.apply(chunk)` — invoking protoChunkToFullChunk, which reads
+   // THIS SAME chunk's own prior-status future via getFutureIfPresentUnchecked. Gating
+   // FULL is a genuine self-referential deadlock: a queued FULL holds a slot while
+   // waiting on its own parent status's future, and that parent can be stuck behind
+   // FULL's own slot in the queue. CONFIRMED by testing, not just reasoned: gating FULL
+   // hung a completely idle fresh-server startup indefinitely (jstack showed the only
+   // active worker thread vanish, zero generation activity, 70+s at 0% progress).
+   // FULL also does zero CPU-heavy work itself (pure pass-through), so excluding it —
+   // and staying with an explicit allowlist rather than a FULL-only blocklist, in case
+   // some other status turns out to have a similar hidden dependency — costs nothing
+   // against the throttling goal.
+   private static final java.util.Set<ChunkStatus> NESTWORLD_GATEABLE_STATUSES = java.util.Set.of(
+         ChunkStatus.f_62318_, ChunkStatus.f_62319_, ChunkStatus.f_62320_, ChunkStatus.f_62322_);
+
+   /**
+    * NestWorld: a counting semaphore-like admission gate — at most {@code capacity}
+    * callers may be "admitted" (running) at once; the rest queue and are released
+    * one-for-one as admitted callers finish. Extracted so FEATURES can get its OWN
+    * independently-configured pool (historically hard-capped at 1 to work around a
+    * since-fixed BulkSectionAccess deadlock; now just a normal throughput knob — see
+    * CHUNK_GEN_FEATURES_MAX_CONCURRENT's javadoc), separate from NOISE/SURFACE/CARVERS's
+    * pool.
+    *
+    * <p>CRITICAL: {@code release()}'s hand-off to the next queued task runs on
+    * WHATEVER thread completes the admitted task's future (via {@code whenComplete},
+    * no explicit executor) — NOT necessarily the thread that originally admitted it.
+    * A pool with capacity &gt; 1 is fine with that (the mutual-exclusion guarantee is
+    * "at most `capacity` running", not "always on one thread"); a pool that MUST
+    * guarantee "never two callers concurrently, no matter which thread" needs
+    * capacity exactly 1 — the slot-count itself is what enforces exclusion then, not
+    * which thread happens to run the release-triggered task.
+    *
+    * <p>CONFIRMED BY A REAL CRASH (real 188-mod pack, not a synthetic test): {@code
+    * real.get()} runs arbitrary vanilla/mod (holder,status) generation code — for
+    * registerSimple-registered statuses (SURFACE/CARVERS/FEATURES) this runs
+    * SYNCHRONOUSLY inline, including third-party Feature-placement code (GregTech,
+    * Mekanism, Lost Cities all touch chunk data directly here). If {@code real.get()}
+    * throws — a plain Exception OR an Error (NoClassDefFoundError, StackOverflowError
+    * etc. are all real possibilities from arbitrary mod code) — BEFORE returning
+    * anything, there is no future to attach {@code whenComplete} to, so this slot is
+    * never released and the caller's returned future never completes: with FEATURES'
+    * pool hard-capped at 1, a SINGLE such throw anywhere in a 188-mod pack permanently
+    * freezes ALL subsequent FEATURES generation forever (matches an observed 60s
+    * ServerHangWatchdog kill with 8/9 worker threads idle and nothing queued — a
+    * "lost admission", not lock contention). Every path that can run {@code real.get()}
+    * — immediate admission AND the deferred release-triggered path, which reuses the
+    * SAME Runnable — must be exception-safe.
+    */
+   private static final class NestworldGenPool {
+      private int capacity;
+      private int slotsFree;
+      private final java.util.ArrayDeque<Runnable> pending = new java.util.ArrayDeque<>();
+
+      NestworldGenPool(int capacity) { this.capacity = capacity; this.slotsFree = capacity; }
+
+      // NestWorld (Layer 13.1, 2026-08-11): live capacity change, no restart needed --
+      // built specifically to run a clean concurrency-scaling benchmark on one warmed-up
+      // JVM instead of restarting (and re-paying a JIT-compilation storm) before every
+      // data point. Shrinking just lets slotsFree go negative (self-corrects as running
+      // tasks release(); no in-flight work is touched). Growing immediately drains and
+      // dispatches as many pending tasks as the new headroom allows, off-thread for the
+      // same reason release() does -- see its comment.
+      void setCapacity(int newCapacity) {
+         newCapacity = Math.max(0, newCapacity);
+         java.util.List<Runnable> toRun = new java.util.ArrayList<>();
+         synchronized (this.pending) {
+            int delta = newCapacity - this.capacity;
+            this.capacity = newCapacity;
+            this.slotsFree += delta;
+            while (this.slotsFree > 0 && !this.pending.isEmpty()) {
+               this.slotsFree--;
+               toRun.add(this.pending.poll());
+            }
+         }
+         for (Runnable r : toRun) Util.m_183991_().execute(r);
+      }
+
+      int capacity() {
+         synchronized (this.pending) { return this.capacity; }
+      }
+
+      int pendingCount() {
+         synchronized (this.pending) { return this.pending.size(); }
+      }
+
+      CompletableFuture<Either<ChunkAccess, ChunkHolder.ChunkLoadingFailure>> gate(
+            java.util.function.Supplier<CompletableFuture<Either<ChunkAccess, ChunkHolder.ChunkLoadingFailure>>> real) {
+         CompletableFuture<Either<ChunkAccess, ChunkHolder.ChunkLoadingFailure>> pendingFuture = new CompletableFuture<>();
+         Runnable task = () -> {
+            CompletableFuture<Either<ChunkAccess, ChunkHolder.ChunkLoadingFailure>> realFuture;
+            try {
+               realFuture = real.get();
+            } catch (Throwable t) {
+               this.release();
+               pendingFuture.completeExceptionally(t);
+               return;
+            }
+            realFuture.whenComplete((r, e) -> {
+               this.release();
+               if (e != null) pendingFuture.completeExceptionally(e);
+               else pendingFuture.complete(r);
+            });
+         };
+         boolean runNow;
+         synchronized (this.pending) {
+            if (this.slotsFree > 0) {
+               this.slotsFree--;
+               runNow = true;
+            } else {
+               this.pending.add(task);
+               runNow = false;
+            }
+         }
+         if (runNow) task.run();
+         return pendingFuture;
+      }
+
+      private void release() {
+         Runnable next;
+         synchronized (this.pending) {
+            next = this.pending.poll();
+            // hand the freed slot directly to the next queued task rather than
+            // incrementing-then-decrementing — avoids a window where a concurrent
+            // acquirer could steal the slot ahead of an already-queued task
+            if (next == null) this.slotsFree++;
+         }
+         // NestWorld: MUST dispatch off-thread, not next.run() inline. For
+         // registerSimple-registered statuses (SURFACE/CARVERS/FEATURES), real.get()
+         // in `task` frequently returns an ALREADY-COMPLETED future, so
+         // realFuture.whenComplete(...) in gate() fires synchronously on THIS thread —
+         // which calls release() again, which can hand off to yet another queued task,
+         // and so on. Under a deep backlog (e.g. several stacked chunk-load bursts) this
+         // unwound into an unbounded same-thread release->run->release->run chain that
+         // pinned the calling thread in real, synchronous FEATURES generation for a
+         // whole 60s tick until ServerWatchdog force-killed the server (CONFIRMED via
+         // live jstack + crash-report stack trace, repeating
+         // NestworldGenPool.release/lambda$gate$0/whenComplete/lambda$gate$1 frames).
+         // Handing the continuation to the shared background pool breaks the chain: each
+         // release() returns immediately and the next task starts on a fresh stack.
+         if (next != null) Util.m_183991_().execute(next);
+      }
+   }
+
+   // Shared pool for NOISE/SURFACE/CARVERS — none construct a BulkSectionAccess (see
+   // NestworldTuning.WORLDGEN_SHARDS's javadoc), so any concurrency up to
+   // CHUNK_GEN_MAX_CONCURRENT among them is safe.
+   private final NestworldGenPool nestworldNoiseSurfaceCarversPool =
+         new NestworldGenPool(Math.max(0, net.nestworld.region.NestworldTuning.CHUNK_GEN_MAX_CONCURRENT));
+   // FEATURES gets its OWN pool, independently tunable via CHUNK_GEN_FEATURES_MAX_CONCURRENT
+   // (NOT tied to CHUNK_GEN_MAX_CONCURRENT — real modpacks can make FEATURES far more
+   // expensive per-chunk than NOISE/SURFACE/CARVERS). Used to be hard-capped at exactly 1
+   // regardless of configuration, working around a since-fixed AB-BA deadlock in
+   // BulkSectionAccess (see that class's own comment, and NestworldTuning.
+   // CHUNK_GEN_FEATURES_MAX_CONCURRENT's javadoc for the full history) — that hazard is now
+   // fixed at its root, so this is a normal throughput knob like the pool below, not a
+   // safety mechanism.
+   private final NestworldGenPool nestworldFeaturesPool =
+         new NestworldGenPool(Math.max(0, net.nestworld.region.NestworldTuning.CHUNK_GEN_FEATURES_MAX_CONCURRENT));
+
+   // NestWorld (Layer 13.1): live concurrency-cap accessors for benchmarking without a
+   // restart. NestworldTuning.CHUNK_GEN_MAX_CONCURRENT's own value (read once at JVM
+   // start) only decides whether the shared pool gates at ALL (see
+   // nestworldGateGeneration's `<= 0` check) -- boot with any positive value (the lowest
+   // level you intend to test) and then drive the actual concurrency level live from here.
+   public void nestworldSetGenConcurrency(int n) { this.nestworldNoiseSurfaceCarversPool.setCapacity(n); }
+   public int nestworldGenConcurrency() { return this.nestworldNoiseSurfaceCarversPool.capacity(); }
+   public int nestworldGenPoolPending() { return this.nestworldNoiseSurfaceCarversPool.pendingCount(); }
+
+   /**
+    * NestWorld: run {@code p_nw_real} (the real generate() call) now if {@code
+    * p_nw_status} is gateable and a concurrency slot is free in ITS pool; otherwise
+    * (ungateable status, or gateable but no free slot) run immediately or queue to run
+    * once another gated generation in the same pool completes and releases its slot.
+    */
+   private CompletableFuture<Either<ChunkAccess, ChunkHolder.ChunkLoadingFailure>> nestworldGateGeneration(
+         ChunkStatus p_nw_status, java.util.function.Supplier<CompletableFuture<Either<ChunkAccess, ChunkHolder.ChunkLoadingFailure>>> p_nw_real) {
+      if (p_nw_status == ChunkStatus.f_62322_) {
+         return net.nestworld.region.NestworldTuning.CHUNK_GEN_FEATURES_MAX_CONCURRENT <= 0
+               ? p_nw_real.get() : this.nestworldFeaturesPool.gate(p_nw_real);
+      }
+      if (net.nestworld.region.NestworldTuning.CHUNK_GEN_MAX_CONCURRENT <= 0) return p_nw_real.get();
+      if (!NESTWORLD_GATEABLE_STATUSES.contains(p_nw_status)) return p_nw_real.get();
+      return this.nestworldNoiseSurfaceCarversPool.gate(p_nw_real);
+   }
 
    public ChunkMap(ServerLevel p_214836_, LevelStorageSource.LevelStorageAccess p_214837_, DataFixer p_214838_, StructureTemplateManager p_214839_, Executor p_214840_, BlockableEventLoop<Runnable> p_214841_, LightChunkGetter p_214842_, ChunkGenerator p_214843_, ChunkProgressListener p_214844_, ChunkStatusUpdateListener p_214845_, Supplier<DimensionDataStorage> p_214846_, int p_214847_, boolean p_214848_) {
       super(p_214837_.m_197394_(p_214836_.m_46472_()).resolve("region"), p_214838_, p_214848_);
@@ -161,13 +_,30 @@
 
       this.f_254626_ = p_214843_.m_255169_(registryaccess.m_255025_(Registries.f_256998_), this.f_214834_, i);
       this.f_140135_ = p_214841_;
-      ProcessorMailbox<Runnable> processormailbox1 = ProcessorMailbox.m_18751_(p_214840_, "worldgen");
+      // NestWorld: N raw worldgen mailboxes instead of 1 — see NestworldTuning.WORLDGEN_SHARDS.
+      // Each is independently registered with queueSorter (which keys its internal
+      // ChunkTaskPriorityQueue map by ProcessorHandle identity — verified by reading
+      // ChunkTaskPriorityQueueSorter.getQueue — so N registrations is a supported shape).
+      int nestworldShardCount = net.nestworld.region.NestworldTuning.WORLDGEN_SHARDS;
+      ProcessorMailbox<Runnable>[] nestworldRawWorldgenShards = new ProcessorMailbox[nestworldShardCount];
+      for (int nestworldI = 0; nestworldI < nestworldShardCount; nestworldI++) {
+         nestworldRawWorldgenShards[nestworldI] = ProcessorMailbox.m_18751_(p_214840_,
+               nestworldShardCount == 1 ? "worldgen" : "worldgen-" + nestworldI);
+      }
       ProcessorHandle<Runnable> processorhandle = ProcessorHandle.m_18714_("main", p_214841_::m_6937_);
       this.f_140144_ = p_214844_;
       this.f_143031_ = p_214845_;
       ProcessorMailbox<Runnable> processormailbox = ProcessorMailbox.m_18751_(p_214840_, "light");
-      this.f_140141_ = new ChunkTaskPriorityQueueSorter(ImmutableList.of(processormailbox1, processorhandle, processormailbox), p_214840_, Integer.MAX_VALUE);
-      this.f_140142_ = this.f_140141_.m_140604_(processormailbox1, false);
+      java.util.List<ProcessorHandle<?>> nestworldSorterProcessors = new java.util.ArrayList<>(nestworldShardCount + 2);
+      for (ProcessorMailbox<Runnable> nestworldShard : nestworldRawWorldgenShards) nestworldSorterProcessors.add(nestworldShard);
+      nestworldSorterProcessors.add(processorhandle);
+      nestworldSorterProcessors.add(processormailbox);
+      this.f_140141_ = new ChunkTaskPriorityQueueSorter(ImmutableList.copyOf(nestworldSorterProcessors), p_214840_, Integer.MAX_VALUE);
+      this.nestworldWorldgenShards = new ProcessorHandle[nestworldShardCount];
+      for (int nestworldI = 0; nestworldI < nestworldShardCount; nestworldI++) {
+         this.nestworldWorldgenShards[nestworldI] = this.f_140141_.m_140604_(nestworldRawWorldgenShards[nestworldI], false);
+      }
+      this.f_140142_ = this.nestworldWorldgenShards[0];
       this.f_140143_ = this.f_140141_.m_140604_(processorhandle, false);
       this.f_140134_ = new ThreadedLevelLightEngine(p_214842_, this, this.f_140133_.m_6042_().f_223549_(), processormailbox, this.f_140141_.m_140604_(processormailbox, false));
       this.f_140145_ = new ChunkMap.DistanceManager(p_214840_, p_214841_);
@@ -268,6 +_,28 @@
    }
 
    private CompletableFuture<Either<List<ChunkAccess>, ChunkHolder.ChunkLoadingFailure>> m_280541_(ChunkHolder p_281446_, int p_282030_, IntFunction<ChunkStatus> p_282923_) {
+      // NestWorld: mark cascade for the WHOLE range-neighbour walk, not just schedule()'s
+      // own recursion — this method is also called directly from prepareAccessibleChunk/
+      // prepareTickingChunk/prepareEntityTickingChunk (via ChunkHolder#updateFutures),
+      // completely outside any schedule() call. Those neighbour fetches are dependencies
+      // of an ALREADY-admitted holder (admitted earlier by chunkGenBudget's own gate in
+      // DistanceManager), so re-gating them here at the admit-budget layer is not just
+      // redundant, it's actively unsafe: with the admit-budget's concurrency cap small
+      // and these calls NOT exempted, several already-in-flight holders can end up
+      // mutually stuck waiting on each other's un-admitted neighbours with no forward
+      // progress — confirmed by testing: every worker thread idle, 0% progress, for as
+      // long as budget stayed on (this is exactly the deadlock class GEN_SPIKE.md's
+      // original design note warned a badly-scoped cap could cause).
+      boolean nestworldOuter = !Boolean.TRUE.equals(this.nestworldInCascade.get());
+      if (nestworldOuter) this.nestworldInCascade.set(Boolean.TRUE);
+      try {
+      return this.nestworldGetChunkRangeFutureInner(p_281446_, p_282030_, p_282923_);
+      } finally {
+         if (nestworldOuter) this.nestworldInCascade.set(Boolean.FALSE);
+      }
+   }
+
+   private CompletableFuture<Either<List<ChunkAccess>, ChunkHolder.ChunkLoadingFailure>> nestworldGetChunkRangeFutureInner(ChunkHolder p_281446_, int p_282030_, IntFunction<ChunkStatus> p_282923_) {
       if (p_282030_ == 0) {
          ChunkStatus chunkstatus1 = p_282923_.apply(0);
          return p_281446_.m_140049_(chunkstatus1, this).thenApply((p_214893_) -> {
@@ -398,6 +_,7 @@
             this.f_140140_ = true;
          }
 
+         net.minecraftforge.event.ForgeEventFactory.fireChunkTicketLevelUpdated(this.f_140133_, p_140177_, p_140180_, p_140178_, p_140179_);
          return p_140179_;
       }
    }
@@ -445,6 +_,7 @@
    }
 
    protected void m_140280_(BooleanSupplier p_140281_) {
+      this.nestworldDrainDeferredAdmissions(); // NestWorld: steady-state drain (see field-group comment above)
       ProfilerFiller profilerfiller = this.f_140133_.m_46473_();
       profilerfiller.m_6180_("poi");
       this.f_140138_.m_6202_(p_140281_);
@@ -503,6 +_,17 @@
             if (this.f_140131_.remove(p_140182_, p_140183_) && p_203002_ != null) {
                if (p_203002_ instanceof LevelChunk) {
                   ((LevelChunk)p_203002_).m_62913_(false);
+                  // NestWorld: fires Forgified Fabric API's ServerChunkEvents.CHUNK_UNLOAD
+                  // directly (source-level, not a Mixin) at the exact point
+                  // fabric-lifecycle-events-v1's own Mixin used to inject (confirmed via
+                  // its @At(INVOKE, target=LevelChunk.setLoaded(Z)V, shift=AFTER)) --
+                  // that Mixin targets this lambda by its vanilla compiler-assigned
+                  // ordinal (lambda$scheduleUnload$14 / SRG lambda$scheduleUnload$14), which NestWorld's
+                  // own earlier-declared lambdas shift to $16, permanently breaking the
+                  // Mixin's injection target. See NestworldFabricChunkEventsCompat and
+                  // project memory for the full trace. No-op when that mod isn't present.
+                  net.nestworld.region.NestworldFabricChunkEventsCompat.fireChunkUnload(this.f_140133_, (LevelChunk)p_203002_);
+                  net.minecraftforge.common.MinecraftForge.EVENT_BUS.post(new net.minecraftforge.event.level.ChunkEvent.Unload(p_203002_));
                }
 
                this.m_140258_(p_203002_);
@@ -537,26 +_,36 @@
    }
 
    public CompletableFuture<Either<ChunkAccess, ChunkHolder.ChunkLoadingFailure>> m_140292_(ChunkHolder p_140293_, ChunkStatus p_140294_) {
-      ChunkPos chunkpos = p_140293_.m_140092_();
-      if (p_140294_ == ChunkStatus.f_62314_) {
-         return this.m_140417_(chunkpos);
-      } else {
-         if (p_140294_ == ChunkStatus.f_62323_) {
-            this.f_140145_.m_140792_(TicketType.f_9446_, chunkpos, ChunkLevel.m_287141_(ChunkStatus.f_62323_), chunkpos);
-         }
-
-         if (!p_140294_.m_280148_()) {
-            Optional<ChunkAccess> optional = p_140293_.m_140049_(p_140294_.m_62482_(), this).getNow(ChunkHolder.f_139995_).left();
-            if (optional.isPresent() && optional.get().m_6415_().m_62427_(p_140294_)) {
-               CompletableFuture<Either<ChunkAccess, ChunkHolder.ChunkLoadingFailure>> completablefuture = p_140294_.m_223244_(this.f_140133_, this.f_214833_, this.f_140134_, (p_203081_) -> {
-                  return this.m_140383_(p_140293_);
-               }, optional.get());
-               this.f_140144_.m_5511_(chunkpos, p_140294_);
-               return completablefuture;
-            }
-         }
-
-         return this.m_140360_(p_140293_, p_140294_);
+      // NestWorld: mark the extent of this scheduling cascade so any recursive
+      // getOrScheduleFuture calls it makes (parent status, neighbour range) are never
+      // gated by nestworldTryAdmitNewSchedule — only the outermost (top-level) call is
+      // subject to the admit budget. See NestworldTuning.CHUNK_GEN_ADMIT_BUDGET.
+      boolean nestworldOuter = !Boolean.TRUE.equals(this.nestworldInCascade.get());
+      if (nestworldOuter) this.nestworldInCascade.set(Boolean.TRUE);
+      try {
+         ChunkPos chunkpos = p_140293_.m_140092_();
+         if (p_140294_ == ChunkStatus.f_62314_) {
+            return this.m_140417_(chunkpos);
+         } else {
+            if (p_140294_ == ChunkStatus.f_62323_) {
+               this.f_140145_.m_140792_(TicketType.f_9446_, chunkpos, ChunkLevel.m_287141_(ChunkStatus.f_62323_), chunkpos);
+            }
+
+            if (!p_140294_.m_280148_()) {
+               Optional<ChunkAccess> optional = p_140293_.m_140049_(p_140294_.m_62482_(), this).getNow(ChunkHolder.f_139995_).left();
+               if (optional.isPresent() && optional.get().m_6415_().m_62427_(p_140294_)) {
+                  CompletableFuture<Either<ChunkAccess, ChunkHolder.ChunkLoadingFailure>> completablefuture = p_140294_.m_223244_(this.f_140133_, this.f_214833_, this.f_140134_, (p_203081_) -> {
+                     return this.m_140383_(p_140293_);
+                  }, optional.get());
+                  this.f_140144_.m_5511_(chunkpos, p_140294_);
+                  return completablefuture;
+               }
+            }
+
+            return this.m_140360_(p_140293_, p_140294_);
+         }
+      } finally {
+         if (nestworldOuter) this.nestworldInCascade.set(Boolean.FALSE);
       }
    }
 
@@ -625,8 +_,26 @@
       this.f_140133_.m_46473_().m_6525_(() -> {
          return "chunkGenerate " + p_140362_;
       });
+      // NestWorld: route to a worldgen-mailbox shard by a COARSE SPATIAL BLOCK, not a raw
+      // chunk-position hash — a raw hash scatters adjacent chunks essentially randomly
+      // across shards, which matters for statuses whose generation can touch a neighbour's
+      // data. All four gateable statuses (NOISE/SURFACE/CARVERS/FEATURES) are routed the
+      // same way now; FEATURES no longer needs the special-cased shard-0-only treatment it
+      // once did — see BulkSectionAccess's own comment for why concurrent FEATURES
+      // placement (ore veins etc., including GregTech/Mekanism/Lost Cities' own custom
+      // placement code, not just vanilla OreFeature) is now deadlock-free by construction
+      // regardless of which shard a chunk and its neighbours land on.
+      ProcessorHandle<ChunkTaskPriorityQueueSorter.Message<Runnable>> nestworldShard;
+      if (this.nestworldWorldgenShards.length == 1) {
+         nestworldShard = this.nestworldWorldgenShards[0];
+      } else {
+         int nestworldBlockX = chunkpos.f_45578_ >> net.nestworld.region.NestworldTuning.WORLDGEN_SHARD_BLOCK_SHIFT;
+         int nestworldBlockZ = chunkpos.f_45579_ >> net.nestworld.region.NestworldTuning.WORLDGEN_SHARD_BLOCK_SHIFT;
+         long nestworldBlockKey = net.minecraft.world.level.ChunkPos.m_45589_(nestworldBlockX, nestworldBlockZ);
+         nestworldShard = this.nestworldWorldgenShards[Math.floorMod(Long.hashCode(nestworldBlockKey), this.nestworldWorldgenShards.length)];
+      }
       Executor executor = (p_214958_) -> {
-         this.f_140142_.m_6937_(ChunkTaskPriorityQueueSorter.m_140642_(p_140361_, p_214958_));
+         nestworldShard.m_6937_(ChunkTaskPriorityQueueSorter.m_140642_(p_140361_, p_214958_));
       };
       return completablefuture.thenComposeAsync((p_214873_) -> {
          return p_214873_.map((p_280971_) -> {
@@ -638,9 +_,9 @@
                      return this.m_140383_(p_140361_);
                   }, chunkaccess);
                } else {
-                  completablefuture1 = p_140362_.m_280308_(executor, this.f_140133_, this.f_140136_, this.f_214833_, this.f_140134_, (p_280966_) -> {
+                  completablefuture1 = this.nestworldGateGeneration(p_140362_, () -> p_140362_.m_280308_(executor, this.f_140133_, this.f_140136_, this.f_214833_, this.f_140134_, (p_280966_) -> {
                      return this.m_140383_(p_140361_);
-                  }, p_280971_);
+                  }, p_280971_));
                }
 
                this.f_140144_.m_5511_(chunkpos, p_140362_);
@@ -693,6 +_,14 @@
    private CompletableFuture<Either<ChunkAccess, ChunkHolder.ChunkLoadingFailure>> m_140383_(ChunkHolder p_140384_) {
       CompletableFuture<Either<ChunkAccess, ChunkHolder.ChunkLoadingFailure>> completablefuture = p_140384_.m_140047_(ChunkStatus.f_62326_.m_62482_());
       return completablefuture.thenApplyAsync((p_287363_) -> {
+         // NestWorld P1.2/P1.4: this whole lambda body is the "chunk finalize" bucket-C work
+         // (ProtoChunk->LevelChunk conversion, entitiesInLevel/LevelTicks registration, Forge
+         // ChunkEvent.Load) -- see project memory p1-1-protochunktofullchunk-commonpool-finding
+         // for why this already runs on the main thread (via mainThreadMailbox, NOT
+         // ForkJoinPool.commonPool() as originally misread). try/finally guarantees the timer
+         // records on every exit path (the early UNLOADED_CHUNK return included). O(1), no scan.
+         long nestworldT0 = System.nanoTime();
+         try {
          ChunkStatus chunkstatus = ChunkLevel.m_287158_(p_140384_.m_140093_());
          return !chunkstatus.m_62427_(ChunkStatus.f_62326_) ? ChunkHolder.f_139995_ : p_287363_.mapLeft((p_214856_) -> {
             ChunkPos chunkpos = p_140384_.m_140092_();
@@ -713,12 +_,34 @@
             levelchunk.m_62952_();
             if (this.f_140132_.add(chunkpos.m_45588_())) {
                levelchunk.m_62913_(true);
+               try {
+               p_140384_.currentlyLoading = levelchunk; // Forge - bypass the future chain when getChunk is called, this prevents deadlocks.
                levelchunk.m_156369_();
+               // NestWorld DIAG (2026-08-11, chasing the LevelTicks NPE -- see project memory
+               // leveticks-race-under-heavy-chunkgen): registerTickContainerInLevel is the
+               // leading suspect for mutating the shared per-level LevelTicks bookkeeping
+               // concurrently with NestworldRegionSystem.runScheduledTicksPhase's own
+               // iteration of it. Loud, diagnostic-only check -- no behaviour change.
+               boolean nestworldIterating = net.nestworld.region.NestworldRegionSystem.nestworldLevelTicksIterating;
+               boolean nestworldOffMain = net.nestworld.region.NestworldRegionSystem.nestworldIsOffMainThread();
+               if (nestworldIterating || nestworldOffMain) {
+                  org.apache.logging.log4j.LogManager.getLogger("NestWorld/DIAG").error(
+                        "registerTickContainerInLevel: iterating={} offMainThread={} thread='{}' chunk={}",
+                        nestworldIterating, nestworldOffMain, Thread.currentThread().getName(), chunkpos,
+                        new Throwable("NestWorld DIAG stack capture"));
+               }
                levelchunk.m_187958_(this.f_140133_);
+               net.minecraftforge.common.MinecraftForge.EVENT_BUS.post(new net.minecraftforge.event.level.ChunkEvent.Load(levelchunk, !(protochunk instanceof ImposterProtoChunk)));
+               } finally {
+                   p_140384_.currentlyLoading = null; // Forge - Stop bypassing the future chain.
+               }
             }
 
             return levelchunk;
          });
+         } finally {
+            net.nestworld.region.WorldgenGlueAttribution.recordChunkFinalize(System.nanoTime() - nestworldT0);
+         }
       }, (p_214951_) -> {
          this.f_140143_.m_6937_(ChunkTaskPriorityQueueSorter.m_140624_(p_214951_, p_140384_.m_140092_().m_45588_(), p_140384_::m_140093_));
       });
@@ -819,6 +_,7 @@
 
             this.f_140133_.m_46473_().m_6174_("chunkSave");
             CompoundTag compoundtag = ChunkSerializer.m_63454_(this.f_140133_, p_140259_);
+            net.minecraftforge.common.MinecraftForge.EVENT_BUS.post(new net.minecraftforge.event.level.ChunkDataEvent.Save(p_140259_, p_140259_.getWorldForge() != null ? p_140259_.getWorldForge() : this.f_140133_, compoundtag));
             this.m_63502_(chunkpos, compoundtag);
             this.m_140229_(chunkpos, chunkstatus.m_62494_());
             return true;
@@ -889,6 +_,7 @@
 
          if (!p_183759_ && p_183758_) {
             p_183755_.m_9088_(p_183756_);
+            net.minecraftforge.event.ForgeEventFactory.fireChunkUnWatch(p_183755_, p_183756_, this.f_140133_);
          }
 
       }
@@ -1037,11 +_,60 @@
    }
 
    public void m_140184_(ServerPlayer p_140185_) {
-      for(ChunkMap.TrackedEntity chunkmap$trackedentity : this.f_140150_.values()) {
-         if (chunkmap$trackedentity.f_140472_ == p_140185_) {
-            chunkmap$trackedentity.m_140487_(this.f_140133_.m_6907_());
+      // NestWorld: vanilla rescans EVERY tracked entity here to recompute visibility for
+      // the moved player — O(total entities) per move packet, which stalls the main thread
+      // at extreme entity counts. When enabled, query only entities within tracking range
+      // of the new position (inflated by the move delta so entities leaving range are still
+      // un-tracked), with a full-scan fallback for large jumps (teleport). See NestworldTuning.
+      ChunkMap.TrackedEntity nestworldSelf = this.f_140150_.get(p_140185_.m_19879_());
+      boolean nestworldCulled = false;
+      // A1: re-evaluate this player's entity tracking at most once per server tick. A fast
+      // flyer emits several move packets per tick; without this each re-scans nearby entities
+      // (O(entities near player)) on the main thread and the tick stalls. The own-chunk
+      // loading below still runs every move (so terrain streams ahead); only the entity
+      // re-evaluation is throttled. nestworldLastMovePos is deliberately left untouched on a
+      // throttled move so the next un-throttled move's delta spans all movement since the last
+      // re-evaluation (entities the player flew past are still picked up).
+      if (net.nestworld.region.NestworldTuning.TRACKER_MOVE_THROTTLE && nestworldSelf != null) {
+         long nestworldNowTick = this.f_140133_.m_7654_().m_129921_();
+         if (nestworldSelf.nestworldLastMoveEvalTick == nestworldNowTick) {
+            nestworldCulled = true; // already re-evaluated this tick — skip both scan paths
          } else {
-            chunkmap$trackedentity.m_140497_(p_140185_);
+            nestworldSelf.nestworldLastMoveEvalTick = nestworldNowTick;
+         }
+      }
+      if (!nestworldCulled && net.nestworld.region.NestworldTuning.TRACKER_SPATIAL_CULL && nestworldSelf != null) {
+         Vec3 nestworldCur = p_140185_.m_20182_();
+         Vec3 nestworldLast = nestworldSelf.nestworldLastMovePos;
+         nestworldSelf.nestworldLastMovePos = nestworldCur;
+         double nestworldMaxRange = (double)(this.f_140126_ * 16);
+         double nestworldDelta = (nestworldLast == null) ? Double.MAX_VALUE
+               : Math.sqrt(nestworldCur.m_82557_(nestworldLast));
+         if (nestworldDelta <= nestworldMaxRange) {
+            // Horizontal tracking range (Y is ignored by updatePlayer) + the move delta:
+            // any entity that was or now is in range lies within this radius of the new pos.
+            double nestworldQ = nestworldMaxRange + nestworldDelta;
+            double nestworldYpad = nestworldQ + 2048.0D; // tracking ignores Y — be generous
+            net.minecraft.world.phys.AABB nestworldBox = new net.minecraft.world.phys.AABB(
+                  nestworldCur.f_82479_ - nestworldQ, nestworldCur.f_82480_ - nestworldYpad, nestworldCur.f_82481_ - nestworldQ,
+                  nestworldCur.f_82479_ + nestworldQ, nestworldCur.f_82480_ + nestworldYpad, nestworldCur.f_82481_ + nestworldQ);
+            for (Entity nestworldE : this.f_140133_.m_6249_(p_140185_, nestworldBox, (e) -> true)) {
+               ChunkMap.TrackedEntity nestworldTe = this.f_140150_.get(nestworldE.m_19879_());
+               if (nestworldTe != null && nestworldTe.f_140472_ != p_140185_) {
+                  nestworldTe.m_140497_(p_140185_);
+               }
+            }
+            nestworldSelf.m_140487_(this.f_140133_.m_6907_());
+            nestworldCulled = true;
+         }
+      }
+      if (!nestworldCulled) {
+         for(ChunkMap.TrackedEntity chunkmap$trackedentity : this.f_140150_.values()) {
+            if (chunkmap$trackedentity.f_140472_ == p_140185_) {
+               chunkmap$trackedentity.m_140487_(this.f_140133_.m_6907_());
+            } else {
+               chunkmap$trackedentity.m_140497_(p_140185_);
+            }
          }
       }
 
@@ -1132,7 +_,7 @@
    }
 
    protected void m_140199_(Entity p_140200_) {
-      if (!(p_140200_ instanceof EnderDragonPart)) {
+      if (!(p_140200_ instanceof net.minecraftforge.entity.PartEntity)) {
          EntityType<?> entitytype = p_140200_.m_6095_();
          int i = entitytype.m_20681_() * 16;
          if (i != 0) {
@@ -1175,28 +_,187 @@
 
    }
 
-   protected void m_140421_() {
+   // NestWorld: lightweight regression guard for the REGIONALIZED_TRACKER skip-check — a prior
+   // bug (chunkMap.tick() running before region threads set nestworldTrackedTick, see the phase
+   // ordering fix in ServerChunkCache/NestworldRegionSystem) silently kept `skipped` at 0% for
+   // every tick. Cheap (one atomic increment per candidate entity) and only active while
+   // REGIONALIZED_TRACKER is on, so kept permanently rather than ripped out post-verification.
+   //
+   // Instance (NOT static) fields: each dimension has its own ChunkMap, and tick() is called
+   // once per dimension per real server tick (overworld via NestworldRegionSystem; nether/end
+   // via their own plain ServerLevel.tick()/ServerChunkCache.tickChunks() path — see
+   // docs/LOCAL_TICK_STAGE4.md's "Anatomy of a tick" §0). A shared static counter meant all
+   // active dimensions incremented the SAME tick-threshold, so the "every 200 ticks" log fired
+   // far more often than once per 200 REAL server ticks, and total/skipped mixed dimensions
+   // together — found live on ATM9 (lines every ~250-300ms instead of ~10s). Purely a
+   // diagnostic-accuracy fix — does not touch tracker scheduling, budget, or skip semantics.
+   final java.util.concurrent.atomic.AtomicLong nestworldSkipDiagSkipped = new java.util.concurrent.atomic.AtomicLong();
+   final java.util.concurrent.atomic.AtomicLong nestworldSkipDiagTotal = new java.util.concurrent.atomic.AtomicLong();
+   int nestworldSkipDiagTickCounter = 0;
+
+   // NestWorld: widened from protected to public so NestworldRegionSystem (different package)
+   // can call this directly, relocated to run AFTER region threads finish their tick instead of
+   // before — see ServerChunkCache.tickChunks() and NestworldRegionSystem.tickAllRegions().
+   public void m_140421_() {
       List<ServerPlayer> list = Lists.newArrayList();
       List<ServerPlayer> list1 = this.f_140133_.m_6907_();
+      // NestWorld (REGIONALIZED_TRACKER): entities whose owning region already tracked them this
+      // tick carry the current tick stamp; skip them here so only players + unowned/deferred entities
+      // are handled on main. -2 never matches the -1 default, so this is inert when the flag is off.
+      long nestworldCurTick = net.nestworld.region.NestworldTuning.REGIONALIZED_TRACKER
+            ? this.f_140133_.m_7654_().m_129921_() : -2L;
+      // NestWorld Phase-1 (TRACKER_PARALLEL_DETECTION): the per-entity detection scan below is the
+      // dominant serial 'vanilla'-phase cost once sendChanges is offloaded. Parallelise it — the pass
+      // is read-only on entity state (region threads parked at the barrier → stable) — and defer the
+      // ONLY mutation (updatePlayers on a section change, which touches shared seenBy) to a short
+      // serial post-pass over just the entities that moved section. Bit-identical visibility.
+      boolean nestworldParDetect = net.nestworld.region.NestworldTuning.TRACKER_PARALLEL_DETECTION
+            && this.f_140150_.size() >= net.nestworld.region.NestworldTuning.TRACKER_PARALLEL_DETECTION_THRESHOLD;
+      if (nestworldParDetect) {
+         // E1: reused scratch arrays + atomic cursors instead of two fresh ConcurrentLinkedQueues
+         // (one linked-node allocation PER ENTITY per tick — ~180k/50ms at 86k entities — plus an
+         // ArrayList copy). Workers claim disjoint slots via getAndIncrement; the stream's terminal
+         // op orders their writes before the serial reads below. Slots are nulled after use so
+         // stale references cannot pin dead entities.
+         int nestworldCap = this.f_140150_.size();
+         if (nestworldBcArr.length < nestworldCap) {
+            nestworldBcArr = new ChunkMap.TrackedEntity[nestworldCap + (nestworldCap >> 2)];
+            nestworldChangedArr = new ChunkMap.TrackedEntity[nestworldCap + (nestworldCap >> 2)];
+         }
+         final ChunkMap.TrackedEntity[] nestworldBc = nestworldBcArr;
+         final ChunkMap.TrackedEntity[] nestworldChanged = nestworldChangedArr;
+         nestworldBcCursor.set(0);
+         nestworldChangedCursor.set(0);
+         this.f_140150_.values().parallelStream().forEach(nestworldTe -> {
+            nestworldSkipDiagTotal.incrementAndGet();
+            if (nestworldTe.nestworldTrackedTick == nestworldCurTick) { nestworldSkipDiagSkipped.incrementAndGet(); return; } // region already tracked it
+            Entity nestworldE = nestworldTe.f_140472_;
+            // NestWorld: Entity Safety Layer (docs/LOCAL_TICK_STAGE4.md) — this fallback
+            // means the entity's owning region did NOT track it this cycle (or it's
+            // unowned, e.g. a player). Reading getX()/xo/etc directly here assumes no
+            // region thread is concurrently moving it — true today only because the
+            // barrier parks every region thread first. Reads go through the owning
+            // region's own published EntitySnapshot instead (safe from any thread); a
+            // player/unowned entity has no region racing it, so a direct read stays
+            // correct there. Absent-from-snapshot (owned but not yet published this
+            // cycle — a brief startup transient) is treated as "no change this cycle",
+            // self-healing once that region's next snapshot publishes.
+            net.nestworld.region.WorldRegion.EntitySnapshot nestworldSnap = nestworldSafePosition(nestworldTe);
+            if (nestworldSnap == null) return;
+            boolean nestworldStill2 = net.nestworld.region.NestworldTuning.TRACKER_SPATIAL_CULL
+                  && !(nestworldE instanceof ServerPlayer)
+                  && nestworldSnap.x() == nestworldSnap.xo() && nestworldSnap.y() == nestworldSnap.yo() && nestworldSnap.z() == nestworldSnap.zo();
+            SectionPos nestworldSp;
+            boolean nestworldFlag = false;
+            if (nestworldStill2) {
+               nestworldSp = nestworldTe.f_140474_;
+            } else {
+               nestworldSp = SectionPos.m_235863_(new Vec3(nestworldSnap.x(), nestworldSnap.y(), nestworldSnap.z()));
+               nestworldFlag = !Objects.equals(nestworldTe.f_140474_, nestworldSp);
+            }
+            nestworldTe.nestworldTickSection = nestworldSp;
+            if (nestworldFlag) nestworldChanged[nestworldChangedCursor.getAndIncrement()] = nestworldTe;
+            if (nestworldFlag || !nestworldTe.f_140475_.isEmpty()
+                  || this.f_140145_.m_183913_(nestworldSp.m_123251_().m_45588_())) {
+               nestworldBc[nestworldBcCursor.getAndIncrement()] = nestworldTe;
+            }
+         });
+         int nestworldNChanged = nestworldChangedCursor.get();
+         int nestworldNBc = nestworldBcCursor.get();
+         // Serial: apply section changes (mutates shared seenBy) for the few entities that moved section.
+         for (int nestworldI = 0; nestworldI < nestworldNChanged; nestworldI++) {
+            ChunkMap.TrackedEntity nestworldTe = nestworldChanged[nestworldI];
+            nestworldChanged[nestworldI] = null;
+            nestworldTe.m_140487_(list1);
+            if (nestworldTe.f_140472_ instanceof ServerPlayer) list.add((ServerPlayer) nestworldTe.f_140472_);
+            nestworldTe.f_140474_ = nestworldTe.nestworldTickSection;
+         }
+         // Parallel: pack dirty data + send. IntStream over the dense array section splits well.
+         if (nestworldNBc >= net.nestworld.region.NestworldTuning.TRACKER_PARALLEL_BROADCAST_THRESHOLD) {
+            java.util.stream.IntStream.range(0, nestworldNBc).parallel()
+                  .forEach(nestworldI -> nestworldBc[nestworldI].f_140471_.m_8533_());
+         } else {
+            for (int nestworldI = 0; nestworldI < nestworldNBc; nestworldI++) nestworldBc[nestworldI].f_140471_.m_8533_();
+         }
+         java.util.Arrays.fill(nestworldBc, 0, nestworldNBc, null);
+      } else {
+      // NestWorld: collect the entities that need a broadcast and run the expensive
+      // sendChanges() in parallel after the loop (region threads are parked at the barrier
+      // here, so entity state is stable). See NestworldTuning.TRACKER_PARALLEL_BROADCAST.
+      java.util.List<ChunkMap.TrackedEntity> nestworldBroadcast =
+            net.nestworld.region.NestworldTuning.TRACKER_PARALLEL_BROADCAST ? new java.util.ArrayList<>() : null;
 
       for(ChunkMap.TrackedEntity chunkmap$trackedentity : this.f_140150_.values()) {
-         SectionPos sectionpos = chunkmap$trackedentity.f_140474_;
-         SectionPos sectionpos1 = SectionPos.m_235861_(chunkmap$trackedentity.f_140472_);
-         boolean flag = !Objects.equals(sectionpos, sectionpos1);
-         if (flag) {
-            chunkmap$trackedentity.m_140487_(list1);
-            Entity entity = chunkmap$trackedentity.f_140472_;
-            if (entity instanceof ServerPlayer) {
-               list.add((ServerPlayer)entity);
-            }
-
-            chunkmap$trackedentity.f_140474_ = sectionpos1;
-         }
-
-         if (flag || this.f_140145_.m_183913_(sectionpos1.m_123251_().m_45588_())) {
-            chunkmap$trackedentity.f_140471_.m_8533_();
-         }
-      }
+         nestworldSkipDiagTotal.incrementAndGet();
+         if (chunkmap$trackedentity.nestworldTrackedTick == nestworldCurTick) { nestworldSkipDiagSkipped.incrementAndGet(); continue; } // region already tracked it
+         Entity nestworldEntity = chunkmap$trackedentity.f_140472_;
+         // NestWorld: SectionPos.of() (block->section floors) runs for every tracked
+         // entity every tick just to detect section changes — O(N) that dominates the
+         // periodic tracker at high entity counts. An entity that did not move since its
+         // last tick cannot have changed section, so reuse its stored lastSectionPos and
+         // skip the recompute. Players always take the full path (few, and critical).
+         //
+         // NestWorld: Entity Safety Layer (docs/LOCAL_TICK_STAGE4.md) — same reasoning as
+         // the parallel-detection variant above: read via the owning region's published
+         // EntitySnapshot, not the live entity, unless unowned (player).
+         net.nestworld.region.WorldRegion.EntitySnapshot nestworldSnap = nestworldSafePosition(chunkmap$trackedentity);
+         if (nestworldSnap == null) continue;
+         boolean nestworldStill = net.nestworld.region.NestworldTuning.TRACKER_SPATIAL_CULL
+               && !(nestworldEntity instanceof ServerPlayer)
+               && nestworldSnap.x() == nestworldSnap.xo()
+               && nestworldSnap.y() == nestworldSnap.yo()
+               && nestworldSnap.z() == nestworldSnap.zo();
+         boolean flag = false;
+         SectionPos sectionpos1;
+         if (nestworldStill) {
+            sectionpos1 = chunkmap$trackedentity.f_140474_; // unchanged — no recompute
+         } else {
+            SectionPos sectionpos = chunkmap$trackedentity.f_140474_;
+            sectionpos1 = SectionPos.m_235863_(new Vec3(nestworldSnap.x(), nestworldSnap.y(), nestworldSnap.z()));
+            flag = !Objects.equals(sectionpos, sectionpos1);
+            if (flag) {
+               chunkmap$trackedentity.m_140487_(list1);
+               if (nestworldEntity instanceof ServerPlayer) {
+                  list.add((ServerPlayer)nestworldEntity);
+               }
+
+               chunkmap$trackedentity.f_140474_ = sectionpos1;
+            }
+         }
+
+         // NestWorld: also push entity update packets (position + dirty metadata — HP,
+         // ItemStack, etc.) whenever a player is already viewing the entity, not only when
+         // its chunk is "entity-ticking". On the region-threaded core the vanilla
+         // inEntityTickingRange signal is computed on the main thread while entities tick on
+         // region threads, so it can read false for a chunk whose entities are genuinely
+         // active — which silently froze client-side entity data (mobs spawned but HP never
+         // updated; dropped items never received their ItemStack metadata so they rendered
+         // invisible). If someone is tracking the entity, they must get its updates.
+         if (flag || !chunkmap$trackedentity.f_140475_.isEmpty() || this.f_140145_.m_183913_(sectionpos1.m_123251_().m_45588_())) {
+            if (nestworldBroadcast != null) {
+               nestworldBroadcast.add(chunkmap$trackedentity);
+            } else {
+               chunkmap$trackedentity.f_140471_.m_8533_();
+            }
+         }
+      }
+
+      // NestWorld: parallel broadcast phase. sendChanges() packs each entity's dirty
+      // SynchedEntityData (thread-safe: clear-first packDirty, read lock, defensive copy) and
+      // enqueues packets on netty-thread-safe connections; entity state is stable (region
+      // threads parked). Above the threshold this turns the O(entities × viewers) serial
+      // tracker broadcast — the dominant 'vanilla'-phase cost at high entity counts — into
+      // parallel work across the common pool. Below it, the serial path avoids fork/join setup.
+      if (nestworldBroadcast != null && !nestworldBroadcast.isEmpty()) {
+         if (nestworldBroadcast.size() >= net.nestworld.region.NestworldTuning.TRACKER_PARALLEL_BROADCAST_THRESHOLD) {
+            nestworldBroadcast.parallelStream().forEach(te -> te.f_140471_.m_8533_());
+         } else {
+            for (ChunkMap.TrackedEntity te : nestworldBroadcast) {
+               te.f_140471_.m_8533_();
+            }
+         }
+      }
+      } // end else: serial detection path (TRACKER_PARALLEL_DETECTION off / below threshold)
 
       if (!list.isEmpty()) {
          for(ChunkMap.TrackedEntity chunkmap$trackedentity1 : this.f_140150_.values()) {
@@ -1204,6 +_,90 @@
          }
       }
 
+      // NestWorld: regression guard — log the skip ratio every 200 ticks, then reset. A healthy
+      // fixed build sits close to 100% (region threads already tracked their owned entities by
+      // the time this runs); if `skipped` collapses back toward 0% with `total` still nonzero,
+      // the phase-ordering fix (ServerChunkCache/NestworldRegionSystem) has regressed and every
+      // owned entity is being tracked twice again.
+      // NestWorld: this diagnostic is meaningful only for the overworld — nestworldTrackedTick
+      // is only ever SET by RegionThread.tickEntities()'s nestworldTrackOwned() call, which only
+      // ever runs for sharded-overworld regions. Nether/End entities' nestworldTrackedTick stays
+      // at its -1 default forever, so a per-dimension counter for them would always read
+      // total=X skipped=0 (0%) — not a regression, just "this mechanism doesn't apply here" —
+      // and would otherwise trip the below-5%-warns-regression check permanently and falsely.
+      if (net.nestworld.region.NestworldTuning.REGIONALIZED_TRACKER
+            && this.f_140133_.m_46472_() == net.minecraft.world.level.Level.f_46428_
+            && ++nestworldSkipDiagTickCounter >= 200) {
+         nestworldSkipDiagTickCounter = 0;
+         long total = nestworldSkipDiagTotal.getAndSet(0);
+         long skipped = nestworldSkipDiagSkipped.getAndSet(0);
+         double pct = total == 0 ? 0.0 : (100.0 * skipped / total);
+         org.apache.logging.log4j.LogManager.getLogger("NestWorld/TrackerDiag").info(
+               "REGIONALIZED_TRACKER skip-check over 200 ticks: dimension={} total={} skipped={} ({}%)",
+               this.f_140133_.m_46472_().m_135782_(), total, skipped, pct);
+         if (total > 0 && pct < 5.0) {
+            org.apache.logging.log4j.LogManager.getLogger("NestWorld/TrackerDiag").warn(
+                  "REGIONALIZED_TRACKER skip-check is near 0% ({}%) — the phase-ordering fix "
+                  + "(chunkMap.tick() must run after region threads finish) may have regressed; "
+                  + "owned entities are likely being tracked twice per tick again.", pct);
+         }
+      }
+   }
+
+   /**
+    * NestWorld: Entity Safety Layer (docs/LOCAL_TICK_STAGE4.md) — safe position read for
+    * the {@code tick()} fallback path (an entity its owning region did NOT track this
+    * cycle, or one with no owning region at all). Owned entities read from that region's
+    * own published {@link net.nestworld.region.WorldRegion.EntitySnapshot} — never the
+    * live {@code Entity} object, which the owning region's thread could be concurrently
+    * mutating. Unowned entities (players; a type never region-owned) have no region
+    * thread racing them, so a direct live read stays correct. Returns null when owned but
+    * not yet present in this cycle's snapshot — a brief startup transient (a region that
+    * hasn't completed its first tick yet); callers treat that as "no change this cycle",
+    * self-healing once that region's next snapshot publishes.
+    */
+   private net.nestworld.region.WorldRegion.EntitySnapshot nestworldSafePosition(ChunkMap.TrackedEntity te) {
+      net.nestworld.region.WorldRegion owner = te.nestworldResolveOwner();
+      if (owner == null) {
+         Entity e = te.f_140472_;
+         return new net.nestworld.region.WorldRegion.EntitySnapshot(
+               e.m_20185_(), e.m_20186_(), e.m_20189_(), e.f_19854_, e.f_19855_, e.f_19856_, e.m_213877_());
+      }
+      return owner.nestworldGetEntitySnapshot().get(te.f_140472_.m_20148_());
+   }
+
+   // NestWorld (REGIONALIZED_TRACKER): run the per-entity tracker step (section detection +
+   // updatePlayers + sendChanges) for an entity OWNED by the calling region thread, during the
+   // region phase. Only the owning region ever touches this entity's tracker state (seenBy,
+   // lastSectionPos), and the region (regionPool) and main (tick/move) passes never overlap, so no
+   // lock is needed. The tick stamp lets the main-thread tick() skip entities the regions handled.
+   // entityMap is not structurally mutated during the region phase (main is at the barrier), so the
+   // get() read is safe. Owned entities are never players, so the player special-cases are dropped.
+   public void nestworldTrackOwned(Entity nestworldEntity, long nestworldTickNo, List<ServerPlayer> nestworldPlayers) {
+      ChunkMap.TrackedEntity te = this.f_140150_.get(nestworldEntity.m_19879_());
+      if (te == null) {
+         return;
+      }
+      boolean nestworldStill = net.nestworld.region.NestworldTuning.TRACKER_SPATIAL_CULL
+            && nestworldEntity.m_20185_() == nestworldEntity.f_19854_
+            && nestworldEntity.m_20186_() == nestworldEntity.f_19855_
+            && nestworldEntity.m_20189_() == nestworldEntity.f_19856_;
+      SectionPos sectionpos1;
+      boolean flag = false;
+      if (nestworldStill) {
+         sectionpos1 = te.f_140474_;
+      } else {
+         sectionpos1 = SectionPos.m_235861_(nestworldEntity);
+         flag = !Objects.equals(te.f_140474_, sectionpos1);
+         if (flag) {
+            te.m_140487_(nestworldPlayers);
+            te.f_140474_ = sectionpos1;
+         }
+      }
+      if (flag || !te.f_140475_.isEmpty() || this.f_140145_.m_183913_(sectionpos1.m_123251_().m_45588_())) {
+         te.f_140471_.m_8533_();
+      }
+      te.nestworldTrackedTick = nestworldTickNo;
    }
 
    public void m_140201_(Entity p_140202_, Packet<?> p_140203_) {
@@ -1282,6 +_,7 @@
          }
       }
 
+      net.minecraftforge.event.ForgeEventFactory.fireChunkWatch(p_183761_, p_183763_, this.f_140133_);
    }
 
    protected PoiManager m_140424_() {
@@ -1293,7 +_,14 @@
    }
 
    void m_287285_(ChunkPos p_287612_, FullChunkStatus p_287685_) {
+      // NestWorld P1.2/P1.4 (docs/P1_WORLDGEN_MAINTHREAD_DECOUPLING_SPEC.md): a direct, precise
+      // hook for bucket A ("справді необхідна main-thread робота") -- this is vanilla's own
+      // deliberate main-thread commit point for a chunk's full-status transition, called via
+      // thenRunAsync(..., mainThreadExecutor) elsewhere in this class. Audit-only, no behavior
+      // change; O(1) per call, no scan.
+      long nestworldT0 = System.nanoTime();
       this.f_143031_.m_156794_(p_287612_, p_287685_);
+      net.nestworld.region.WorldgenGlueAttribution.recordMainGlue(System.nanoTime() - nestworldT0);
    }
 
    class DistanceManager extends net.minecraft.server.level.DistanceManager {
@@ -1321,7 +_,53 @@
       final Entity f_140472_;
       private final int f_140473_;
       SectionPos f_140474_;
+      // NestWorld: section computed during the parallel detection pass (TRACKER_PARALLEL_DETECTION),
+      // read back in the serial section-change post-pass. Written by exactly one stream worker per
+      // entity, read after the parallel terminal op completes (happens-before), so no lock needed.
+      SectionPos nestworldTickSection;
+      // NestWorld (REGIONALIZED_TRACKER): server tick on which this entity's owning region already
+      // ran the tracker step in the region phase, so the main-thread tick() can skip it. Written by
+      // the owning region thread (regionPool), read by main (vanilla) — different phases, no overlap.
+      volatile long nestworldTrackedTick = -1L;
       private final Set<ServerPlayerConnection> f_140475_ = Sets.newIdentityHashSet();
+      // NestWorld: last position this player's move() ran at, to derive the move delta
+      // for the spatial-cull tracker (only set for player TrackedEntities).
+      Vec3 nestworldLastMovePos;
+      // NestWorld: server tick of the last move()-driven entity re-evaluation for this
+      // player. A fast-moving player emits several move packets per tick; without this
+      // each one re-scans nearby entities (O(entities near player)) on the main thread,
+      // so flying through a dense entity pile multiplies the cost by packets/tick and
+      // stalls the tick. Re-evaluate at most once per tick (the periodic tick() and the
+      // next move both pick up the remainder). -1 = never. See NestworldTuning.
+      long nestworldLastMoveEvalTick = -1L;
+      // NestWorld: Entity Safety Layer (docs/LOCAL_TICK_STAGE4.md) — cached owning-region
+      // reference for the tick() fallback below, so the common case (ownership unchanged
+      // since last check) resolves in O(1) instead of an O(active-regions) scan every
+      // time. Cache-then-validate, not proactively updated on every ownership change: a
+      // stale reference is caught cheaply (WorldRegion.ownsEntity() is a HashSet contains
+      // check) and only THEN does a fresh scan run — self-correcting without needing to
+      // hook every ownership-changing code path (BoundaryEntityTransfer's transfer/
+      // pinned-unassign, RegionSplitManager's split/merge entity handoff, etc.), which
+      // would be easy to miss a spot on in exactly the way this path's past incidents
+      // (phase-ordering, "invisible items") already showed this class of bug likes to
+      // hide. May be stale/null between checks; always re-validated before trusted.
+      net.nestworld.region.WorldRegion nestworldCachedOwner;
+
+      /** NestWorld: Entity Safety Layer — resolves (and self-corrects) this entity's
+       *  owning region for the tick() fallback path. O(1) in the common case. Safe to
+       *  call concurrently from multiple threads (parallel-stream detection variant) —
+       *  WorldRegion.ownsEntity()/WorldGrid.findOwningRegion() are both safe for
+       *  concurrent reads; a benign race on the cache field itself just means two
+       *  threads independently resolve the same answer, not a correctness issue. */
+      net.nestworld.region.WorldRegion nestworldResolveOwner() {
+         net.nestworld.region.WorldRegion cached = this.nestworldCachedOwner;
+         if (cached != null && cached.ownsEntity(this.f_140472_.m_20148_())) return cached;
+         net.nestworld.region.WorldRegion fresh = net.nestworld.region.NestworldRegionSystem.isInitialised()
+               ? net.nestworld.region.NestworldRegionSystem.get().getGrid().findOwningRegion(this.f_140472_.m_20148_())
+               : null;
+         this.nestworldCachedOwner = fresh;
+         return fresh;
+      }
 
       public TrackedEntity(Entity p_140478_, int p_140479_, int p_140480_, boolean p_140481_) {
          this.f_140471_ = new ServerEntity(ChunkMap.this.f_140133_, p_140478_, p_140480_, p_140481_, this::m_140489_);
@@ -1396,10 +_,15 @@
       private int m_140496_() {
          int i = this.f_140473_;
 
-         for(Entity entity : this.f_140472_.m_146897_()) {
-            int j = entity.m_6095_().m_20681_() * 16;
-            if (j > i) {
-               i = j;
+         // NestWorld: only entities carrying passengers can widen the range; skip the
+         // getIndirectPassengers() stream allocation for the (vast) majority that have
+         // none — it dominated the move() hot path at high entity counts.
+         if (this.f_140472_.m_20160_()) {
+            for(Entity entity : this.f_140472_.m_146897_()) {
+               int j = entity.m_6095_().m_20681_() * 16;
+               if (j > i) {
+                  i = j;
+               }
             }
          }
 
