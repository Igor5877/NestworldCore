--- a/net/minecraft/server/level/ServerChunkCache.java
+++ b/net/minecraft/server/level/ServerChunkCache.java
@@ -63,6 +_,24 @@
    private final long[] f_8337_ = new long[4];
    private final ChunkStatus[] f_8338_ = new ChunkStatus[4];
    private final ChunkAccess[] f_8339_ = new ChunkAccess[4];
+
+   // NestWorld: NW-CHUNK-FASTPATH instrumentation (see docs/LOCAL_TICK_STAGE4.md). Main-thread
+   // getChunk() call accounting — cheap (a handful of incrementAndGet()s per call), left
+   // permanently on rather than flag-gated since the cost is negligible next to the work being
+   // measured. Reported via /nestworld chunkstats.
+   public final java.util.concurrent.atomic.AtomicLong nestworldChunkLookups = new java.util.concurrent.atomic.AtomicLong();
+   public final java.util.concurrent.atomic.AtomicLong nestworldFastPathHits = new java.util.concurrent.atomic.AtomicLong();
+   public final java.util.concurrent.atomic.AtomicLong nestworldBlockingCalls = new java.util.concurrent.atomic.AtomicLong();
+   public final java.util.concurrent.atomic.AtomicLong nestworldWaitNanos = new java.util.concurrent.atomic.AtomicLong();
+   public final java.util.concurrent.atomic.AtomicLong nestworldProxyCreations = new java.util.concurrent.atomic.AtomicLong();
+   // NestWorld: counts how many times a RegionThread's getChunk() wait hit the
+   // GETCHUNK_WAIT_TIMEOUT_MS bound instead of resolving — see the "RegionThread getChunk
+   // blocking crash" fix above. Non-zero readings mean the main thread was slow enough,
+   // for long enough, that this safety valve actually engaged — worth watching after a
+   // heavy event (mass chunk-gen, a mod's raycast into ungenerated terrain) even though
+   // the crash it prevents is rare.
+   public final java.util.concurrent.atomic.AtomicLong nestworldRegionGetChunkTimeouts = new java.util.concurrent.atomic.AtomicLong();
+
    @Nullable
    @VisibleForDebug
    private NaturalSpawner.SpawnState f_8340_;
@@ -109,6 +_,51 @@
    @Nullable
    public ChunkAccess m_7587_(int p_8360_, int p_8361_, ChunkStatus p_8362_, boolean p_8363_) {
       if (Thread.currentThread() != this.f_8330_) {
+         if (Thread.currentThread() instanceof net.nestworld.region.RegionThread nestworldRt) {
+            LevelChunk nestworldChunk = this.nestworldGetLoadedChunk(p_8360_, p_8361_);
+            if (nestworldChunk != null) return nestworldChunk;
+            // load=false: honour vanilla "null if absent" — callers null-check, so we
+            // never block here (this killed the dominant RemoveBlockGoal herd).
+            if (!p_8363_) return null;
+            // load=true default: block for correct data, because the snapshot can
+            // still miss a chunk that IS loaded and void-air would then corrupt the
+            // read. -Dnestworld.nonBlockingChunkReads=true opts into per-thread
+            // void-air (faster, but entities see AIR at unloaded edges).
+            if (net.nestworld.region.NestworldTuning.NONBLOCKING_CHUNK_READS) {
+               return nestworldRt.nestworldEmptyChunk();
+            }
+            // NestWorld: bound this region thread's wait — see docs/LOCAL_TICK_STAGE4.md,
+            // "RegionThread getChunk blocking crash". A real crash-report confirmed a region
+            // thread can get stuck here indefinitely (plain join() has no timeout) when the
+            // chunk genuinely isn't ready yet AND the main thread it depends on (this call is
+            // dispatched to run there) is itself saturated — e.g. a mod's raycast reaching
+            // ungenerated terrain during a heavy chunk-gen event. That single stuck region
+            // thread outlived both RegionThreadPool's 30s region-round abandon-wait and the
+            // 60s ServerWatchdog threshold, crashing the whole JVM. Reuses the same
+            // GETCHUNK_WAIT_TIMEOUT_MS budget the main-thread single-chunk timeout path
+            // already uses (see below) — on timeout, degrade to the same per-thread empty
+            // chunk NONBLOCKING_CHUNK_READS uses above, rather than block indefinitely. The
+            // dispatched task itself is NOT cancelled — it keeps running on main and (if it
+            // ever completes) its result is simply discarded; this only stops THIS caller
+            // from waiting on it past a safe bound.
+            if (net.nestworld.region.NestworldTuning.GETCHUNK_WAIT_TIMEOUT_MS > 0) {
+               CompletableFuture<ChunkAccess> nestworldFuture = CompletableFuture.supplyAsync(() -> {
+                  return this.m_7587_(p_8360_, p_8361_, p_8362_, p_8363_);
+               }, this.f_8332_);
+               try {
+                  return nestworldFuture.get(net.nestworld.region.NestworldTuning.GETCHUNK_WAIT_TIMEOUT_MS,
+                        java.util.concurrent.TimeUnit.MILLISECONDS);
+               } catch (java.util.concurrent.TimeoutException nestworldTimeout) {
+                  nestworldRegionGetChunkTimeouts.incrementAndGet();
+                  return nestworldRt.nestworldEmptyChunk();
+               } catch (InterruptedException nestworldInterrupted) {
+                  Thread.currentThread().interrupt();
+                  return nestworldRt.nestworldEmptyChunk();
+               } catch (java.util.concurrent.ExecutionException nestworldExecutionException) {
+                  throw new RuntimeException(nestworldExecutionException.getCause());
+               }
+            }
+         }
          return CompletableFuture.supplyAsync(() -> {
             return this.m_7587_(p_8360_, p_8361_, p_8362_, p_8363_);
          }, this.f_8332_).join();
@@ -117,6 +_,37 @@
          profilerfiller.m_6174_("getChunk");
          long i = ChunkPos.m_45589_(p_8360_, p_8361_);
 
+         // NestWorld: NW-CHUNK-FASTPATH. The 4-slot lastChunk cache below only survives a
+         // handful of DIFFERENT-chunk lookups between hits — a mod iterating a HashSet of
+         // block positions spanning dozens of chunks (e.g. a large explosion's neighbour-
+         // update pass) thrashes it on nearly every call, falling through to the blocking
+         // managedBlock()/getChunkFutureMainThread() path even though the chunk is already
+         // fully loaded and ticking (measured live: Draconic Evolution's reactor meltdown,
+         // ~1.6ms/tick average in ServerChunkCache.getChunk() this way). nestworldLoadedFull
+         // is the SAME lock-free snapshot region threads already read (see
+         // nestworldGetLoadedChunk) — reused here, not a new mechanism — refreshed once/tick
+         // by nestworldRefreshLoadedChunks(), called as literally the first line of
+         // NestworldRegionSystem.tickAllRegions(), i.e. always fresh (this tick's data) by
+         // the time any other tick-phase code (including Forge's onPreServerTick listeners,
+         // where this reactor code runs) executes. No memory-visibility subtlety: this is
+         // the MAIN thread reading a volatile field it (indirectly, via that same-thread
+         // call) just published — even safer than the already-shipped region-thread case,
+         // which crosses threads. Scoped to ChunkStatus.FULL + load=true, matching
+         // GETCHUNK_WAIT_TIMEOUT_MS's existing scoping precedent below — every OTHER call
+         // shape (other statuses, load=false probes, worldgen dependency resolution) is
+         // completely untouched, falling through to the unchanged vanilla path exactly as
+         // before. A miss here (chunk not yet FULL, or genuinely unloaded) costs one cheap
+         // hashmap lookup and then falls through normally — never a wrong answer, only
+         // "slow path taken" vs "fast path taken".
+         nestworldChunkLookups.incrementAndGet();
+         if (p_8362_ == ChunkStatus.f_62326_ && p_8363_) {
+            LevelChunk nestworldFast = this.nestworldLoadedFull.get(i);
+            if (nestworldFast != null) {
+               nestworldFastPathHits.incrementAndGet();
+               return nestworldFast;
+            }
+         }
+
          for(int j = 0; j < 4; ++j) {
             if (i == this.f_8337_[j] && p_8362_ == this.f_8338_[j]) {
                ChunkAccess chunkaccess = this.f_8339_[j];
@@ -128,7 +_,89 @@
 
          profilerfiller.m_6174_("getChunkCacheMiss");
          CompletableFuture<Either<ChunkAccess, ChunkHolder.ChunkLoadingFailure>> completablefuture = this.m_8456_(p_8360_, p_8361_, p_8362_, p_8363_);
-         this.f_8332_.m_18701_(completablefuture::isDone);
+         // NestWorld: the ForceLoadCommand watchdog-crash fix's harder sibling. This exact
+         // call shape (status==FULL, load==true) is what Level.getChunk(x, z) resolves to —
+         // the ORIGINAL crash that motivated this project's Phase-1 work (a mod's map-click
+         // teleport blocking the main thread past the 60s ServerHangWatchdog on genuinely
+         // virgin territory on a heavy real pack). Scoped tightly to FULL+load so internal
+         // worldgen dependency-resolution calls (other statuses, or load=false probes) are
+         // completely untouched — vanilla-identical for every OTHER call shape.
+         if (p_8362_ == ChunkStatus.f_62326_ && p_8363_ && net.nestworld.region.NestworldTuning.GETCHUNK_WAIT_TIMEOUT_MS > 0) {
+            net.minecraft.world.level.chunk.LevelChunk.NestworldProxyLevelChunk nestworldProxy = this.nestworldPendingProxies.get(i);
+            if (nestworldProxy != null) {
+               // Someone else is already waiting on this exact position — return the SAME
+               // proxy reference (not a new one), so both callers transparently see it
+               // resolve to the real chunk together once ready.
+               if (completablefuture.isDone()) {
+                  this.nestworldResolveProxy(i, nestworldProxy, completablefuture);
+               }
+               return nestworldProxy;
+            }
+            // NestWorld: the hard invariant (docs/GEN_SPIKE.md, "Layer 10" — project owner's
+            // explicit direction, 2026-08-11): "MAIN THREAD НІКОЛИ НЕ МОЖЕ БЛОКУВАТИСЯ НА
+            // WORLDGEN" — not bounded, ZERO. GETCHUNK_ZERO_WAIT_MAIN_THREAD (default true)
+            // makes this the default: never even attempt the bounded wait below, degrade
+            // straight to a proxy immediately, same as if the burst budget were already
+            // exhausted (which is still checked too, so an operator who explicitly disables
+            // zero-wait mode keeps the previous bounded-tolerance behavior as a fallback —
+            // GETCHUNK_BURST_BUDGET_MS/GETCHUNK_WAIT_TIMEOUT_MS are NOT removed, just no
+            // longer consulted by default). Correct because the proxy path already handles
+            // "not resolved yet" transparently (chunk keeps generating in the background;
+            // the proxy resolves once it's ready) — this only changes WHEN that path is
+            // taken, reusing 100% of the existing machinery, not adding a new one.
+            if ((net.nestworld.region.NestworldTuning.GETCHUNK_ZERO_WAIT_MAIN_THREAD
+                    || this.nestworldBurstBudgetRemainingNanos <= 0) && !completablefuture.isDone()) {
+               nestworldProxyCreations.incrementAndGet();
+               net.minecraft.world.level.chunk.LevelChunk.NestworldProxyLevelChunk nestworldBurstProxy = new net.minecraft.world.level.chunk.LevelChunk.NestworldProxyLevelChunk(
+                     this.f_8329_, new ChunkPos(p_8360_, p_8361_),
+                     this.f_8329_.m_9598_().m_175515_(net.minecraft.core.registries.Registries.f_256952_)
+                           .m_246971_(net.minecraft.world.level.biome.Biomes.f_48202_));
+               this.nestworldPendingProxies.put(i, nestworldBurstProxy);
+               completablefuture.whenComplete((either, throwable) -> this.nestworldResolveProxy(i, nestworldBurstProxy, completablefuture));
+               return nestworldBurstProxy;
+            }
+            // NestWorld: cap THIS call's own wait by whichever is smaller, its individual
+            // GETCHUNK_WAIT_TIMEOUT_MS bound or the tick's remaining burst budget — found
+            // necessary after a real crash (2026-08-10, EvilCraft's WorldHelpers.foldArea via
+            // a Draconic Evolution reactor explosion): the burst-budget check above only
+            // gated calls AFTER the first, so a single genuinely-cold chunk could still burn
+            // up to the FULL per-call timeout (20s default) before the burst mechanism ever
+            // got a chance to act, and 2-3 such calls in the same tick (a foldArea scanning
+            // several distinct unloaded chunk positions) stacked past the 60s watchdog even
+            // though no individual call exceeded its own nominal bound. This closes that gap:
+            // now EVERY call in a tick, including the first, is bounded by whatever's left of
+            // the shared per-tick budget, not just its own standalone timeout.
+            long nestworldCallBudgetNanos = Math.min(
+                    net.nestworld.region.NestworldTuning.GETCHUNK_WAIT_TIMEOUT_MS * 1_000_000L,
+                    Math.max(this.nestworldBurstBudgetRemainingNanos, 0L));
+            long nestworldDeadline = System.nanoTime() + nestworldCallBudgetNanos;
+            long nestworldBlockStart = System.nanoTime();
+            nestworldBlockingCalls.incrementAndGet();
+            this.f_8332_.m_18701_(() -> completablefuture.isDone() || System.nanoTime() >= nestworldDeadline);
+            long nestworldElapsed = System.nanoTime() - nestworldBlockStart;
+            nestworldWaitNanos.addAndGet(nestworldElapsed);
+            this.nestworldBurstBudgetRemainingNanos -= nestworldElapsed;
+            if (!completablefuture.isDone()) {
+               nestworldProxyCreations.incrementAndGet();
+               net.minecraft.world.level.chunk.LevelChunk.NestworldProxyLevelChunk proxy = new net.minecraft.world.level.chunk.LevelChunk.NestworldProxyLevelChunk(
+                     this.f_8329_, new ChunkPos(p_8360_, p_8361_),
+                     this.f_8329_.m_9598_().m_175515_(net.minecraft.core.registries.Registries.f_256952_)
+                           .m_246971_(net.minecraft.world.level.biome.Biomes.f_48202_));
+               this.nestworldPendingProxies.put(i, proxy);
+               completablefuture.whenComplete((either, throwable) -> this.nestworldResolveProxy(i, proxy, completablefuture));
+               // Deliberately NOT stored via storeInCache — that 4-slot cache is for
+               // legitimate resolved chunks; the proxy is looked up via
+               // nestworldPendingProxies specifically so it stops being handed out (and the
+               // normal fast path resumes) the instant nestworldResolveProxy clears it.
+               return proxy;
+            }
+            // Completed within the deadline after all — fall through to the normal path below.
+         } else {
+            long nestworldBlockStart = System.nanoTime();
+            nestworldBlockingCalls.incrementAndGet();
+            this.f_8332_.m_18701_(completablefuture::isDone);
+            nestworldWaitNanos.addAndGet(System.nanoTime() - nestworldBlockStart);
+         }
          ChunkAccess chunkaccess1 = completablefuture.join().map((p_8406_) -> {
             return p_8406_;
          }, (p_8423_) -> {
@@ -143,10 +_,34 @@
       }
    }
 
+   /** Pending single-chunk timeout proxies, keyed by ChunkPos.asLong — main-thread-only
+    *  (this whole getChunk() branch only runs on the main thread), so a plain map is safe. */
+   private final it.unimi.dsi.fastutil.longs.Long2ObjectMap<net.minecraft.world.level.chunk.LevelChunk.NestworldProxyLevelChunk> nestworldPendingProxies =
+         new it.unimi.dsi.fastutil.longs.Long2ObjectOpenHashMap<>();
+
+   private void nestworldResolveProxy(long pos, net.minecraft.world.level.chunk.LevelChunk.NestworldProxyLevelChunk proxy,
+                                       CompletableFuture<Either<ChunkAccess, ChunkHolder.ChunkLoadingFailure>> future) {
+      if (proxy.isResolved()) return;
+      // Always unregister on completion, success OR failure — a genuine generation failure
+      // (not just a slow one) must not leave this position permanently stuck serving an
+      // unresolvable, never-cleared proxy to every future caller; the next call should get a
+      // fresh attempt through the normal path instead.
+      future.whenComplete((either, throwable) -> {
+         this.nestworldPendingProxies.remove(pos, proxy);
+         if (throwable == null && either != null) {
+            either.ifLeft((chunk) -> {
+               if (chunk instanceof LevelChunk levelChunk) {
+                  proxy.resolve(levelChunk);
+               }
+            });
+         }
+      });
+   }
+
    @Nullable
    public LevelChunk m_7131_(int p_8357_, int p_8358_) {
       if (Thread.currentThread() != this.f_8330_) {
-         return null;
+         return Thread.currentThread() instanceof net.nestworld.region.RegionThread ? this.nestworldGetLoadedChunk(p_8357_, p_8358_) : null;
       } else {
          this.f_8329_.m_46473_().m_6174_("getChunkNow");
          long i = ChunkPos.m_45589_(p_8357_, p_8358_);
@@ -162,6 +_,7 @@
          if (chunkholder == null) {
             return null;
          } else {
+            if (chunkholder.currentlyLoading != null) return chunkholder.currentlyLoading; // Forge: If the requested chunk is loading, bypass the future chain to prevent a deadlock.
             Either<ChunkAccess, ChunkHolder.ChunkLoadingFailure> either = chunkholder.m_140080_(ChunkStatus.f_62326_).getNow((Either<ChunkAccess, ChunkHolder.ChunkLoadingFailure>)null);
             if (either == null) {
                return null;
@@ -180,6 +_,92 @@
       }
    }
 
+   @Nullable
+   private LevelChunk nestworldGetLoadedChunk(int p_x, int p_z) {
+      // NestWorld: lock-free read of an already-FULL chunk from a region thread.
+      // Uses the region thread's own chunk cache because entity ticking does
+      // thousands of block reads per tick (collisions, AI, pathfinding).
+      net.nestworld.region.RegionThread regionThread = (net.nestworld.region.RegionThread) Thread.currentThread();
+      long i = ChunkPos.m_45589_(p_x, p_z);
+      LevelChunk cached = regionThread.getCachedChunk(i);
+      if (cached != null) return cached;
+      // Reliable read of the main-published loaded-FULL snapshot: avoids missing an
+      // already-loaded chunk (stale visibleChunkMap from off-main) and blocking on
+      // getChunk, which caused a park/unpark herd on the main thread under load.
+      LevelChunk pub = this.nestworldLoadedFull.get(i);
+      if (pub != null) { regionThread.cacheChunk(i, pub); return pub; }
+      ChunkHolder chunkholder = this.m_8364_(i);
+      if (chunkholder == null) return null;
+      LevelChunk result = null;
+      if (chunkholder.currentlyLoading != null) {
+         result = chunkholder.currentlyLoading;
+      } else {
+         Either<ChunkAccess, ChunkHolder.ChunkLoadingFailure> either = chunkholder.m_140080_(ChunkStatus.f_62326_).getNow((Either<ChunkAccess, ChunkHolder.ChunkLoadingFailure>)null);
+         if (either != null) {
+            ChunkAccess chunkaccess = either.left().orElse((ChunkAccess)null);
+            if (chunkaccess instanceof LevelChunk) result = (LevelChunk)chunkaccess;
+         }
+      }
+      if (result != null) regionThread.cacheChunk(i, result);
+      return result;
+   }
+
+   /** Main-published snapshot of loaded FULL chunks; region threads read it
+    *  lock-free to skip blocking getChunk + the resulting park/unpark herd.
+    *  Refilled + volatile-published each tick on main; never mutated while
+    *  published. The two backing maps ping-pong: the one being refilled was
+    *  published two ticks ago and is unreachable by then (region threads are
+    *  parked when the refresh runs and re-read the volatile field each access),
+    *  so reuse avoids a fresh ~80KB map allocation per tick at 10k chunks. */
+   public volatile it.unimi.dsi.fastutil.longs.Long2ObjectMap<LevelChunk> nestworldLoadedFull =
+         it.unimi.dsi.fastutil.longs.Long2ObjectMaps.emptyMap();
+   private final it.unimi.dsi.fastutil.longs.Long2ObjectOpenHashMap<LevelChunk> nestworldFullA =
+         new it.unimi.dsi.fastutil.longs.Long2ObjectOpenHashMap<>();
+   private final it.unimi.dsi.fastutil.longs.Long2ObjectOpenHashMap<LevelChunk> nestworldFullB =
+         new it.unimi.dsi.fastutil.longs.Long2ObjectOpenHashMap<>();
+   private boolean nestworldFullFlip;
+
+   // NestWorld 2026-08-11 (live ATM9 incident investigation, distancemanager-tier1-volume-freeze.md
+   // follow-up): this method's own cost is O(chunkMap.getChunks().size()) -- ALL currently-tracked
+   // ChunkHolders (any status, not just loaded/FULL ones), not just a "pending" backlog -- and it
+   // runs unconditionally, first thing, every single tick. Adding matching timing telemetry to the
+   // existing nestworldTimedUpdateFutures pattern, so a repeat incident shows WHICH main-thread
+   // mechanism (this one, or DistanceManager's promotion path, or something else) actually dominates
+   // instead of requiring a fresh live jstack session to find out again.
+   public final java.util.concurrent.atomic.AtomicLong nestworldRefreshCalls = new java.util.concurrent.atomic.AtomicLong();
+   public final java.util.concurrent.atomic.AtomicLong nestworldRefreshTotalNanos = new java.util.concurrent.atomic.AtomicLong();
+   public volatile long nestworldRefreshLastNanos = 0L;
+   public volatile long nestworldRefreshLastSize = 0L;
+   public volatile long nestworldRefreshLastScanned = 0L;
+   public volatile long nestworldRefreshMaxNanos = 0L;
+   public volatile long nestworldRefreshMaxScanned = 0L;
+
+   public void nestworldRefreshLoadedChunks() {
+      long nestworldStart = System.nanoTime();
+      it.unimi.dsi.fastutil.longs.Long2ObjectOpenHashMap<LevelChunk> m =
+            (nestworldFullFlip = !nestworldFullFlip) ? nestworldFullA : nestworldFullB;
+      m.clear();
+      long nestworldScanned = 0L;
+      for (ChunkHolder h : this.f_8325_.m_140416_()) {
+         nestworldScanned++;
+         LevelChunk c = h.currentlyLoading;
+         if (c == null) {
+            Either<ChunkAccess, ChunkHolder.ChunkLoadingFailure> e = h.m_140080_(ChunkStatus.f_62326_).getNow((Either<ChunkAccess, ChunkHolder.ChunkLoadingFailure>)null);
+            if (e != null) { ChunkAccess ca = e.left().orElse((ChunkAccess)null); if (ca instanceof LevelChunk) c = (LevelChunk)ca; }
+         }
+         if (c != null) m.put(c.m_7697_().m_45588_(), c);
+      }
+      this.nestworldLoadedFull = m;
+      long nestworldElapsed = System.nanoTime() - nestworldStart;
+      this.nestworldRefreshCalls.incrementAndGet();
+      this.nestworldRefreshTotalNanos.addAndGet(nestworldElapsed);
+      this.nestworldRefreshLastNanos = nestworldElapsed;
+      this.nestworldRefreshLastSize = m.size();
+      this.nestworldRefreshLastScanned = nestworldScanned;
+      if (nestworldElapsed > this.nestworldRefreshMaxNanos) this.nestworldRefreshMaxNanos = nestworldElapsed;
+      if (nestworldScanned > this.nestworldRefreshMaxScanned) this.nestworldRefreshMaxScanned = nestworldScanned;
+   }
+
    private void m_8488_() {
       Arrays.fill(this.f_8337_, ChunkPos.f_45577_);
       Arrays.fill(this.f_8338_, (Object)null);
@@ -203,7 +_,10 @@
       return completablefuture;
    }
 
-   private CompletableFuture<Either<ChunkAccess, ChunkHolder.ChunkLoadingFailure>> m_8456_(int p_8457_, int p_8458_, ChunkStatus p_8459_, boolean p_8460_) {
+   // NestWorld: was private. Exposed non-blocking so ForceLoadCommand can fire a
+   // whole burst of chunk futures up front and wait on them together, instead of
+   // vanilla's serial getChunk()-per-chunk (see ServerLevel.addForcedChunkAsync).
+   public CompletableFuture<Either<ChunkAccess, ChunkHolder.ChunkLoadingFailure>> m_8456_(int p_8457_, int p_8458_, ChunkStatus p_8459_, boolean p_8460_) {
       ChunkPos chunkpos = new ChunkPos(p_8457_, p_8458_);
       long i = chunkpos.m_45588_();
       int j = ChunkLevel.m_287141_(p_8459_);
@@ -265,12 +_,35 @@
    }
 
    public boolean m_8466_() {
+      // NestWorld: the one truly universal pump point — reached from every
+      // managedBlock loop (MinecraftServer's own, e.g. during prepareLevels startup,
+      // AND mainThreadProcessor's, e.g. a synchronous getChunk(load=true) wait) as well
+      // as the regular per-tick task drain. A chunkGenAdmitBudget-deferred admission
+      // MUST be drained from here, not from ChunkMap#tick alone: the startup spawn-area
+      // wait (MinecraftServer.prepareLevels -> waitUntilNextTick) never calls
+      // ChunkMap#tick at all, so a tick-only drain left every deferred admission
+      // stranded forever during server startup (confirmed by testing: a fresh server
+      // with the cap on hung indefinitely preparing the spawn area, 0% CPU, nothing
+      // in flight anywhere).
+      // NestWorld P0.1: attribution audit, see PollTaskAttribution.
+      long nestworldT0 = System.nanoTime();
+      this.f_8325_.nestworldDrainDeferredAdmissions();
+      net.nestworld.region.PollTaskAttribution.record(
+         net.nestworld.region.PollTaskAttribution.Category.ADMISSION_DRAIN, System.nanoTime() - nestworldT0);
       return this.f_8332_.m_7245_();
    }
 
    boolean m_8489_() {
+      // NestWorld P0.1: split the two DistanceManager/ChunkMap sub-steps for attribution.
+      long nestworldT0 = System.nanoTime();
       boolean flag = this.f_8327_.m_140805_(this.f_8325_);
+      long nestworldT1 = System.nanoTime();
+      net.nestworld.region.PollTaskAttribution.record(
+         net.nestworld.region.PollTaskAttribution.Category.DISTANCE_MANAGER, nestworldT1 - nestworldT0);
       boolean flag1 = this.f_8325_.m_140324_();
+      long nestworldT2 = System.nanoTime();
+      net.nestworld.region.PollTaskAttribution.record(
+         net.nestworld.region.PollTaskAttribution.Category.CHUNK_MAP_PROMOTE, nestworldT2 - nestworldT1);
       if (!flag && !flag1) {
          return false;
       } else {
@@ -302,19 +_,59 @@
       this.f_8325_.close();
    }
 
+   // NW-CHUNKSOURCE-SUBPHASE: breaks down the "chunkSource" bucket (ServerLevel's
+   // vanilla sub-phase timer) into distanceManager/tickChunks/unload, to isolate
+   // whether DistanceManager.runAllUpdates() itself is the dominant cost inside
+   // it or whether tickChunks()'s natural-spawn/random-tick scan dominates instead.
+   private static final boolean NESTWORLD_CS_SUBPHASE_LOG =
+           !"false".equalsIgnoreCase(System.getProperty("nestworld.timingLog", "true"));
+   private static final int NESTWORLD_CS_SUBPHASE_LOG_INTERVAL = 200;
+   private static final long[] nestworldCsSubphaseNanos = new long[3];
+   private static int nestworldCsSubphaseTicks = 0;
+
+   /** NestWorld: remaining main-thread getChunk() FULL+load burst budget for THIS tick —
+    *  see NestworldTuning.GETCHUNK_BURST_BUDGET_MS. Reset every tick() call; only ever
+    *  read/written on the main thread (same as the rest of this class's mutable state). */
+   private long nestworldBurstBudgetRemainingNanos;
+
    public void m_201698_(BooleanSupplier p_201913_, boolean p_201914_) {
+      long nestworldCsT0 = System.nanoTime();
+      this.nestworldBurstBudgetRemainingNanos = net.nestworld.region.NestworldTuning.GETCHUNK_BURST_BUDGET_MS > 0
+            ? net.nestworld.region.NestworldTuning.GETCHUNK_BURST_BUDGET_MS * 1_000_000L : Long.MAX_VALUE;
+      this.f_8327_.nestworldResetGenBudget(); // NestWorld: refresh per-tick chunk-promotion budget
       this.f_8329_.m_46473_().m_6180_("purge");
       this.f_8327_.m_140776_();
       this.m_8489_();
+      long nestworldCsT1 = System.nanoTime();
       this.f_8329_.m_46473_().m_6182_("chunks");
       if (p_201914_) {
          this.m_8490_();
       }
+      long nestworldCsT2 = System.nanoTime();
 
       this.f_8329_.m_46473_().m_6182_("unload");
       this.f_8325_.m_140280_(p_201913_);
       this.f_8329_.m_46473_().m_7238_();
       this.m_8488_();
+      long nestworldCsT3 = System.nanoTime();
+
+      if (this.f_8329_.m_46472_() == Level.f_46428_) {
+         nestworldCsSubphaseNanos[0] += nestworldCsT1 - nestworldCsT0; // purge + DistanceManager.runAllUpdates()
+         nestworldCsSubphaseNanos[1] += nestworldCsT2 - nestworldCsT1; // tickChunks (natural spawn scan, random tick, broadcast)
+         nestworldCsSubphaseNanos[2] += nestworldCsT3 - nestworldCsT2; // chunkMap.tick() (unload queue) + clearCache
+         if (++nestworldCsSubphaseTicks >= NESTWORLD_CS_SUBPHASE_LOG_INTERVAL) {
+            if (NESTWORLD_CS_SUBPHASE_LOG) {
+               org.slf4j.LoggerFactory.getLogger("NestWorld/ChunkSourceSubphase").info(
+                     "chunkSource sub-phases avg ms over {} ticks: distanceManager={} tickChunks={} unload={}",
+                     nestworldCsSubphaseTicks,
+                     String.format("%.2f", nestworldCsSubphaseNanos[0] / 1e6 / nestworldCsSubphaseTicks),
+                     String.format("%.2f", nestworldCsSubphaseNanos[1] / 1e6 / nestworldCsSubphaseTicks),
+                     String.format("%.2f", nestworldCsSubphaseNanos[2] / 1e6 / nestworldCsSubphaseTicks));
+            }
+            java.util.Arrays.fill(nestworldCsSubphaseNanos, 0L);
+            nestworldCsSubphaseTicks = 0;
+         }
+      }
    }
 
    private void m_8490_() {
@@ -322,8 +_,16 @@
       long j = i - this.f_8334_;
       this.f_8334_ = i;
       boolean flag = this.f_8329_.m_46659_();
+      // NestWorld: the tracker's skip-check (nestworldTrackedTick == nestworldCurTick) can only
+      // ever succeed if this runs AFTER region threads set that stamp in their own tick phase —
+      // this call happens here (phase 1) strictly BEFORE that phase, so the check always failed
+      // and every owned entity was tracked twice (once here, once by its region). Defer to
+      // NestworldRegionSystem.tickAllRegions(), which calls chunkMap.tick() itself once region
+      // threads have finished for this tick.
+      boolean nestworldDeferTrackerTick = net.nestworld.region.NestworldRegionSystem.isInitialised()
+            && this.f_8329_.m_46472_() == Level.f_46428_;
       if (flag) {
-         this.f_8325_.m_140421_();
+         if (!nestworldDeferTrackerTick) this.f_8325_.m_140421_();
       } else {
          LevelData leveldata = this.f_8329_.m_6106_();
          ProfilerFiller profilerfiller = this.f_8329_.m_46473_();
@@ -351,7 +_,7 @@
          for(ServerChunkCache.ChunkAndHolder serverchunkcache$chunkandholder : list) {
             LevelChunk levelchunk1 = serverchunkcache$chunkandholder.f_184028_;
             ChunkPos chunkpos = levelchunk1.m_7697_();
-            if (this.f_8329_.m_201916_(chunkpos) && this.f_8325_.m_183879_(chunkpos)) {
+            if ((this.f_8329_.m_201916_(chunkpos) && this.f_8325_.m_183879_(chunkpos)) || this.f_8327_.shouldForceTicks(chunkpos.m_45588_())) {
                levelchunk1.m_187632_(j);
                if (flag2 && (this.f_8335_ || this.f_8336_) && this.f_8329_.m_6857_().m_61927_(chunkpos)) {
                   NaturalSpawner.m_47029_(this.f_8329_, levelchunk1, naturalspawner$spawnstate, this.f_8336_, this.f_8335_, flag1);
@@ -363,6 +_,10 @@
             }
          }
 
+         if (net.nestworld.region.NestworldRegionSystem.isInitialised()) {
+            net.nestworld.region.NestworldRegionSystem.get().flushRandomTicksPhase();
+         }
+
          profilerfiller.m_6182_("customSpawners");
          if (flag2) {
             this.f_8329_.m_8799_(this.f_8335_, this.f_8336_);
@@ -374,7 +_,7 @@
          });
          profilerfiller.m_7238_();
          profilerfiller.m_7238_();
-         this.f_8325_.m_140421_();
+         if (!nestworldDeferTrackerTick) this.f_8325_.m_140421_();
       }
    }
 
@@ -432,11 +_,17 @@
    }
 
    public <T> void m_8387_(TicketType<T> p_8388_, ChunkPos p_8389_, int p_8390_, T p_8391_) {
-      this.f_8327_.m_140840_(p_8388_, p_8389_, p_8390_, p_8391_);
+      addRegionTicket(p_8388_, p_8389_, p_8390_, p_8391_, false);
+   }
+   public <T> void addRegionTicket(TicketType<T> p_8388_, ChunkPos p_8389_, int p_8390_, T p_8391_, boolean forceTicks) {
+      this.f_8327_.addRegionTicket(p_8388_, p_8389_, p_8390_, p_8391_, forceTicks);
    }
 
    public <T> void m_8438_(TicketType<T> p_8439_, ChunkPos p_8440_, int p_8441_, T p_8442_) {
-      this.f_8327_.m_140849_(p_8439_, p_8440_, p_8441_, p_8442_);
+      removeRegionTicket(p_8439_, p_8440_, p_8441_, p_8442_, false);
+   }
+   public <T> void removeRegionTicket(TicketType<T> p_8439_, ChunkPos p_8440_, int p_8441_, T p_8442_, boolean forceTicks) {
+      this.f_8327_.removeRegionTicket(p_8439_, p_8440_, p_8441_, p_8442_, forceTicks);
    }
 
    public void m_6692_(ChunkPos p_8400_, boolean p_8401_) {
@@ -451,10 +_,32 @@
    }
 
    public void m_8443_(Entity p_8444_) {
+      // NestWorld: a region thread discarding an entity (e.g. TNT consumed by an
+      // explosion) must not mutate ChunkMap's non-thread-safe entityMap while the
+      // main thread iterates it in ChunkMap.tick. Queue the tracking removal for
+      // main (drained at the region barrier), symmetric with off-main spawns.
+      if (net.nestworld.region.NestworldRegionSystem.isInitialised()
+            && this.f_8329_.m_46472_() == net.minecraft.world.level.Level.f_46428_
+            && Thread.currentThread() instanceof net.nestworld.region.RegionThread) {
+         net.nestworld.region.NestworldRegionSystem.get().queueEntityRemoval(p_8444_);
+         return;
+      }
       this.f_8325_.m_140331_(p_8444_);
    }
 
    public void m_8463_(Entity p_8464_) {
+      // NestWorld: symmetric with removeEntity above. A region thread can reach this through a
+      // section-move visibility transition (e.g. an enderman teleporting into an entity-ticking
+      // section: setPosRaw -> PESM Callback.onMove -> updateStatus -> startTracking), which must
+      // not mutate ChunkMap's non-thread-safe entityMap off-main. Queue the tracking add for the
+      // main-thread drain at the region barrier (drained after removals, so a leave+re-enter
+      // within one tick resolves to the correct final tracked state).
+      if (net.nestworld.region.NestworldRegionSystem.isInitialised()
+            && this.f_8329_.m_46472_() == net.minecraft.world.level.Level.f_46428_
+            && Thread.currentThread() instanceof net.nestworld.region.RegionThread) {
+         net.nestworld.region.NestworldRegionSystem.get().queueEntityTrackingAdd(p_8464_);
+         return;
+      }
       this.f_8325_.m_140199_(p_8464_);
    }
 
@@ -531,14 +_,44 @@
 
       protected void m_6367_(Runnable p_8502_) {
          ServerChunkCache.this.f_8329_.m_46473_().m_6174_("runTask");
+         // NestWorld P0.1: this is the ONE place a generic queued task actually executes, so it's
+         // the natural O(1)-per-task hook for the "worldgen completion / region dispatch / other"
+         // sub-breakdown the audit asked for — classified by class name, timed individually, no scan.
+         long nestworldT0 = System.nanoTime();
          super.m_6367_(p_8502_);
+         long nestworldElapsed = System.nanoTime() - nestworldT0;
+         String nestworldClassName = p_8502_.getClass().getName();
+         net.nestworld.region.PollTaskAttribution.Category nestworldCat =
+            net.nestworld.region.PollTaskAttribution.classifyGeneric(p_8502_);
+         net.nestworld.region.PollTaskAttribution.record(nestworldCat, nestworldElapsed);
+         if (nestworldCat == net.nestworld.region.PollTaskAttribution.Category.GENERIC_OTHER) {
+            net.nestworld.region.PollTaskAttribution.recordGenericOtherDetail(nestworldClassName, nestworldElapsed);
+         }
+         // P1.0 audit (docs/P1_WORLDGEN_MAINTHREAD_DECOUPLING_SPEC.md): worldgen/main-thread
+         // boundary classification, independent of the coarser Category above -- see
+         // classifyBoundary's javadoc for the heuristic and its caveats.
+         net.nestworld.region.PollTaskAttribution.recordBoundary(
+            net.nestworld.region.PollTaskAttribution.classifyBoundary(nestworldClassName), nestworldElapsed);
       }
 
       public boolean m_7245_() {
+         // NestWorld: managedBlock() (called both here and from MinecraftServer's own
+         // startup/tick wait loops) dispatches to THIS override directly, bypassing
+         // ServerChunkCache's own pollTask() wrapper entirely — so the admit-budget
+         // drain has to live here too, not just there, to cover every synchronous wait.
+         long nestworldT0 = System.nanoTime();
+         ServerChunkCache.this.f_8325_.nestworldDrainDeferredAdmissions();
+         net.nestworld.region.PollTaskAttribution.record(
+            net.nestworld.region.PollTaskAttribution.Category.ADMISSION_DRAIN, System.nanoTime() - nestworldT0);
          if (ServerChunkCache.this.m_8489_()) {
             return true;
          } else {
+            long nestworldT1 = System.nanoTime();
             ServerChunkCache.this.f_8331_.m_9409_();
+            net.nestworld.region.PollTaskAttribution.record(
+               net.nestworld.region.PollTaskAttribution.Category.LIGHTING, System.nanoTime() - nestworldT1);
+            // super.pollTask()'s own queue-drain time is captured per-task in doRunTask() above,
+            // not double-counted here.
             return super.m_7245_();
          }
       }
