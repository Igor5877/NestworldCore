--- a/net/minecraft/server/commands/ForceLoadCommand.java
+++ b/net/minecraft/server/commands/ForceLoadCommand.java
@@ -29,6 +_,28 @@
    private static final SimpleCommandExceptionType f_137670_ = new SimpleCommandExceptionType(Component.m_237115_("commands.forceload.added.failure"));
    private static final SimpleCommandExceptionType f_137671_ = new SimpleCommandExceptionType(Component.m_237115_("commands.forceload.removed.failure"));
 
+   // NestWorld Layer 12: shared cross-call burst budget for /forceload's own blocking wait.
+   // Same bug class + same fix shape as ServerChunkCache.getChunk()'s burst-budget-first-call
+   // gap (NestworldTuning.GETCHUNK_BURST_BUDGET_MS): FORCELOAD_WAIT_TIMEOUT_MS alone bounds a
+   // SINGLE call, but several calls issued back to back each independently get their own full
+   // allowance and can still stack past the 60s watchdog. Rolling wall-clock window (not
+   // per-tick: forceload is infrequent/administrative, and one call's own wait can itself span
+   // many ticks) -- see NestworldTuning.FORCELOAD_BURST_BUDGET_MS / FORCELOAD_BURST_WINDOW_MS.
+   // Main-thread-only (forceload always runs on the server thread), so plain static fields
+   // are safe -- no synchronization needed.
+   private static long nestworldForceloadBudgetWindowStartNanos = 0L;
+   private static long nestworldForceloadBudgetRemainingNanos = 0L;
+
+   private static long nestworldForceloadCallBudgetNanos() {
+      long now = System.nanoTime();
+      long windowNanos = net.nestworld.region.NestworldTuning.FORCELOAD_BURST_WINDOW_MS * 1_000_000L;
+      if (nestworldForceloadBudgetWindowStartNanos == 0L || now - nestworldForceloadBudgetWindowStartNanos > windowNanos) {
+         nestworldForceloadBudgetWindowStartNanos = now;
+         nestworldForceloadBudgetRemainingNanos = net.nestworld.region.NestworldTuning.FORCELOAD_BURST_BUDGET_MS * 1_000_000L;
+      }
+      return Math.max(0L, nestworldForceloadBudgetRemainingNanos);
+   }
+
    public static void m_137676_(CommandDispatcher<CommandSourceStack> p_137677_) {
       p_137677_.register(Commands.m_82127_("forceload").requires((p_137703_) -> {
          return p_137703_.m_6761_(2);
@@ -119,13 +_,103 @@
             ChunkPos chunkpos = null;
             int j2 = 0;
 
-            for(int k2 = i1; k2 <= k1; ++k2) {
-               for(int l2 = j1; l2 <= l1; ++l2) {
-                  boolean flag = serverlevel.m_8602_(k2, l2, p_137689_);
-                  if (flag) {
-                     ++j2;
-                     if (chunkpos == null) {
-                        chunkpos = new ChunkPos(k2, l2);
+            if (p_137689_) {
+               // NestWorld: fire every chunk's load future up front instead of
+               // blocking per-chunk. Vanilla's serial setChunkForced() loop turns
+               // an N-chunk burst into N sequential managedBlock waits (pays for
+               // the SUM of all N generation costs on the main thread); this pays
+               // for the MAX of N costs running in parallel across the worker
+               // pool instead, one wait at the end. Still routes through
+               // ChunkHolder.getOrScheduleFuture, so chunkGenAdmitBudget /
+               // chunkGenMaxConcurrent still gate it exactly as before — this
+               // only removes vanilla's extra artificial seriality on top of
+               // that gating. See docs/GEN_SPIKE.md.
+               java.util.List<java.util.concurrent.CompletableFuture<?>> nestworldFutures = new java.util.ArrayList<>();
+               for(int k2 = i1; k2 <= k1; ++k2) {
+                  for(int l2 = j1; l2 <= l1; ++l2) {
+                     java.util.concurrent.CompletableFuture<?> future = serverlevel.addForcedChunkAsync(k2, l2);
+                     if (future != null) {
+                        ++j2;
+                        if (chunkpos == null) {
+                           chunkpos = new ChunkPos(k2, l2);
+                        }
+                        nestworldFutures.add(future);
+                     }
+                  }
+               }
+               if (!nestworldFutures.isEmpty()) {
+                  java.util.concurrent.CompletableFuture<Void> nestworldAll =
+                        java.util.concurrent.CompletableFuture.allOf(nestworldFutures.toArray(new java.util.concurrent.CompletableFuture[0]));
+                  // NestWorld: bound the wait itself, not just its shape. The batching above
+                  // already turns N sequential waits into one wait for the slowest chunk among
+                  // them — but on a heavy real modpack even ONE virgin chunk's generation cost
+                  // can itself exceed the 60s ServerHangWatchdog threshold, hard-killing the
+                  // whole JVM with no graceful shutdown (reproduced live 2026-08-07, see
+                  // NestworldTuning.FORCELOAD_WAIT_TIMEOUT_MS's javadoc).
+                  //
+                  // NOT using CompletableFuture.orTimeout(): an earlier version of this fix did,
+                  // and it reproducibly still hit the 60s watchdog on a real 419-mod pack under
+                  // repeated forceload calls. Mirrors the ServerChunkCache.getChunk() proxy
+                  // fix's approach instead: check a plain System.nanoTime() deadline INSIDE the
+                  // BooleanSupplier that managedBlock itself polls on the main thread, so the
+                  // check has no dependency on a separate scheduler thread.
+                  //
+                  // RESOLVED 2026-08-07 (see forceload-freeze-root-cause-found in project
+                  // memory): repeated forceload calls WERE still hitting the 60s watchdog even
+                  // with this deadline check in place, but not for the reason first suspected
+                  // (GC pauses were directly ruled out via -Xlog:gc* -- zero GC activity during
+                  // the freeze window). JFR execution sampling found the Server thread genuinely
+                  // executing real work the whole time inside ChunkMap's own bulk admission
+                  // gate (chunkGenAdmitBudget), which used to double as a hard standing
+                  // in-flight concurrency cap pinned to the same small value as its per-window
+                  // rate -- serializing a routine forceload's ~40-60 scheduling units into 15+
+                  // sequential rounds. Fixed at the root in ChunkMap.nestworldHasAdmitCapacity()
+                  // (commit 31d84d667) by decoupling the standing cap into its own independently
+                  // configured NestworldTuning.CHUNK_GEN_ADMIT_INFLIGHT_CAP (default unlimited).
+                  // This deadline check remains valuable as defense in depth regardless.
+                  //
+                  // Layer 12 (2026-08-11): the check above stopped ONE call from blocking past
+                  // FORCELOAD_WAIT_TIMEOUT_MS, but said nothing about SEVERAL calls issued back
+                  // to back -- each independently got its own full allowance. Live-crashed a
+                  // real ATM9 server (4-call, 1024-chunk batch, Watchdog kill at 60s) despite
+                  // this per-call bound already being in place. Now every call's own deadline is
+                  // capped to whichever is smaller: its per-call timeout, or what's left of the
+                  // shared rolling-window burst budget -- mirrors ServerChunkCache.getChunk()'s
+                  // Math.min(perCallTimeout, remainingBudget) fix exactly.
+                  if (net.nestworld.region.NestworldTuning.FORCELOAD_WAIT_TIMEOUT_MS > 0) {
+                     long nestworldCallBudgetNanos = Math.min(
+                           net.nestworld.region.NestworldTuning.FORCELOAD_WAIT_TIMEOUT_MS * 1_000_000L,
+                           nestworldForceloadCallBudgetNanos());
+                     long nestworldWaitStart = System.nanoTime();
+                     long nestworldDeadline = nestworldWaitStart + nestworldCallBudgetNanos;
+                     p_137686_.m_81377_().m_18701_(() -> nestworldAll.isDone() || System.nanoTime() >= nestworldDeadline);
+                     nestworldForceloadBudgetRemainingNanos -= (System.nanoTime() - nestworldWaitStart);
+                     if (!nestworldAll.isDone()) {
+                        // The forced-chunk registration above already happened unconditionally
+                        // before this wait started; timing out here does not undo it —
+                        // generation continues on the normal worker pool regardless.
+                        int nestworldCount = j2;
+                        p_137686_.m_288197_(() -> Component.m_237113_(nestworldCount
+                              + " chunks marked forced; generation is taking longer than "
+                              + (net.nestworld.region.NestworldTuning.FORCELOAD_WAIT_TIMEOUT_MS / 1000) + "s and continues in the background."), true);
+                        return nestworldCount;
+                     }
+                     // Completed within the deadline after all — join() below is now safe
+                     // and won't block (isDone() is already true).
+                  } else {
+                     p_137686_.m_81377_().m_18701_(nestworldAll::isDone);
+                  }
+                  nestworldAll.join();
+               }
+            } else {
+               for(int k2 = i1; k2 <= k1; ++k2) {
+                  for(int l2 = j1; l2 <= l1; ++l2) {
+                     boolean flag = serverlevel.m_8602_(k2, l2, false);
+                     if (flag) {
+                        ++j2;
+                        if (chunkpos == null) {
+                           chunkpos = new ChunkPos(k2, l2);
+                        }
                      }
                   }
                }
