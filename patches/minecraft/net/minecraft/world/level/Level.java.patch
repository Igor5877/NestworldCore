--- a/net/minecraft/world/level/Level.java
+++ b/net/minecraft/world/level/Level.java
@@ -70,7 +_,7 @@
 import net.minecraft.world.phys.Vec3;
 import net.minecraft.world.scores.Scoreboard;
 
-public abstract class Level implements LevelAccessor, AutoCloseable {
+public abstract class Level extends net.minecraftforge.common.capabilities.CapabilityProvider<Level> implements LevelAccessor, AutoCloseable, net.minecraftforge.common.extensions.IForgeLevel {
    public static final Codec<ResourceKey<Level>> f_46427_ = ResourceKey.m_195966_(Registries.f_256858_);
    public static final ResourceKey<Level> f_46428_ = ResourceKey.m_135785_(Registries.f_256858_, new ResourceLocation("overworld"));
    public static final ResourceKey<Level> f_46429_ = ResourceKey.m_135785_(Registries.f_256858_, new ResourceLocation("the_nether"));
@@ -95,7 +_,7 @@
    public float f_46438_;
    public float f_46439_;
    public float f_46440_;
-   public final RandomSource f_46441_ = RandomSource.m_216327_();
+   public final RandomSource f_46441_ = RandomSource.m_216337_(); // NestWorld: thread-safe random for parallel region entity ticking
    /** @deprecated */
    @Deprecated
    private final RandomSource f_220348_ = RandomSource.m_216337_();
@@ -110,8 +_,14 @@
    private final RegistryAccess f_268710_;
    private final DamageSources f_268497_;
    private long f_186455_;
+   public boolean restoringBlockSnapshots = false;
+   public boolean captureBlockSnapshots = false;
+   public java.util.ArrayList<net.minecraftforge.common.util.BlockSnapshot> capturedBlockSnapshots = new java.util.ArrayList<>();
+   private final java.util.ArrayList<BlockEntity> freshBlockEntities = new java.util.ArrayList<>();
+   private final java.util.ArrayList<BlockEntity> pendingFreshBlockEntities = new java.util.ArrayList<>();
 
    protected Level(WritableLevelData p_270739_, ResourceKey<Level> p_270683_, RegistryAccess p_270200_, Holder<DimensionType> p_270240_, Supplier<ProfilerFiller> p_270692_, boolean p_270904_, boolean p_270470_, long p_270248_, int p_270466_) {
+      super(Level.class);
       this.f_46446_ = p_270692_;
       this.f_46442_ = p_270739_;
       this.f_204147_ = p_270240_;
@@ -196,13 +_,89 @@
       } else if (!this.f_46443_ && this.m_46659_()) {
          return false;
       } else {
+         // NestWorld: Stage 3 EXTENSION beyond Explosion (docs/LOCAL_TICK_STAGE4.md,
+         // "entity-triggered block-write race" -> classification table's remaining UNSAFE
+         // rows: mob-AI direct block writes, mod code bypassing vanilla Explosion). Covers
+         // THIS single choke point that setBlock/removeBlock/destroyBlock/setBlockAndUpdate
+         // all funnel through — e.g. EnderDragon/WitherBoss goals, and Draconic Evolution's
+         // reactor (confirmed via bytecode read the same session to call ServerLevel
+         // .removeBlock() directly, bypassing vanilla Explosion entirely).
+         //
+         // Stage 5 "Blocker 3" widening (docs/LOCAL_TICK_STAGE4.md, "border-band under
+         // independent local time"): the ORIGINAL version of this guard only checked
+         // ownership (out-of-bounds), not border-band proximity — a write comfortably
+         // inside the calling region's own bounds but within BORDER_BAND_CHUNKS of one of
+         // its edges applied directly, with its full neighborChanged/piston/comparator
+         // cascade, unlike the other 3 border-band-routed phases (scheduled ticks, block
+         // events, block entities) which have always kept ANYTHING in the band off region
+         // threads entirely, specifically because a cascade can reach a few blocks past its
+         // origin. Now both cases — out-of-bounds AND in-bounds-but-in-band — get the same
+         // deferred treatment (posted to the OWNING region's mailbox, which for the
+         // in-band-but-in-bounds case is the calling region itself — same barrier-safe
+         // apply point, just off the region thread's own call stack).
+         //
+         // The cheap containsChunk()/isInBorderBand() bounds checks (both simple O(1)
+         // rectangle tests against the CALLING region, not a grid scan) filter out the
+         // overwhelming common case — writing well inside your own territory — for free;
+         // only a genuine border-band or cross-region write (rare) pays for the grid lookup.
+         if (Thread.currentThread() instanceof net.nestworld.region.RegionThread nestworldRt) {
+            net.nestworld.region.WorldRegion nestworldCallingRegion = nestworldRt.getRegion();
+            int nestworldCx = p_46605_.m_123341_() >> 4;
+            int nestworldCz = p_46605_.m_123343_() >> 4;
+            boolean nestworldOutOfBounds = !nestworldCallingRegion.containsChunk(nestworldCx, nestworldCz);
+            if ((nestworldOutOfBounds || nestworldCallingRegion.isInBorderBand(nestworldCx, nestworldCz))
+                  && net.nestworld.region.NestworldRegionSystem.isInitialised()
+                  && net.nestworld.region.NestworldRegionSystem.get().isManagedLevel(this)) {
+               net.nestworld.region.WorldRegion nestworldOwner = nestworldOutOfBounds
+                     ? net.nestworld.region.NestworldRegionSystem.get().getDimensionRegion(this).getGrid().getRegionForChunk(nestworldCx, nestworldCz)
+                     : nestworldCallingRegion;
+               if (nestworldOwner != null) {
+                  return net.nestworld.region.NestworldRegionSystem.get().nestworldDeferForeignBlockWrite(
+                        nestworldCallingRegion, nestworldOwner, p_46605_, p_46606_, p_46607_, p_46608_);
+               }
+            }
+         }
+         // Phase 9 (docs/REGION_OWNERSHIP_CROSS_REGION_SPEC.md sec13): validates the Stage 3
+         // guard's own "safe to write directly here" belief, from every caller shape (region
+         // thread in-bounds, region thread applying a just-drained deferred write, or main
+         // thread). No-op unless -Dnestworld.ownershipAssertionsEnabled=true.
+         net.nestworld.region.NestworldOwnershipAssertions.assertBlockOwner(this, p_46605_, "setBlock");
          LevelChunk levelchunk = this.m_46745_(p_46605_);
          Block block = p_46606_.m_60734_();
+
+         p_46605_ = p_46605_.m_7949_(); // Forge - prevent mutable BlockPos leaks
+         net.minecraftforge.common.util.BlockSnapshot blockSnapshot = null;
+         if (this.captureBlockSnapshots && !this.f_46443_) {
+             blockSnapshot = net.minecraftforge.common.util.BlockSnapshot.create(this.f_46421_, this, p_46605_, p_46607_);
+             this.capturedBlockSnapshots.add(blockSnapshot);
+         }
+
+         BlockState old = m_8055_(p_46605_);
+         int oldLight = old.getLightEmission(this, p_46605_);
+         int oldOpacity = old.m_60739_(this, p_46605_);
+
          BlockState blockstate = levelchunk.m_6978_(p_46605_, p_46606_, (p_46607_ & 64) != 0);
          if (blockstate == null) {
+            if (blockSnapshot != null) this.capturedBlockSnapshots.remove(blockSnapshot);
             return false;
          } else {
             BlockState blockstate1 = this.m_8055_(p_46605_);
+
+            if (blockSnapshot == null) { // Don't notify clients or update physics while capturing blockstates
+               this.markAndNotifyBlock(p_46605_, levelchunk, blockstate, p_46606_, p_46607_, p_46608_);
+            }
+
+            return true;
+         }
+      }
+   }
+
+   // Split off from original setBlockState(BlockPos, BlockState, int, int) method in order to directly send client and physic updates
+   public void markAndNotifyBlock(BlockPos p_46605_, @Nullable LevelChunk levelchunk, BlockState blockstate, BlockState p_46606_, int p_46607_, int p_46608_) {
+      Block block = p_46606_.m_60734_();
+      BlockState blockstate1 = m_8055_(p_46605_);
+      {
+         {
             if (blockstate1 == p_46606_) {
                if (blockstate != blockstate1) {
                   this.m_6550_(p_46605_, blockstate, blockstate1);
@@ -227,9 +_,8 @@
                }
 
                this.m_6559_(p_46605_, blockstate, blockstate1);
+               p_46606_.onBlockStateChange(this, p_46605_, blockstate);
             }
-
-            return true;
          }
       }
    }
@@ -279,6 +_,7 @@
    }
 
    public void m_46672_(BlockPos p_46673_, Block p_46674_) {
+      net.minecraftforge.event.ForgeEventFactory.onNeighborNotify(this, p_46673_, this.m_8055_(p_46673_), java.util.EnumSet.allOf(Direction.class), false).isCanceled();
    }
 
    public void m_46590_(BlockPos p_46591_, Block p_46592_, Direction p_46593_) {
@@ -291,7 +_,12 @@
    }
 
    public void m_213683_(Direction p_220385_, BlockState p_220386_, BlockPos p_220387_, BlockPos p_220388_, int p_220389_, int p_220390_) {
-      this.f_220350_.m_213547_(p_220385_, p_220386_, p_220387_, p_220388_, p_220389_, p_220390_);
+      this.nestworldNeighborUpdater().m_213547_(p_220385_, p_220386_, p_220387_, p_220388_, p_220389_, p_220390_);
+   }
+
+   public NeighborUpdater nestworldNeighborUpdater() {
+      NeighborUpdater nestworldNu = net.nestworld.region.NestworldRedstone.regionNeighborUpdater(this);
+      return nestworldNu != null ? nestworldNu : this.f_220350_;
    }
 
    public int m_6924_(Heightmap.Types p_46571_, int p_46572_, int p_46573_) {
@@ -395,41 +_,103 @@
    }
 
    public void m_151525_(TickingBlockEntity p_151526_) {
-      (this.f_151504_ ? this.f_151503_ : this.f_151512_).add(p_151526_);
+      synchronized (this.f_151503_) {
+         (this.f_151504_ ? this.f_151503_ : this.f_151512_).add(p_151526_);
+      }
+   }
+
+   public void addFreshBlockEntities(java.util.Collection<BlockEntity> beList) {
+      synchronized (this.pendingFreshBlockEntities) {
+         if (this.f_151504_) {
+            this.pendingFreshBlockEntities.addAll(beList);
+         } else {
+            this.freshBlockEntities.addAll(beList);
+         }
+      }
    }
 
    protected void m_46463_() {
       ProfilerFiller profilerfiller = this.m_46473_();
       profilerfiller.m_6180_("blockEntities");
+      synchronized (this.pendingFreshBlockEntities) {
+         if (!this.pendingFreshBlockEntities.isEmpty()) {
+            this.freshBlockEntities.addAll(this.pendingFreshBlockEntities);
+            this.pendingFreshBlockEntities.clear();
+         }
+      }
       this.f_151504_ = true;
+      if (!this.freshBlockEntities.isEmpty()) {
+         this.freshBlockEntities.forEach(BlockEntity::onLoad);
+         this.freshBlockEntities.clear();
+      }
+      synchronized (this.f_151503_) {
       if (!this.f_151503_.isEmpty()) {
          this.f_151512_.addAll(this.f_151503_);
          this.f_151503_.clear();
       }
+      }
 
       Iterator<TickingBlockEntity> iterator = this.f_151512_.iterator();
+      java.util.List<TickingBlockEntity> nestworldDue = net.nestworld.region.NestworldRegionSystem.beginBlockEntityPhase(this);
 
       while(iterator.hasNext()) {
          TickingBlockEntity tickingblockentity = iterator.next();
          if (tickingblockentity.m_142220_()) {
             iterator.remove();
          } else if (this.m_220393_(tickingblockentity.m_142689_())) {
-            tickingblockentity.m_142224_();
+            if (nestworldDue != null) {
+               nestworldDue.add(tickingblockentity);
+            } else {
+               tickingblockentity.m_142224_();
+            }
          }
+      }
+
+      if (nestworldDue != null) {
+         net.nestworld.region.NestworldRegionSystem.get().runBlockEntityPhase((net.minecraft.server.level.ServerLevel)this, nestworldDue);
       }
 
       this.f_151504_ = false;
       profilerfiller.m_7238_();
    }
 
+   // NestWorld: overworld entities are protected from a single bad tick taking down the
+   // whole server by RegionThread.tickEntities()'s own catch(Throwable) (see RegionThread.java).
+   // This vanilla method is the ONLY per-entity guard for Nether/End (and single-player/
+   // non-region-sharded worlds in general) -- those dimensions are not region-sharded and
+   // still run this exact loop (see ServerLevel.java's entity-tick loop, guarded to skip only
+   // for the overworld once NestworldRegionSystem is initialised). Vanilla+Forge's default here
+   // is to rethrow as a ReportedException whenever removeErroringEntities (a Forge config the
+   // server owner must remember to set) is false -- which crashes the ENTIRE server for a single
+   // misbehaving entity (confirmed live: a StackOverflowError from a recursive SlabBlock/
+   // DirectionalPlaceContext canSurvive<->getStateForPlacement loop in the_end, 2026-08-17).
+   // Recover unconditionally instead, mirroring RegionThread's own resilience so every
+   // dimension gets the same guarantee regardless of that config flag.
+   private static final java.util.concurrent.atomic.AtomicInteger NESTWORLD_ENTITY_TICK_ERROR_STACKS = new java.util.concurrent.atomic.AtomicInteger();
+
    public <T extends Entity> void m_46653_(Consumer<T> p_46654_, T p_46655_) {
       try {
+         net.minecraftforge.server.timings.TimeTracker.ENTITY_UPDATE.trackStart(p_46655_);
+         long nestworldTickAttrStart = System.nanoTime();
          p_46654_.accept(p_46655_);
+         long nestworldTickAttrNanos = System.nanoTime() - nestworldTickAttrStart;
+         net.nestworld.region.TickAttribution.recordEntityTick(
+               net.minecraft.world.entity.EntityType.m_20613_(p_46655_.m_6095_()).m_135827_(),
+               nestworldTickAttrNanos);
+         net.nestworld.region.TickAttribution.recordChunkCost(this.m_46472_().m_135782_().toString(),
+               p_46655_.m_146902_(), nestworldTickAttrNanos);
       } catch (Throwable throwable) {
-         CrashReport crashreport = CrashReport.m_127521_(throwable, "Ticking entity");
-         CrashReportCategory crashreportcategory = crashreport.m_127514_("Entity being ticked");
-         p_46655_.m_7976_(crashreportcategory);
-         throw new ReportedException(crashreport);
+         if (NESTWORLD_ENTITY_TICK_ERROR_STACKS.getAndIncrement() < 10) {
+            com.mojang.logging.LogUtils.getLogger().warn("[NestWorld] Entity {} ({}) tick error (full stack), discarding entity",
+                  p_46655_.m_20148_(), p_46655_.m_6095_().m_20675_(), throwable);
+         } else {
+            com.mojang.logging.LogUtils.getLogger().warn("[NestWorld] Entity {} ({}) tick error: {}: {}, discarding entity",
+                  p_46655_.m_20148_(), p_46655_.m_6095_().m_20675_(),
+                  throwable.getClass().getSimpleName(), throwable.getMessage());
+         }
+         p_46655_.m_146870_();
+      } finally {
+         net.minecraftforge.server.timings.TimeTracker.ENTITY_UPDATE.trackEnd(p_46655_);
       }
    }
 
@@ -471,7 +_,7 @@
             explosion$blockinteraction1 = this.m_255157_(GameRules.f_254629_);
             break;
          case MOB:
-            explosion$blockinteraction1 = this.m_46469_().m_46207_(GameRules.f_46132_) ? this.m_255157_(GameRules.f_254692_) : Explosion.BlockInteraction.KEEP;
+            explosion$blockinteraction1 = net.minecraftforge.event.ForgeEventFactory.getMobGriefingEvent(this, p_256233_) ? this.m_255157_(GameRules.f_254692_) : Explosion.BlockInteraction.KEEP;
             break;
          case TNT:
             explosion$blockinteraction1 = this.m_255157_(GameRules.f_254705_);
@@ -482,6 +_,7 @@
 
       Explosion.BlockInteraction explosion$blockinteraction = explosion$blockinteraction1;
       Explosion explosion = new Explosion(this, p_256233_, p_255861_, p_255867_, p_256447_, p_255732_, p_255717_, p_256013_, p_256228_, explosion$blockinteraction);
+      if (net.minecraftforge.event.ForgeEventFactory.onExplosionStart(this, explosion)) return explosion;
       explosion.m_46061_();
       explosion.m_46075_(p_256377_);
       return explosion;
@@ -498,8 +_,33 @@
       if (this.m_151570_(p_46716_)) {
          return null;
       } else {
-         return !this.f_46443_ && Thread.currentThread() != this.f_46423_ ? null : this.m_46745_(p_46716_).m_5685_(p_46716_, LevelChunk.EntityCreationType.IMMEDIATE);
+         // NestWorld: region threads tick block entities — vanilla's off-main
+         // guard made getBlockEntity return null there (hopper "sees no chest").
+         // A region thread reading a position may be reading ANOTHER region's
+         // territory (previously zero protection — see RegionChunkView javadoc);
+         // route through the chunk view so cross-region reads get the ghost/
+         // locked-read protection instead of an unguarded live read.
+         if (Thread.currentThread() instanceof net.nestworld.region.RegionThread
+                 && net.nestworld.region.NestworldRegionSystem.isInitialised()
+                 && net.nestworld.region.NestworldRegionSystem.get().isManagedLevel(this)) {
+            return net.nestworld.region.NestworldRegionSystem.get().getDimensionRegion(this).getChunkView()
+                    .getBlockEntity((net.minecraft.server.level.ServerLevel) this, p_46716_);
+         }
+         return this.nestworldGetBlockEntityRaw(p_46716_);
       }
+   }
+
+   // NestWorld: the real chunk-based lookup, factored out of getBlockEntity() above so
+   // RegionChunkView's own read branches (same-region direct read, foreign-region locked
+   // read, lock-timeout fallback) can reach the actual data without calling back into
+   // getBlockEntity() — which would just redirect into RegionChunkView again and recurse
+   // forever (every one of those branches used to call level.getBlockEntity(pos), and since
+   // the calling thread is always a RegionThread here, that call would immediately re-enter
+   // the same redirect). Confirmed live: StackOverflowError on every block-entity access
+   // from a region thread under real mod load (e.g. productivebees bee tick), not just
+   // cross-region ones.
+   public BlockEntity nestworldGetBlockEntityRaw(BlockPos p_46716_) {
+      return !this.f_46443_ && Thread.currentThread() != this.f_46423_ && !(Thread.currentThread() instanceof net.nestworld.region.RegionThread) ? null : this.m_46745_(p_46716_).m_5685_(p_46716_, LevelChunk.EntityCreationType.IMMEDIATE);
    }
 
    public void m_151523_(BlockEntity p_151524_) {
@@ -513,6 +_,7 @@
       if (!this.m_151570_(p_46748_)) {
          this.m_46745_(p_46748_).m_8114_(p_46748_);
       }
+      this.m_46717_(p_46748_, m_8055_(p_46748_).m_60734_()); //Notify neighbors of changes
    }
 
    public boolean m_46749_(BlockPos p_46750_) {
@@ -583,6 +_,7 @@
             list.add(p_151522_);
          }
 
+         if (false)
          if (p_151522_ instanceof EnderDragon) {
             for(EnderDragonPart enderdragonpart : ((EnderDragon)p_151522_).m_31156_()) {
                if (p_151522_ != p_46536_ && p_46538_.test(enderdragonpart)) {
@@ -592,6 +_,11 @@
          }
 
       });
+      for (net.minecraftforge.entity.PartEntity<?> p : this.getPartEntities()) {
+         if (p != p_46536_ && p.m_20191_().m_82381_(p_46537_) && p_46538_.test(p)) {
+            list.add(p);
+         }
+      }
       return list;
    }
 
@@ -615,6 +_,8 @@
             }
          }
 
+
+         if (false)
          if (p_261454_ instanceof EnderDragon enderdragon) {
             for(EnderDragonPart enderdragonpart : enderdragon.m_31156_()) {
                T t = p_261885_.m_141992_(enderdragonpart);
@@ -629,6 +_,15 @@
 
          return AbortableIterationConsumer.Continuation.CONTINUE;
       });
+      for (net.minecraftforge.entity.PartEntity<?> p : this.getPartEntities()) {
+         T t = p_261885_.m_141992_(p);
+         if (t != null && t.m_20191_().m_82381_(p_262086_) && p_261688_.test(t)) {
+            p_262071_.add(t);
+            if (p_262071_.size() >= p_261858_) {
+               break;
+            }
+         }
+      }
    }
 
    @Nullable
@@ -760,16 +_,15 @@
    public abstract Scoreboard m_6188_();
 
    public void m_46717_(BlockPos p_46718_, Block p_46719_) {
-      for(Direction direction : Direction.Plane.HORIZONTAL) {
+      for(Direction direction : Direction.values()) {
          BlockPos blockpos = p_46718_.m_121945_(direction);
          if (this.m_46805_(blockpos)) {
             BlockState blockstate = this.m_8055_(blockpos);
-            if (blockstate.m_60713_(Blocks.f_50328_)) {
-               this.m_213960_(blockstate, blockpos, p_46719_, p_46718_, false);
-            } else if (blockstate.m_60796_(this, blockpos)) {
+            blockstate.onNeighborChange(this, blockpos, p_46718_);
+            if (blockstate.m_60796_(this, blockpos)) {
                blockpos = blockpos.m_121945_(direction);
                blockstate = this.m_8055_(blockpos);
-               if (blockstate.m_60713_(Blocks.f_50328_)) {
+               if (blockstate.getWeakChanges(this, blockpos)) {
                   this.m_213960_(blockstate, blockpos, p_46719_, p_46718_, false);
                }
             }
@@ -854,6 +_,18 @@
 
    public BiomeManager m_7062_() {
       return this.f_46420_;
+   }
+
+   private double maxEntityRadius = 2.0D;
+   @Override
+   public double getMaxEntityRadius() {
+      return maxEntityRadius;
+   }
+   @Override
+   public double increaseMaxEntityRadius(double value) {
+      if (value > maxEntityRadius)
+         maxEntityRadius = value;
+      return maxEntityRadius;
    }
 
    public final boolean m_46659_() {
