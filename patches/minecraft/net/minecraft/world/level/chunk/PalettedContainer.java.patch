--- a/net/minecraft/world/level/chunk/PalettedContainer.java
+++ b/net/minecraft/world/level/chunk/PalettedContainer.java
@@ -24,6 +_,7 @@
 import net.minecraft.util.ZeroBitStorage;
 
 public class PalettedContainer<T> implements PaletteResize<T>, PalettedContainerRO<T> {
+   // TODO: forceBits-parametered setBits function. -C
    private static final int f_188031_ = 0;
    private final PaletteResize<T> f_63070_ = (p_238275_, p_238276_) -> {
       return 0;
@@ -31,14 +_,36 @@
    private final IdMap<T> f_63071_;
    private volatile PalettedContainer.Data<T> f_188032_;
    private final PalettedContainer.Strategy f_188033_;
-   private final ThreadingDetector f_199441_ = new ThreadingDetector("PalettedContainer");
+   // NestWorld: vanilla's ThreadingDetector can only crash on concurrent
+   // access — and its threadThatFailedToAcquire is never reset, so one caught
+   // conflict (region-thread block write vs chunk-save serialisation on main)
+   // poisons the detector and the next writer parks forever (watchdog kill).
+   // All mutation/serialisation paths already bracket with acquire/release in
+   // try/finally, so a real lock makes them safely exclusive. Hot reads stay
+   // lock-free as in vanilla. A Semaphore, NOT a ReentrantLock: worldgen
+   // (NoiseBasedChunkGenerator.fillFromNoise) acquires sections on one worker
+   // thread and releases them in whenComplete on another — an owner-bound
+   // lock throws IllegalMonitorStateException there. Vanilla never acquires
+   // re-entrantly (the detector would have crashed on it), so a binary
+   // semaphore gives the same exclusion without thread affinity.
+   private final java.util.concurrent.Semaphore nestworldContainerLock = new java.util.concurrent.Semaphore(1);
 
    public void m_63084_() {
-      this.f_199441_.m_199416_();
+      this.nestworldContainerLock.acquireUninterruptibly();
+   }
+
+   /**
+    * NestWorld: non-blocking acquire — used by {@code BulkSectionAccess}'s deadlock-free
+    * multi-section acquisition (release-all-and-reacquire-in-total-order on contention,
+    * rather than blocking while already holding other sections' locks). See
+    * BulkSectionAccess.java's own comment for the full algorithm and why it's deadlock-free.
+    */
+   public boolean tryAcquire() {
+      return this.nestworldContainerLock.tryAcquire();
    }
 
    public void m_63120_() {
-      this.f_199441_.m_199422_();
+      this.nestworldContainerLock.release();
    }
 
    public static <T> Codec<PalettedContainer<T>> m_238371_(IdMap<T> p_238372_, Codec<T> p_238373_, PalettedContainer.Strategy p_238374_, T p_238375_) {
