--- a/net/minecraft/world/level/chunk/ProtoChunk.java
+++ b/net/minecraft/world/level/chunk/ProtoChunk.java
@@ -118,25 +_,37 @@
             }
 
             EnumSet<Heightmap.Types> enumset1 = this.m_6415_().m_62500_();
-            EnumSet<Heightmap.Types> enumset = null;
-
-            for(Heightmap.Types heightmap$types : enumset1) {
-               Heightmap heightmap = this.f_187608_.get(heightmap$types);
-               if (heightmap == null) {
-                  if (enumset == null) {
-                     enumset = EnumSet.noneOf(Heightmap.Types.class);
+
+            // NestWorld: a WorldGenRegion's window during FEATURES generation can span
+            // into a NEIGHBOUR chunk (ore/blob features, structures placing blocks past
+            // their "center" chunk) — that neighbour may be concurrently decorating
+            // itself on its own worker thread. this.heightmaps is a plain EnumMap and
+            // each Heightmap's internal BitStorage mutation (primeHeightmaps/update) is
+            // not thread-safe; without this lock two threads racing on the same shared
+            // chunk's heightmap can corrupt it (lost updates / torn bit-storage writes),
+            // silently producing wrong surface heights. Same bug shape, same fix as the
+            // postProcessing lock above — lock on the shared per-chunk structure itself.
+            synchronized (this.f_187608_) {
+               EnumSet<Heightmap.Types> enumset = null;
+
+               for(Heightmap.Types heightmap$types : enumset1) {
+                  Heightmap heightmap = this.f_187608_.get(heightmap$types);
+                  if (heightmap == null) {
+                     if (enumset == null) {
+                        enumset = EnumSet.noneOf(Heightmap.Types.class);
+                     }
+
+                     enumset.add(heightmap$types);
                   }
-
-                  enumset.add(heightmap$types);
-               }
-            }
-
-            if (enumset != null) {
-               Heightmap.m_64256_(this, enumset);
-            }
-
-            for(Heightmap.Types heightmap$types1 : enumset1) {
-               this.f_187608_.get(heightmap$types1).m_64249_(i1, j, k1, p_63218_);
+               }
+
+               if (enumset != null) {
+                  Heightmap.m_64256_(this, enumset);
+               }
+
+               for(Heightmap.Types heightmap$types1 : enumset1) {
+                  this.f_187608_.get(heightmap$types1).m_64249_(i1, j, k1, p_63218_);
+               }
             }
 
             return blockstate;
@@ -147,20 +_,33 @@
    }
 
    public void m_142169_(BlockEntity p_156488_) {
-      this.f_187610_.put(p_156488_.m_58899_(), p_156488_);
+      // NestWorld: same cross-chunk-write race as the heightmaps lock above — a
+      // structure/feature block entity placed past the decorating chunk's own bounds
+      // lands in a neighbour chunk's plain HashMap while that neighbour may be
+      // concurrently mutating it on its own worker thread.
+      synchronized (this.f_187610_) {
+         this.f_187610_.put(p_156488_.m_58899_(), p_156488_);
+      }
    }
 
    @Nullable
    public BlockEntity m_7702_(BlockPos p_63257_) {
-      return this.f_187610_.get(p_63257_);
+      synchronized (this.f_187610_) {
+         return this.f_187610_.get(p_63257_);
+      }
    }
 
    public Map<BlockPos, BlockEntity> m_63292_() {
       return this.f_187610_;
    }
 
+   // NestWorld: THREAD_SAFETY_PASSES.md #12 -- same crash-class as #8/the ChunkAccess
+   // structureStarts/structuresRefences fix above. entities is a plain ArrayList reachable
+   // from WorldGenRegion.getChunk(i,j).addEntity(...) across the cross-chunk write path.
    public void m_63242_(CompoundTag p_63243_) {
-      this.f_63157_.add(p_63243_);
+      synchronized (this.f_63157_) {
+         this.f_63157_.add(p_63243_);
+      }
    }
 
    public void m_6286_(Entity p_63183_) {
@@ -185,7 +_,12 @@
    }
 
    public List<CompoundTag> m_63293_() {
-      return this.f_63157_;
+      // NestWorld: defensive copy under the lock, not the live list -- same lesson as
+      // fix #9/#11 (a caller iterating the returned list off-lock would still race a
+      // concurrent addEntity() from another region thread's WorldGenRegion write).
+      synchronized (this.f_63157_) {
+         return new java.util.ArrayList<>(this.f_63157_);
+      }
    }
 
    public ChunkStatus m_6415_() {
@@ -227,14 +_,20 @@
    }
 
    public void m_8113_(BlockPos p_63266_) {
+      // NestWorld: see ChunkAccess.addPackedPostProcess's patch — same cross-chunk-boundary
+      // race, same fix (lock on the shared per-chunk postProcessing array).
       if (!this.m_151570_(p_63266_)) {
-         ChunkAccess.m_62095_(this.f_187602_, this.m_151564_(p_63266_.m_123342_())).add(m_63280_(p_63266_));
+         synchronized (this.f_187602_) {
+            ChunkAccess.m_62095_(this.f_187602_, this.m_151564_(p_63266_.m_123342_())).add(m_63280_(p_63266_));
+         }
       }
 
    }
 
    public void m_6561_(short p_63225_, int p_63226_) {
-      ChunkAccess.m_62095_(this.f_187602_, p_63226_).add(p_63225_);
+      synchronized (this.f_187602_) {
+         ChunkAccess.m_62095_(this.f_187602_, p_63226_).add(p_63225_);
+      }
    }
 
    public Map<BlockPos, CompoundTag> m_63294_() {
@@ -244,27 +_,45 @@
    @Nullable
    public CompoundTag m_8051_(BlockPos p_63275_) {
       BlockEntity blockentity = this.m_7702_(p_63275_);
-      return blockentity != null ? blockentity.m_187480_() : this.f_187609_.get(p_63275_);
+      if (blockentity != null) {
+         return blockentity.m_187480_();
+      }
+      synchronized (this.f_187609_) {
+         return this.f_187609_.get(p_63275_);
+      }
    }
 
    public void m_8114_(BlockPos p_63262_) {
-      this.f_187610_.remove(p_63262_);
-      this.f_187609_.remove(p_63262_);
+      synchronized (this.f_187610_) {
+         this.f_187610_.remove(p_63262_);
+      }
+      synchronized (this.f_187609_) {
+         this.f_187609_.remove(p_63262_);
+      }
    }
 
+   // NestWorld: THREAD_SAFETY_PASSES.md #12 -- getOrCreateCarvingMask()'s unsynchronized
+   // computeIfAbsent is exactly the "check-then-create" shape this class's own postProcessing
+   // comment already calls unsafe; same fix pattern A as the fields above.
    @Nullable
    public CarvingMask m_183612_(GenerationStep.Carving p_188185_) {
-      return this.f_63166_.get(p_188185_);
+      synchronized (this.f_63166_) {
+         return this.f_63166_.get(p_188185_);
+      }
    }
 
    public CarvingMask m_183613_(GenerationStep.Carving p_188191_) {
-      return this.f_63166_.computeIfAbsent(p_188191_, (p_289528_) -> {
-         return new CarvingMask(this.m_141928_(), this.m_141937_());
-      });
+      synchronized (this.f_63166_) {
+         return this.f_63166_.computeIfAbsent(p_188191_, (p_289528_) -> {
+            return new CarvingMask(this.m_141928_(), this.m_141937_());
+         });
+      }
    }
 
    public void m_188186_(GenerationStep.Carving p_188187_, CarvingMask p_188188_) {
-      this.f_63166_.put(p_188187_, p_188188_);
+      synchronized (this.f_63166_) {
+         this.f_63166_.put(p_188187_, p_188188_);
+      }
    }
 
    public void m_63209_(LevelLightEngine p_63210_) {
