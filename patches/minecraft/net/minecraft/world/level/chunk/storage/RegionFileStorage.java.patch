--- a/net/minecraft/world/level/chunk/storage/RegionFileStorage.java
+++ b/net/minecraft/world/level/chunk/storage/RegionFileStorage.java
@@ -25,21 +_,29 @@
       this.f_63701_ = p_196955_;
    }
 
+   // NestWorld: synchronized on regionCache so this stays safe once a second caller
+   // exists (nestworldReadConcurrent, below, called from ChunkPrefetch's worker pool)
+   // -- previously safe only by convention (single-threaded IOWorker mailbox was the
+   // sole caller). The disk I/O itself (getChunkDataInputStream/NbtIo.read) still runs
+   // OUTSIDE this lock in both read() and nestworldReadConcurrent(), so this only
+   // serializes the cheap map lookup/eviction, not the slow part.
    private RegionFile m_63711_(ChunkPos p_63712_) throws IOException {
-      long i = ChunkPos.m_45589_(p_63712_.m_45610_(), p_63712_.m_45612_());
-      RegionFile regionfile = this.f_63699_.getAndMoveToFirst(i);
-      if (regionfile != null) {
-         return regionfile;
-      } else {
-         if (this.f_63699_.size() >= 256) {
-            this.f_63699_.removeLast().close();
+      synchronized (this.f_63699_) {
+         long i = ChunkPos.m_45589_(p_63712_.m_45610_(), p_63712_.m_45612_());
+         RegionFile regionfile = this.f_63699_.getAndMoveToFirst(i);
+         if (regionfile != null) {
+            return regionfile;
+         } else {
+            if (this.f_63699_.size() >= 256) {
+               this.f_63699_.removeLast().close();
+            }
+
+            FileUtil.m_257659_(this.f_63700_);
+            Path path = this.f_63700_.resolve("r." + p_63712_.m_45610_() + "." + p_63712_.m_45612_() + ".mca");
+            RegionFile regionfile1 = new RegionFile(path, this.f_63700_, this.f_63701_);
+            this.f_63699_.putAndMoveToFirst(i, regionfile1);
+            return regionfile1;
          }
-
-         FileUtil.m_257659_(this.f_63700_);
-         Path path = this.f_63700_.resolve("r." + p_63712_.m_45610_() + "." + p_63712_.m_45612_() + ".mca");
-         RegionFile regionfile1 = new RegionFile(path, this.f_63700_, this.f_63701_);
-         this.f_63699_.putAndMoveToFirst(i, regionfile1);
-         return regionfile1;
       }
    }
 
@@ -48,6 +_,25 @@
       RegionFile regionfile = this.m_63711_(p_63707_);
 
       try (DataInputStream datainputstream = regionfile.m_63645_(p_63707_)) {
+         return datainputstream == null ? null : NbtIo.m_128928_(datainputstream);
+      }
+   }
+
+   /** NestWorld: read-only, safe to call from multiple threads concurrently (unlike
+    *  {@link #read}, which vanilla only ever calls from IOWorker's single-threaded
+    *  mailbox). Exclusively for {@link net.nestworld.region.ChunkPrefetch}'s worker
+    *  pool -- never touches the write path, so it can never race a pending store.
+    *  Concurrent calls for DIFFERENT region files run genuinely in parallel (disk I/O
+    *  happens outside getRegionFile()'s lock); calls for the SAME region file
+    *  serialize on that RegionFile's own internal synchronization (vanilla behavior,
+    *  unchanged). Functionally identical to read() -- kept as a separate method
+    *  rather than just calling read() so the "this one is meant for concurrent
+    *  callers" contract is explicit at the call site, not just in a comment on read(). */
+   @Nullable
+   public CompoundTag nestworldReadConcurrent(ChunkPos pos) throws IOException {
+      RegionFile regionfile = this.m_63711_(pos);
+
+      try (DataInputStream datainputstream = regionfile.m_63645_(pos)) {
          return datainputstream == null ? null : NbtIo.m_128928_(datainputstream);
       }
    }
