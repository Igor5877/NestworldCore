--- a/net/minecraft/world/level/chunk/LevelChunk.java
+++ b/net/minecraft/world/level/chunk/LevelChunk.java
@@ -47,7 +_,7 @@
 import net.minecraft.world.ticks.TickContainerAccess;
 import org.slf4j.Logger;
 
-public class LevelChunk extends ChunkAccess {
+public class LevelChunk extends ChunkAccess implements net.minecraftforge.common.capabilities.ICapabilityProviderImpl<LevelChunk> {
    static final Logger f_62771_ = LogUtils.getLogger();
    private static final TickingBlockEntity f_156361_ = new TickingBlockEntity() {
       public void m_142224_() {
@@ -94,6 +_,7 @@
       this.f_62791_ = p_196861_;
       this.f_62784_ = p_196857_;
       this.f_187943_ = p_196858_;
+      this.capProvider.initInternal();
    }
 
    public LevelChunk(ServerLevel p_196850_, ProtoChunk p_196851_, @Nullable LevelChunk.PostLoadProcessor p_196852_) {
@@ -245,14 +_,14 @@
             boolean flag2 = blockstate.m_155947_();
             if (!this.f_62776_.f_46443_) {
                blockstate.m_60753_(this.f_62776_, p_62865_, p_62866_, p_62867_);
-            } else if (!blockstate.m_60713_(block) && flag2) {
+         } else if ((!blockstate.m_60713_(block) || !p_62866_.m_155947_()) && flag2) {
                this.m_8114_(p_62865_);
             }
 
             if (!levelchunksection.m_62982_(j, k, l).m_60713_(block)) {
                return null;
             } else {
-               if (!this.f_62776_.f_46443_) {
+            if (!this.f_62776_.f_46443_ && !this.f_62776_.captureBlockSnapshots) {
                   p_62866_.m_60696_(this.f_62776_, p_62865_, blockstate, p_62867_);
                }
 
@@ -295,6 +_,10 @@
    @Nullable
    public BlockEntity m_5685_(BlockPos p_62868_, LevelChunk.EntityCreationType p_62869_) {
       BlockEntity blockentity = this.f_187610_.get(p_62868_);
+      if (blockentity != null && blockentity.m_58901_()) {
+         f_187610_.remove(p_62868_);
+         blockentity = null;
+      }
       if (blockentity == null) {
          CompoundTag compoundtag = this.f_187609_.remove(p_62868_);
          if (compoundtag != null) {
@@ -312,9 +_,6 @@
                this.m_142170_(blockentity);
             }
          }
-      } else if (blockentity.m_58901_()) {
-         this.f_187610_.remove(p_62868_);
-         return null;
       }
 
       return blockentity;
@@ -322,7 +_,12 @@
 
    public void m_142170_(BlockEntity p_156391_) {
       this.m_142169_(p_156391_);
-      if (this.m_156370_()) {
+      boolean nestworldInLevel = this.m_156370_();
+      if (net.nestworld.region.NestworldDimensionRegion.nestworldMatchesTracePos(p_156391_.m_58899_())) {
+         f_62771_.info("NestWorld #28 trace: addAndRegisterBlockEntity {} on [{}] loaded={} isInLevel={} status={}",
+               p_156391_.m_58899_(), Thread.currentThread().getName(), this.f_62775_, nestworldInLevel, this.m_287138_());
+      }
+      if (nestworldInLevel) {
          Level level = this.f_62776_;
          if (level instanceof ServerLevel) {
             ServerLevel serverlevel = (ServerLevel)level;
@@ -330,6 +_,12 @@
          }
 
          this.m_156406_(p_156391_);
+         p_156391_.onLoad();
+         if (net.nestworld.region.NestworldDimensionRegion.nestworldMatchesTracePos(p_156391_.m_58899_())) {
+            f_62771_.info("NestWorld #28 trace: updateBlockEntityTicker+onLoad DID run for {}", p_156391_.m_58899_());
+         }
+      } else if (net.nestworld.region.NestworldDimensionRegion.nestworldMatchesTracePos(p_156391_.m_58899_())) {
+         f_62771_.info("NestWorld #28 trace: SKIPPED updateBlockEntityTicker+onLoad for {} (isInLevel was false)", p_156391_.m_58899_());
       }
 
    }
@@ -369,9 +_,14 @@
    public CompoundTag m_8051_(BlockPos p_62932_) {
       BlockEntity blockentity = this.m_7702_(p_62932_);
       if (blockentity != null && !blockentity.m_58901_()) {
+         try {
          CompoundTag compoundtag1 = blockentity.m_187480_();
          compoundtag1.m_128379_("keepPacked", false);
          return compoundtag1;
+         } catch (Exception e) {
+            f_62771_.error("A BlockEntity type {} has thrown an exception trying to write state. It will not persist, Report this to the mod author", blockentity.getClass().getName(), e);
+            return null;
+         }
       } else {
          CompoundTag compoundtag = this.f_187609_.get(p_62932_);
          if (compoundtag != null) {
@@ -455,7 +_,7 @@
       p_187974_.accept((p_187968_, p_187969_, p_187970_) -> {
          BlockEntity blockentity = this.m_5685_(p_187968_, LevelChunk.EntityCreationType.IMMEDIATE);
          if (blockentity != null && p_187970_ != null && blockentity.m_58903_() == p_187969_) {
-            blockentity.m_142466_(p_187970_);
+            blockentity.handleUpdateTag(p_187970_);
          }
 
       });
@@ -564,6 +_,7 @@
    }
 
    public void m_187957_() {
+      this.f_187610_.values().forEach(BlockEntity::onChunkUnloaded);
       this.f_187610_.values().forEach(BlockEntity::m_7651_);
       this.f_187610_.clear();
       this.f_156362_.values().forEach((p_187966_) -> {
@@ -573,6 +_,7 @@
    }
 
    public void m_156369_() {
+      this.f_62776_.addFreshBlockEntities(this.f_187610_.values());
       this.f_187610_.values().forEach((p_187988_) -> {
          Level level = this.f_62776_;
          if (level instanceof ServerLevel serverlevel) {
@@ -621,6 +_,41 @@
       return new LevelChunk.BoundTickingBlockEntity<>(p_156376_, p_156377_);
    }
 
+   // FORGE START
+   private final net.minecraftforge.common.capabilities.CapabilityProvider.AsField<LevelChunk> capProvider = new net.minecraftforge.common.capabilities.CapabilityProvider.AsField<>(LevelChunk.class, this);
+
+   @org.jetbrains.annotations.NotNull
+   @Override
+   public <T> net.minecraftforge.common.util.LazyOptional<T> getCapability(@org.jetbrains.annotations.NotNull net.minecraftforge.common.capabilities.Capability<T> cap, @org.jetbrains.annotations.Nullable net.minecraft.core.Direction side)
+   {
+      return capProvider.getCapability(cap, side);
+   }
+
+   @Override
+   public boolean areCapsCompatible(net.minecraftforge.common.capabilities.CapabilityProvider<LevelChunk> other)
+   {
+      return capProvider.areCapsCompatible(other);
+   }
+
+   @Override
+   public boolean areCapsCompatible(@org.jetbrains.annotations.Nullable net.minecraftforge.common.capabilities.CapabilityDispatcher other)
+   {
+      return capProvider.areCapsCompatible(other);
+   }
+
+   @Override
+   public void invalidateCaps()
+   {
+      capProvider.invalidateCaps();
+   }
+
+   @Override
+   public void reviveCaps()
+   {
+      capProvider.reviveCaps();
+   }
+   // FORGE END
+
    class BoundTickingBlockEntity<T extends BlockEntity> implements TickingBlockEntity {
       private final T f_156428_;
       private final BlockEntityTicker<T> f_156429_;
@@ -637,6 +_,7 @@
             if (LevelChunk.this.m_156410_(blockpos)) {
                try {
                   ProfilerFiller profilerfiller = LevelChunk.this.f_62776_.m_46473_();
+                  net.minecraftforge.server.timings.TimeTracker.BLOCK_ENTITY_UPDATE.trackStart(f_156428_);
                   profilerfiller.m_6521_(this::m_142280_);
                   BlockState blockstate = LevelChunk.this.m_8055_(blockpos);
                   if (this.f_156428_.m_58903_().m_155262_(blockstate)) {
@@ -652,6 +_,12 @@
                   CrashReport crashreport = CrashReport.m_127521_(throwable, "Ticking block entity");
                   CrashReportCategory crashreportcategory = crashreport.m_127514_("Block entity being ticked");
                   this.f_156428_.m_58886_(crashreportcategory);
+
+                  if (net.minecraftforge.common.ForgeConfig.SERVER.removeErroringBlockEntities.get()) {
+                     f_62771_.error("{}", crashreport.m_127526_());
+                     f_156428_.m_7651_();
+                     LevelChunk.this.m_8114_(f_156428_.m_58899_());
+                  } else
                   throw new ReportedException(crashreport);
                }
             }
@@ -682,6 +_,34 @@
       CHECK;
    }
 
+
+   /**
+    * <strong>FOR INTERNAL USE ONLY</strong>
+    * <p>
+    * Only public for use in {@link net.minecraft.world.level.chunk.storage.ChunkSerializer}.
+    */
+   @java.lang.Deprecated
+   @org.jetbrains.annotations.Nullable
+   public final CompoundTag writeCapsToNBT() {
+      return capProvider.serializeInternal();
+   }
+
+   /**
+    * <strong>FOR INTERNAL USE ONLY</strong>
+    * <p>
+    * Only public for use in {@link net.minecraft.world.level.chunk.storage.ChunkSerializer}.
+    *
+    */
+   @java.lang.Deprecated
+   public final void readCapsFromNBT(CompoundTag tag) {
+      capProvider.deserializeInternal(tag);
+   }
+
+   @Override
+   public Level getWorldForge() {
+      return m_62953_();
+   }
+
    @FunctionalInterface
    public interface PostLoadProcessor {
       void m_196866_(LevelChunk p_196867_);
@@ -716,6 +_,121 @@
 
       public String toString() {
          return this.f_156444_.toString() + " <wrapped>";
+      }
+   }
+
+   /**
+    * NestWorld: a stand-in returned by {@code ServerChunkCache.getChunk()} when the real chunk
+    * hasn't finished generating within {@code NestworldTuning.GETCHUNK_WAIT_TIMEOUT_MS} — closes
+    * the sibling of the ForceLoadCommand watchdog-crash fix for the SINGLE-chunk synchronous read
+    * path (e.g. a mod's map-teleport calling {@code Level.getChunk(x, z)} directly, the ORIGINAL
+    * crash that motivated this whole project's Phase-1 work).
+    *
+    * <p>Nested inside {@code LevelChunk} rather than a standalone top-level file: a wholly-new
+    * top-level file in a vanilla package has no baseline in Forge's patch tooling ({@code
+    * checkAndFixPatches}/{@code fixPatchImports} both assume patches modify an existing vanilla
+    * file — a {@code /dev/null}-baseline new-file patch fails their import-order validation with
+    * no way to fix it, confirmed empirically). A nested class inside an already-patched vanilla
+    * file is just more diff content against a real baseline, which the tooling already handles.
+    *
+    * <p>{@code Level.getChunk(x, z)}'s contract is {@code @NotNull} and synchronous — unlike the
+    * ForceLoadCommand fix (where "registered, still generating" was an acceptable command
+    * response), a caller here expects to get back SOMETHING immediately usable. Design reviewed
+    * with Gemini (2026-08-07): a simpler "return a fresh, uncached {@code EmptyLevelChunk} each
+    * timed-out call" was considered and REJECTED — any caller that holds onto the returned
+    * reference (not just the immediate caller, but e.g. a block entity or mod system that caches
+    * a chunk pointer across ticks) would see permanently-stale empty data even after the real
+    * chunk finishes generating in the background, since a fresh empty object each call is never
+    * connected to the real one. This class solves that: {@code ServerChunkCache} returns the SAME
+    * proxy instance for repeat calls to the same still-pending position (see its
+    * {@code nestworldPendingProxies} map), and this proxy transparently starts delegating to the
+    * real chunk the moment it's ready — any code holding this exact reference sees it "become"
+    * real without needing to re-fetch.
+    *
+    * <p>Delegates every read/write to {@link #realChunk} once set (via {@link #resolve}); before
+    * that, falls back to the exact same safe-empty behaviour as vanilla's own {@link
+    * EmptyLevelChunk} (VOID_AIR reads, no-op writes, empty block-entity map) — not a new invented
+    * fallback, the same one vanilla already uses elsewhere for "nothing here yet" reads.
+    */
+   public static class NestworldProxyLevelChunk extends LevelChunk {
+      private final net.minecraft.core.Holder<net.minecraft.world.level.biome.Biome> nestworldFallbackBiome;
+      private volatile LevelChunk realChunk;
+
+      public NestworldProxyLevelChunk(Level level, ChunkPos pos, net.minecraft.core.Holder<net.minecraft.world.level.biome.Biome> fallbackBiome) {
+         super(level, pos);
+         this.nestworldFallbackBiome = fallbackBiome;
+      }
+
+      /** Called once the real chunk finishes generating — every future call on this same
+       *  reference (whoever is still holding it) starts seeing real data immediately. */
+      public void resolve(LevelChunk real) {
+         this.realChunk = real;
+      }
+
+      public boolean isResolved() {
+         return this.realChunk != null;
+      }
+
+      public BlockState m_8055_(BlockPos pos) {
+         LevelChunk real = this.realChunk;
+         return real != null ? real.m_8055_(pos) : Blocks.f_50626_.m_49966_();
+      }
+
+      @Nullable
+      public BlockState m_6978_(BlockPos pos, BlockState state, boolean isMoving) {
+         LevelChunk real = this.realChunk;
+         return real != null ? real.m_6978_(pos, state, isMoving) : null;
+      }
+
+      public FluidState m_6425_(BlockPos pos) {
+         LevelChunk real = this.realChunk;
+         return real != null ? real.m_6425_(pos) : Fluids.f_76191_.m_76145_();
+      }
+
+      public int m_7146_(BlockPos pos) {
+         LevelChunk real = this.realChunk;
+         return real != null ? real.m_7146_(pos) : 0;
+      }
+
+      @Nullable
+      public BlockEntity m_5685_(BlockPos pos, LevelChunk.EntityCreationType creationType) {
+         LevelChunk real = this.realChunk;
+         return real != null ? real.m_5685_(pos, creationType) : null;
+      }
+
+      public void m_142170_(BlockEntity be) {
+         LevelChunk real = this.realChunk;
+         if (real != null) real.m_142170_(be);
+      }
+
+      public void m_142169_(BlockEntity be) {
+         LevelChunk real = this.realChunk;
+         if (real != null) real.m_142169_(be);
+      }
+
+      public void m_8114_(BlockPos pos) {
+         LevelChunk real = this.realChunk;
+         if (real != null) real.m_8114_(pos);
+      }
+
+      public boolean m_6430_() {
+         LevelChunk real = this.realChunk;
+         return real == null || real.m_6430_();
+      }
+
+      public boolean m_5566_(int minY, int maxY) {
+         LevelChunk real = this.realChunk;
+         return real == null || real.m_5566_(minY, maxY);
+      }
+
+      public FullChunkStatus m_287138_() {
+         LevelChunk real = this.realChunk;
+         return real != null ? real.m_287138_() : FullChunkStatus.FULL;
+      }
+
+      public net.minecraft.core.Holder<net.minecraft.world.level.biome.Biome> m_203495_(int x, int y, int z) {
+         LevelChunk real = this.realChunk;
+         return real != null ? real.m_203495_(x, y, z) : this.nestworldFallbackBiome;
       }
    }
 }
