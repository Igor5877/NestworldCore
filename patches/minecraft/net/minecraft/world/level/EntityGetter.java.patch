--- a/net/minecraft/world/level/EntityGetter.java
+++ b/net/minecraft/world/level/EntityGetter.java
@@ -53,16 +_,35 @@
    default List<VoxelShape> m_183134_(@Nullable Entity p_186451_, AABB p_186452_) {
       if (p_186452_.m_82309_() < 1.0E-7D) {
          return List.of();
+      } else if (net.nestworld.region.NestworldTuning.SKIP_EMPTY_ENTITY_COLLISION
+            && this instanceof net.minecraft.server.level.ServerLevel nestworldSl
+            && nestworldSl.nestworldCollidableCount() == 0
+            && !(p_186451_ instanceof net.minecraft.world.entity.vehicle.AbstractMinecart
+                  || p_186451_ instanceof net.minecraft.world.entity.vehicle.Boat)) {
+         // NestWorld (C1): no loaded entity can hard-block movement, so the broad-phase below
+         // would iterate the (possibly very dense) overlapping sections only to return an empty
+         // list. Skip it — this is the dominant cost of moving a huge TNT/item pile that collides
+         // with nothing. Bit-identical: the result is provably List.of() when the count is zero.
+         // Boat/minecart movers are exempt: their predicate (Boat.canVehicleCollide) also accepts
+         // merely-pushable entities (mobs, armor stands), which the counter deliberately does not
+         // track — so for them an empty result is NOT provable from the count.
+         return List.of();
       } else {
          Predicate<Entity> predicate = p_186451_ == null ? EntitySelector.f_185987_ : EntitySelector.f_20408_.and(p_186451_::m_7337_);
-         List<Entity> list = this.m_6249_(p_186451_, p_186452_.m_82400_(1.0E-7D), predicate);
+         List<Entity> list;
+         if (this instanceof Level nestworldLevel && (p_186451_ instanceof net.minecraft.world.entity.vehicle.AbstractMinecart || p_186451_ instanceof net.minecraft.world.entity.vehicle.Boat)) {
+            list = new java.util.ArrayList<>();
+            nestworldLevel.m_260826_(net.minecraft.world.level.entity.EntityTypeTest.m_156916_(Entity.class), p_186452_.m_82400_(1.0E-7D), predicate.and((nestworldOther) -> nestworldOther != p_186451_), list, net.nestworld.region.NestworldTuning.MAX_MINECART_PUSH);
+         } else {
+            list = this.m_6249_(p_186451_, p_186452_.m_82400_(1.0E-7D), predicate);
+         }
          if (list.isEmpty()) {
             return List.of();
          } else {
             ImmutableList.Builder<VoxelShape> builder = ImmutableList.builderWithExpectedSize(list.size());
 
             for(Entity entity : list) {
-               builder.add(Shapes.m_83064_(entity.m_20191_()));
+               builder.add(Shapes.m_83064_(net.nestworld.region.EntityBoundsHelper.safeBoundingBox(entity)));
             }
 
             return builder.build();
