--- a/net/minecraft/world/entity/ai/attributes/AttributeMap.java
+++ b/net/minecraft/world/entity/ai/attributes/AttributeMap.java
@@ -20,8 +_,17 @@
 
 public class AttributeMap {
    private static final Logger f_22138_ = LogUtils.getLogger();
-   private final Map<Attribute, AttributeInstance> f_22139_ = Maps.newHashMap();
-   private final Set<AttributeInstance> f_22140_ = Sets.newHashSet();
+   // NestWorld: was Maps.newHashMap()/Sets.newHashSet() (vanilla). Under NestWorld's parallel
+   // region threads, ServerEntity.sendDirtyEntityData() (main thread, post-barrier tracker walk)
+   // hands out a live reference to dirtyAttributes and iterates+clears it directly with no
+   // defensive copy -- safe in vanilla (single thread only) but a real ConcurrentModificationException
+   // when a region thread concurrently calls setBaseValue/addModifier/removeModifier on this same
+   // entity's attributes (confirmed live crash 2026-08-14, crash-2026-08-14_21.43.33-server.txt).
+   // ConcurrentHashMap-backed collections tolerate concurrent structural modification during
+   // iteration (weakly consistent) instead of throwing; getInstance()'s computeIfAbsent has the
+   // same theoretical exposure via getSyncableAttributes()'s live stream, fixed preventatively here.
+   private final Map<Attribute, AttributeInstance> f_22139_ = new java.util.concurrent.ConcurrentHashMap<>();
+   private final Set<AttributeInstance> f_22140_ = java.util.Collections.newSetFromMap(new java.util.concurrent.ConcurrentHashMap<>());
    private final AttributeSupplier f_22141_;
 
    public AttributeMap(AttributeSupplier p_22144_) {
