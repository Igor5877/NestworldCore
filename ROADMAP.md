# NestworldCore — стан, план, цільовий результат

> Оновлено: 2026-06-14, гілка `claude/nestworld-hybrid-arch-invq4h`.
> NestworldCore — форк Minecraft Forge 1.20.1: один спільний світ у одній JVM,
> порізаний на прямокутні чанк-регіони, кожен тікає власним потоком.
> Адаптивний спліт/мердж за навантаженням, безшовні переходи для гравців.

---

## 1. Що зроблено

### Ядро шардингу
- **`WorldRegion` / `WorldGrid` / `RegionTree`** — BSP-дерево прямокутних регіонів;
  початковий регіон покриває весь світ (±1 875 000 чанків), тож кожна ентіті
  завжди має власника. Спліт ріже по **медіані ентіті-навантаження** (не по
  середині простору — це створювало "гребінець" із 36 порожніх регіонів).
- **`RegionThread` / `RegionThreadPool`** — потік на регіон; протокол
  per-tick CountDownLatch; main thread під час очікування обслуговує
  `ServerChunkCache.pollTask()` (синхронні запити чанків без дедлоків).
  Порожні регіони не прокидаються. Краш потоку → аварійний сейв + респавн.
- **`RegionSplitManager`** — бюджетна евристика: спліт коли власна вартість
  регіону >25 мс (половина 50-мс бюджету) 5 оцінок поспіль, мердж <10 мс
  25 оцінок; кандидати на мердж персистяться; ручні спліти пінуються.
- **Кордони:** `BoundaryManager` (ghost-в'юхи делегують читання живому чанку),
  `BoundarySignalQueue` (крос-регіонні сигнали із затримкою ≤1 тік),
  `BoundaryEntityTransfer` (переприв'язка ентіті щотіку),
  `CrossRegionCapabilityBus` (поки не задіяний реальним навантаженням).
- **Гравці тікають на main thread** (стан спільний з network-потоком).

### Thread-safety фікси (всі підтверджені живим навантаженням)
- **`ConcurrentEntitySectionStorage`** — RW-лок над ванільними
  `LongAVLTreeSet`/`Long2ObjectOpenHashMap` секцій ентіті. Без нього паралельні
  region-потоки ламали AVL-дерево → нескінченний цикл читачів → watchdog убивав
  сервер (відтворено і виправлено).
- **`ClassInstanceMultiMap`** (вміст ентіті-секцій) — `ConcurrentHashMap` +
  `CopyOnWriteArrayList`, мутації під монітором (патч). Це був корінь
  спорадичного "Entity tick error: null" (CME без message) і крашів зупинки
  через `NaturalSpawner`: region-потік ітерував `allInstances` секції, поки
  інший потік робив add/remove. Діагностовано повними стеками, перевірено:
  0 помилок за 10 хв під навантаженням, де раніше було ~12.
- **Реєстри ентіті** (аудит off-main спавну: яйця курей, breeding, блискавка —
  усе це `addFreshEntity` з region-потоків): `EntityLookup` (byUuid →
  `ConcurrentHashMap`, byId → synchronized fastutil, ітерації по снапшоту,
  атомарний putIfAbsent), `knownUuids` і `navigatingMobs` →
  `ConcurrentHashMap.newKeySet()`, `EntityTickList` — synchronized (патчі).
  Перевірено: 10 хв, 2000 вагонеток + 300 курей-несучок — 0 помилок.
- **`Level.random`** → `RandomSource.createThreadSafe()` (патч).
- **Per-thread `CollectingNeighborUpdater`** — ванільний neighbor-updater
  рівня це один спільний `ArrayDeque`+лічильник без жодної синхронізації;
  region-потоки (scheduled/random ticks, AC) гонилися з main, ламали стек,
  і ВСІ neighbor updates світу мовчки ковталися (вода не текла, рослини не
  відривалися, важелі мертві). Усі точки входу Level/ServerLevel ідуть через
  `nestworldNeighborUpdater()`: region-потік отримує приватний лінивий
  екземпляр (дренується синхронно на зовнішньому виклику), інші потоки —
  спільний. Перевірено: вода/лампа/важіль + 3 хв soak, 6.5k ентіті,
  6 регіонів, 0 помилок (коміт 8d476fb95).
- **`LevelTicks`** schedule/add/remove/query — `synchronized` (патч).
- **`ServerLevel.blockEvent`** add — `synchronized` (патч).
- Re-entrancy guard у wire-оновленнях (скидання хендлера тільки на
  зовнішньому фреймі).

### Продуктивність
- **`ServerChunkCache` патч:** lock-free читання FULL-чанків з region-потоків
  (ванільний шлях гнав off-main доступ через main-thread round-trip — це був
  10x perf-баг) + 16-слотовий per-thread кеш чанків.
- **Alternate Current 1.9.0** (MIT, Space Walker) завендорено в
  `src/main/java/alternate/current`; хуки через Forge-патчі
  (`ServerLevel.nestworldWireHandler`, делегація з `RedStoneWireBlock`).
  Редстоун на тестовій машині: 35 → 19 мс.
- **Паралельні scheduled block/fluid ticks (Фаза 1 редстоуну):** main збирає
  належні тіки (порядок ванільний), бакетить по регіонах, регіони виконують
  паралельно окремим work-раундом всередині `ServerLevel.tick`. Прикордонна
  смуга 2 чанки + безхазяйні тіки виконуються на main ДО раунду. AC у
  region-потоці обмежений межами регіону: мережа вилазить за межі →
  `NetworkOutOfBounds` → відкат і повтор на main наступного тіку.
- **Стримування точкових хотспотів** (N ентіті в одному блоці — шардінг таке
  не паралелить принципово):
  - колізії вагонеток обмежені 8 сусідами/тік — обидва O(n²)-шляхи: push у
    `AbstractMinecart.tick` та `getEntityCollisions` у `Entity.move()`
    (bounded `getEntities` з ABORT; `-Dnestworld.maxMinecartPush`);
  - guard у спліт-менеджері пропускає спліти, які не розділяють
    ентіті-навантаження (median-cut дає меншій дитині <10% ентіті);
  - ентіті-раунд регіону обмежений бюджетом 40 мс
    (`-Dnestworld.regionEntityBudgetMs`), решта переноситься round-robin на
    наступний тік — хотспот гальмує локально, не тягнучи lockstep усього
    сервера (`deferred=` у `/nestworld status`).
  - cap поширено на човни (`Boat.tick`) і живих мобів
    (`LivingEntity.pushEntities`, `-Dnestworld.maxEntityPush`); для мобів
    ефективний ліміт піднімається до правила maxEntityCramming, тож
    cramming-урон спрацьовує по-ванільному (перевірено: 172/200 свиней
    померли від тісняви);
  - work-раунд (scheduled block/fluid ticks) ділить той самий бюджет 40 мс
    з ентіті-раундом: недовиконані Runnable переносяться на наступний тік
    (carry-over, у т.ч. для регіонів без ентіті), у ентіті-раунду гарантований
    мінімальний слайс 10 мс. `workDeferred=` у status. Гігантська
    редстоун/флюїд машина тепер теж гальмує локально, а не сервер.
  - Виміряно: 500 TNT-вагонеток в 1 блоці — було 15.8 TPS, стало 20.0;
    5000 вагонеток — 20.0 TPS (кошт хотспот-регіону обрізаний бюджетом);
    стрес бюджету 1 мс: 4150/4490 ентіті + сотні block ticks у carry-over —
    сервер 20.0 TPS, backlog повністю дренується при поверненні до 40 мс.

### Інструменти
- `/nestworld status|split|merge|borders` — серверний MSPT + вартість (мс) і
  ентіті по кожному регіону; візуалізація кордонів частинками.
- Фазовий тайминг у лог кожні 200 тіків
  (vanilla / signals / entityXfer / regionPool / ghostZones / splitMerge).
- Self-test `NESTWORLD_AUTOSPLIT=<tick>`; RCON для дев-сервера (port 25575).
- `/spark` у грі через standalone-агент (`SparkBridge`, ізольований
  URLClassLoader; jar шукається через `-Dnestworld.spark.jar` → CWD → корінь
  репо). Реєструється лише коли немає справжнього мода spark.
- **Продакшн-білд працює** (2026-06-11): `./gradlew :forge:installerJar` →
  `--installServer` → `/root/nestworld-prod` (гра 25565, RCON 25576).
  NestWorld ініціалізується, моди з `mods/` вантажаться (spark 1.10.53
  перевірено профілюванням під навантаженням: 2000 вагонеток + каскад —
  MSPT 42.3 med / 20.0 TPS, бюджет+guard працюють у проді).

### Виміряний результат (та сама лаг-машина + ~1750 ентіті, 8 ядер)
| Конфігурація | Тік | TPS |
|---|---|---|
| Ваніл-еквівалент (одна нитка + ванільний редстоун) | 79–86 мс | 11.6–12.6 |
| NestworldCore зараз | 45–48 мс | **20.0** |

Повний цикл спліт→мердж, автоспліти під навантаженням (8 робочих регіонів,
ентіті 147–368/регіон), нуль thread-safety крашів після фіксів.

---

## 2. Що потрібно зробити

### Найближче (стабільність)
- [x] ~~Діагностувати `Entity tick error: null`~~ — діагностовано і ВИПРАВЛЕНО
      (патч `ClassInstanceMultiMap`, див. розділ thread-safety фіксів).
- [x] ~~`EntityLookup` аудит~~ — зроблено і ВИПРАВЛЕНО разом із `knownUuids`,
      `EntityTickList`, `navigatingMobs` (див. thread-safety фікси).
- [x] ~~Загейтити діагностичні логи (10-сек тайминги)~~ — зроблено (7dc51f1c0):
      фазовий лог /200 тіків за `-Dnestworld.timingLog` (деф. true для dev-
      профайлінгу, false глушить у проді). Акумулятори скидаються щоінтервал
      навіть коли лог вимкнено.
- [ ] Запушити гілку (9+ локальних комітів).

### Перф-знахідки з профілювання (spark, прод, 2026-06-11)
- [x] ~~`checkAndReassign` 3.8% main~~ — частково: last-chunk кеш +
      layout-version + prune раз/с (~350 → ~200 нс/ентіті, d1c68b52).
- [ ] **Event-driven ownership:** залишковий кошт — сама ітерація по всіх
      ентіті (~200 нс × N щотіка). Прибрати повністю: section-move callback
      у PersistentEntitySectionManager → dirty-черга UUID, обробляти лише її.
- [ ] **Вибухи** (детонація 2000 TNT-вагонеток поклала сервер). УТОЧНЕНО
      2026-06-14 аудитом коду: PrimedTnt тікає на region-потоці, тож
      `Level.explode()` (рейкаст + руйнування блоків + урон) виконується ТАМ і
      ВЖЕ обмежений 40-мс ентіті-бюджетом регіону — стара примітка «main-thread
      без бюджету» застаріла. Справжній відкритий ризик інший: великий вибух
      пише блоки в чанки СУСІДНЬОГО регіону (`explosion.finalizeExplosion` →
      setBlock за межами регіону-власника) → крос-потокова гонка з його
      потоком. Тобто це thread-safety задача (border-band / маршрутизація
      блок-запису вибуху власнику чанка), а не «cap». NB: ExplosionEvent.Start-
      деферал НЕ підходить — ServerLevel.explode шле ClientboundExplodePacket
      ПІСЛЯ explode(), тож скасований-і-відкладений вибух дає dud-пакет (порожні
      блоки) гравцям цього тіку. Чистий деферал потребує патча ServerLevel.explode
      + перевірки візуалу в грі. Кандидати лишаються: батчинг рейкастів (Paper),
      виконання вибуху в work-раунді регіону-власника.

### Фаза 2 редстоуну
- [x] ~~**Block events (пістони)** у region-потоки~~ — зроблено (2c, acce6c014):
      blockEvents() більше не тікає однонитково на main; runBlockEventsPhase
      дренує чергу f_8556_ і бакетить кожну подію за позицією — інтер'єр на
      потоці регіону паралельно, смуга/безхазяйні на main ДО раунду (події
      пишуть блоки і каскадять через межі, той самий аргумент, що scheduled
      ticks). Нетікабельні чанки → reschedule, успішні → broadcast.
      Перевірено живим сервером: поршень extend+retract на region-треді,
      0 помилок, 20.0 TPS.
- [x] ~~Random ticks у region-потоки~~ — зроблено (2a): tickChunk маршрутизує
      interior-чанки в бакети регіонів (та сама смуга 2 чанки → main),
      flush після циклу tickChunks, витягнутий метод без профайлера.
- [x] ~~Block entities у region-потоки~~ — зроблено (2c, e9fd9b3f8):
      tickBlockEntities збирає тікери → interior у бакети регіонів,
      смуга/unowned на main; сатуровані регіони СКИДАЮТЬ бакет (BE-тікери
      перезбираються щотіка — черга росла б необмежено). КРИТИЧНО:
      ванільний Level.getBlockEntity тихо повертає null off-main (guard
      1.17+) — воронки тікались, але "не бачили" скриню; guard тепер
      пропускає RegionThread. Той самий клас багів, що getChunkNow.
      Діагностика: BE phase лог /200 тіків + -Dnestworld.beTracePos=x,y,z.
- [x] ~~Load-aware розрізи~~ — зроблено (8e434bc70): median-cut сідав рівно
      на лаг-машину (вона і Є медіана ентіті) — двічі живцем спостерігали
      кут 4 регіонів точно на машині. Тепер кандидати скоряться за
      зайнятістю смуги, тиха лінія перемагає, нема тихої — регіон не
      ріжеться. Живий результат: машина 10 → 17-18 TPS.
- [x] ~~**Block-tick щільність у скоринг розрізів**~~ — зроблено у два кроки:
      (1) BlockTickHeat — sparse per-chunk rolling-window лічильник усіх
      scheduled block/fluid/BE тіків, вливається в гістограму скорера з вагою
      CUT_HEAT_WEIGHT (15d1eeaa1); (2) скорер тепер оцінює **обидві осі** й
      бере розріз із найменшим навантаженням у смузі — раніше він був замкнений
      на довшу вісь (preferredSplitAxis), тож машину, яку чисто розділяє лише
      перпендикулярний розріз, лишав колонкою в смузі. RegionTree дістав
      split(region, axis, cut); scoreCut повертає ScoredCut(cut, band) для
      порівняння осей. Детермінований self-test (NESTWORLD_CUT_TEST) розширено
      крос-осьовим кейсом — PASS. Живцем: спліт чисто розвів spawn-heat у
      інтер'єри дочірніх регіонів (жоден гарячий чанк не в смузі), 20 TPS.
- [x] ~~**Персистентна розкладка регіонів**~~ — зроблено (2026-06-14): на
      старті дерево більше не будується з нуля (раніше перші 2-4 с весь світ =
      1 регіон на 1 потоці + 100 тіків до першого спліту — скарга гравця
      2026-06-12). `RegionTree.writeNbt()/RegionTree(grid, CompoundTag)`
      серіалізують BSP рекурсивно (branch: axis+діти; leaf: межі+pinned; id та
      live-стан НЕ зберігаються, створюються свіжими). `NestworldRegionSystem`
      пише `<world>/data/nestworld-regions.dat` у shutdown ПЕРЕД зупинкою
      потоків і вантажить у init — спавн потоків відкладено до створення всіх
      підсистем (boundary/transfer), які чіпає RegionThread.tick. Корумпований/
      відсутній файл → тихий fallback на 1 регіон (стара поведінка, сумісність
      зі старими світами). Верифіковано RCON: split×2 → stop (Saved 3 region(s))
      → restart → "restored saved layout — 3 region(s)" з ІДЕНТИЧНИМИ межами,
      3 потоки на старті без очікування; merge сиблінгів після рестарту працює
      (вкладення дерева, не лише плоский список, відновлюється коректно).
      Беклог: зберігати тільки в shutdown — краш сервера втрачає розкладку
      (періодичний save на автосейві — TODO); потоки спавняться на старті для
      всіх збережених регіонів незалежно від навантаження (мердж розрядить).
- [ ] **Розумніший вибір лінії розрізу:** медіана по ентіті зараз ріже прямо
      через машини → їхні block ticks потрапляють у прикордонну смугу і
      виконуються на main (спостережено: `vanilla` фаза ~20 мс саме через це).
      Зважувати розріз щільністю scheduled ticks; розглянути смугу 1 чанк
      після аудиту глибини каскадів.

### Сумісність із модами (стратегія: працює "з коробки" або пінується)
> **Перевірено живими модами 2026-06-14:** Forge-нативні моди працюють і
> прискорюються — **Create** (block entities тікають ванільно → у region-бакети,
> 8859 cogwheels: due=8859 main=0, реально паралелиться), **AE2** (сумісний, але
> тікає себе на main через власний ServerTickEvent → шардинг не прискорює).
> **🔴 Sinytra Connector (Fabric-на-Forge паки) НЕ підтримуються:** наш білд
> перекомпільовує/патчить MC (LivingEntity для maxEntityPush) → зсуває synthetic
> lambda, які мікшить Fabric API (`fabric-entity-events-v1`); на стоку Forge
> проходить, на нашому — Critical injection failure → жоден Fabric-мод не
> вантажиться (тест ZombieCraft, 403 моди). TODO: або не патчити класи, які
> таргетить Fabric API, або задокументувати як офіційне обмеження.
> Тріада пінінгу ПОВНА (2026-06-14): тип ентіті + тип BE + modid цілком
> (`/nestworld pinmod <ns>`, 0d7231315 — пінить усі ентіті й BE неймспейсу;
> файл `nestworld-pinned-mods.txt`; verified pinmod minecraft → region 114→0,
> BE 50→main, unpinmod реверс). Плюс opt-in auto-pin за помилками.
- [x] ~~**Thread-pinning конфіг (ентіті)**~~ — зроблено (2026-06-14):
      `NestworldPins` тримає Set<EntityType> (швидка перевірка) + Set<id>
      (персистенс у `<world>/data/nestworld-pins.txt`, людино-редагований).
      Pinned-тип НІКОЛИ не потрапляє в region.ownedEntityIds
      (`BoundaryEntityTransfer` пропускає й знімає з власника), а тікається на
      main у `tickPinnedEntitiesOnMain()` ПІСЛЯ гравців — дзеркалить region-gate
      (removed/passenger/checkDespawn/isPositionEntityTicking/tickNonPassenger).
      Нуль кошту коли піни порожні (guard на isEmpty). Команди
      `/nestworld pin|unpin|pins`, ефект із наступного тіку. Верифіковано RCON:
      pin zombie → region #0 83→63 (−20, вийшли з власності), піни-файл
      записаний, 0 помилок; гравітаційний тест — pinned-зомбі з y=250 падає на
      термінальній швидкості (повноцінний per-tick тікінг на main), unpin
      повертає region-тікінг. Беклог: пінити modid цілком і типи BE (BE зараз
      тікаються в region-фазі — інша точка вмикання); passenger pinned-типу
      тікається потоком vehicle (рідкісний кейс).
- [x] ~~**Thread-pinning BE-типів**~~ — зроблено (2026-06-14, afafae17d):
      `NestworldPins` дістав Set<String> pinnedBeIds (+ файл
      `nestworld-be-pins.txt`). BE-фаза (`runBlockEntityPhase`) звіряє кожен
      тікер за `TickingBlockEntity.getType()` (рядок registry-id) і шле pinned
      типи в main-бакет незалежно від позиції; нуль кошту коли порожньо.
      `/nestworld pin|unpin` авто-визначають тип (ентіті vs BE), `pins` показує
      обидва розділи. Верифіковано RCON: pin minecraft:hopper → усі 18 hopper-
      тікерів з region-бакета #0 у main (due=52 без змін, main 0→18, bucket
      52→34), unpin повертає (main 18→0), 0 помилок.
- [x] ~~**Auto-pin** (opt-in)~~ — зроблено (2026-06-14, 0890a1e0b):
      `NestworldPins.noteEntityTickError` лічить помилки per-EntityType у catch
      `RegionThread.tickEntities`; на порозі пінить тип на main, логує гучно з
      підказкою `/nestworld unpin`. OFF за замовчуванням
      (`-Dnestworld.autoPin=true`, поріг `-Dnestworld.autoPinThreshold=20`) —
      транзієнтна гонка інакше запінила б поширений тип і тихо серіалізувала його
      (TNT-вагонетки лаг-машини впали б на main). Коли off — рання гілка return,
      нуль зміни поведінки. Детермінований self-test NESTWORLD_AUTOPIN_TEST: PASS.
      Лишилось (опц.): auto-pin BE-типів за тим самим механізмом.
- [x] ~~Прогнати `CrossRegionCapabilityBus` / `BoundarySignalQueue`~~ — зроблено
      2026-06-14 (RCON-тест на живому кордоні #10|#11): редстоун-сигнал перетинає
      межу в обидва боки (вмик+вимик, дріт гасне коректно), хопер #10→скриня #11
      перекинув 31 діамант (capability bus / cross-region getBlockEntity), вода
      потекла через кордон з правильними рівнями. 0 помилок. Gap #3 закрито для
      vanilla-механік; модові крос-регіонні capability (енергія/предмети модів)
      ще не гнали.
- [ ] Задокументувати, які Forge-події можуть прилітати з region-потоків.

### Паритет з ванілою / робастність
- [x] ~~BE-тікінг: gate по ticking-чанках~~ — насправді ВЖЕ зроблено
      (аудит 2026-06-14): патчений `Level.tickBlockEntities` (Level.java:481)
      збирає тікер у nestworldDue лише якщо `shouldTickBlocksAt(ticker.getPos())`
      — це і є ванільний ticking-chunk гейт. Примітка «усі завантажені чанки»
      була застарілою.
- [~] Ізоляція краша регіону — ЧАСТКОВО зроблено (2026-06-14, eaf398786):
      respawn-цикл зупинено (ковзне вікно 5хв, експоненційний backoff
      5/10/20/40/60с, після maxRegionCrashes=5 регіон DISABLED, сервер живе
      далі). Верифіковано інʼєкцією краша. Лишилось: тікати disabled-регіон на
      main замість заморозки його ентіті.
- [ ] Інші виміри (Nether/End) — зараз шардиться лише overworld.
- [ ] Вирішити: чи персистити розкладку регіонів через рестарт
      (зараз скидається до 1 регіону — швидко відновлюється автосплітами).

### Масштабне тестування
- [ ] 50+ гравців (боти), кілька машин у різних кутках світу, спостереження
      за ghost zones / entity transfer / capability bus під реальним крос-
      регіонним трафіком.

---

## 3. Фінальний цільовий результат

**Один спільний світ в одній JVM, що масштабується по ядрах.**

1. **Економіка пам'яті:** моди вантажаться один раз (~93% економії RAM проти
   N окремих серверів), нема IPC, спільний стан.
2. **Масштабування:** ентіті + block ticks розподіляються по регіонах ≈ ядрах.
   Приймальний тест: сумарне навантаження ≥150 мс однониткової роботи
   (велика лаг-машина + ферми) тримає **стабільні 20 TPS на 8 ядрах**, коли
   ваніла на тому ж залізі давала б 5–7 TPS.
3. **Безшовність:** гравець бачить один світ без швів; будь-яка взаємодія
   через кордон регіонів працює із затримкою ≤1 тік (модель
   BoundarySignalQueue), семантика редстоуну всередині регіону — ванільна.
4. **Стабільність:** нуль thread-safety крашів; краш одного регіону не валить
   сервер і не чіпає інші регіони.
5. **Моди:** добре написані моди працюють без змін; проблемні — пінуються на
   main thread конфігом або auto-pin'ом; для крос-регіонних взаємодій є
   документований API (signal queue / capability bus).
6. **Прозорість:** `/nestworld status` + метрики дають адмінові повну картину:
   хто скільки коштує, де кордони, чому стався спліт.

---

## Майбутня архітектура / порт на інші версії (план 2026-06-14)
- ✅ **DONE: cap-колізій сумісний із Lithium-родиною** (Canary/radium/lithium):
  cap гейтнуто `>= 0`; при `< 0` зберігається ванільний getEntities-виклик → їхній
  @Redirect інʼєктиться. Покрито LivingEntity/Boat/AbstractMinecart, перевірено з Canary 0.3.3 (8f81d0ebc).
- **Порт на інші версії — стратегія:** зараз форк Forge (source-патчі перекомпілюють MC)
  → версіє-специфічно + конфлікти. ПРАВИЛЬНИЙ ШЛЯХ: переписати інтеграцію як **Mixin/coremod**
  (JAR-мод, не форк) → портабельно між версіями + NeoForge, без конфліктів FFAPI/Canary
  (Mixin не зсуває synthetic'и). Логіка net.nestworld.region версіє-незалежна; версіє-
  специфічне лише інтеграційне склеювання (tick/chunk/entity hooks).
- **Deferred-spawn:** бюджетувати дренаж для велетенських сплесків спавну.

## Перформанс-беклог (з spark-аналізу 2026-06-15)
- ✅ **DONE #1: fill-idle-cores split** (367e2ea4e, ddf9602c6) — ділити найгарячіший
  регіон коли ядра простоюють (28% CPU); ціль=cores-1; анти-трешинг merge-suppression;
  порожні регіони зливаються. Перевірено наживо (1→8 регіонів, 57→8мс).
- ⏭ **#2 балансування за вартістю + work-stealing** — main чекає найповільніший регіон
  (96с awaitLatch). Різати за tick-cost (частково є load-aware cut); дати простоюючим
  region-потокам «красти» роботу перевантаженого. РИЗИК: спільні черги + бар'єр.
- ⏭ **#3 злити work-round + entity-round бар'єри** — 2 послідовні бар'єри = подвійний
  штраф за дисбаланс. Пайплайн/злиття. РИЗИК: порядок ghost-zone між фазами.
- ❌ **#4 entity-tracking off-main — ВИМІРЯНО: НЕ ВАРТО** (docs/PARALLEL_TRACKING_DESIGN.md),
  реалізація чекає тесту з реальними гравцями. Безпечний підхід: партиція по регіону-
  власнику + 2 фази з бар'єром; Netty-send потокобезпечний. ЗАСТЕРЕЖЕННЯ: цінність ∝
  к-сть гравців (на ядрі main простоює — виграш лише на populated); mod-pairing хуки +
  баги видно лише наживо → не пушу всліпу. ВИМІРЯНО (20 ботів+800 ентіті): tracking ~5мс на вже-вільному main, бар'єр регіонів 40мс → нуль виграшу. Реальне вузьке місце — entity-AI; відповідь = Canary зверху (#5).
- 💡 **#5 рекомендувати стек core + Canary/radium** — тепер сумісно; Canary прискорює
  Mob.tick на КОЖНОМУ region-потоці (синергія).

## Стрес-тест join/leave (2026-06-15, docs/STRESS_TEST_CHURN.md)
- Ядро тримало 20 TPS до 550 churning-ботів (12 регіонів). Межа = vanilla конект/chunk-load на main, НЕ ядро. Конект-DoS → окремий шар (проксі/anti-bot/throttle), не шардинг.

